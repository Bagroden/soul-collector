# Обновление звуков атак крысы

## Дата: 28 октября 2025

## Описание
Изменены звуки для способности "Крысиный укус" и добавлен звук для базовой атаки крысы.

## Изменения

### 1. Регистрация звука в SoundManager (`Scripts/SoundManager.gd`)

**Добавлено:**
```gdscript
sound_effects["rat_attack1"] = preload("res://Audio/Sounds/rat_attack1.wav")
```

Звук `rat_attack1` зарегистрирован в `_load_sound_effects()` для доступа через `SoundManager.play_sound()`.

### 2. Звук способности "Крысиный укус" (`Scripts/Abilities/EnemyAbility.gd`)

**Было:**
```gdscript
SoundManager.play_sound("rat_bite", -10.0)
```

**Стало:**
```gdscript
SoundManager.play_sound("rat_attack1", -10.0)
```

**Изменения внесены в двух местах:**
- Строка ~102: В специальной обработке `rat_bite_attack`
- Строка ~480: В общем match для звуков способностей

### 3. Звук базовой атаки крысы (`Scripts/Battle/battle_manager.gd`)

**Добавлено:**
```gdscript
elif damage_type == "physical" and enemy_node.display_name == "Крыса":
    # Звук базовой атаки крысы
    var audio_player = AudioStreamPlayer.new()
    audio_player.stream = load("res://Audio/Sounds/rat_attack1.wav")
    audio_player.volume_db = -10.0
    audio_player.bus = "Master"
    add_child(audio_player)
    audio_player.play()
    # Удаляем после проигрывания
    audio_player.finished.connect(func(): audio_player.queue_free())
```

**Место добавления:** Функция `_enemy_action()`, блок обработки базовой атаки врага (`if not ability_used:`), после проверки на магическую атаку.

## Файл звука
- **Путь**: `Audio/Sounds/rat_attack1.wav`
- **Громкость**: -10.0 dB
- **Формат**: WAV

## Когда проигрывается

### Способность "Крысиный укус":
- Враг "Крыса" использует активную способность "Крысиный укус" (`rat_bite_attack`)
- Проигрывается во время использования способности
- Громкость: -10.0 dB

### Базовая атака крысы:
- Враг "Крыса" делает обычную атаку (когда способность на перезарядке)
- `damage_type == "physical"`
- `enemy_node.display_name == "Крыса"`
- Громкость: -10.0 dB

## Технические детали

### Проверка имени врага
Для базовой атаки используется проверка `enemy_node.display_name == "Крыса"` чтобы звук проигрывался только для врага-крысы, а не для всех врагов с физической атакой.

### Громкость
Установлена громкость `-10.0 dB` (тише чем у других врагов) так как крыса - мелкий враг.

### Система воспроизведения
Используется динамическое создание `AudioStreamPlayer`:
- Загрузка звука через `load()`
- Настройка громкости и шины
- Автоматическое удаление после проигрывания через `finished.connect()`

## Статус
🟢 **Завершено** - Все изменения внесены и проверены линтером

## Примечания
- Звук `rat_attack1.wav` используется как для способности "Крысиный укус", так и для базовой атаки крысы
- Это создает единый звуковой образ для крысы в бою
- Громкость -10.0 dB подобрана с учетом размера врага

