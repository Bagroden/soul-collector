# План системы изучения активных способностей врагов

## 🎯 Концепция системы

Игрок может изучать активные способности врагов, побеждая их в бою, и использовать эти способности в бою через специальные слоты. При использовании способности врага появляется призрачная копия этого врага.

---

## 📋 Основные компоненты

### **1. Слоты способностей**
- **Собственные способности ГГ:** 2 слота (фиксированные)
- **Способности врагов:** 1 слот (изначально)
- **Расширение через мета-прогрессию:** до 3-5 слотов для способностей врагов

### **2. Механика изучения**
- Аналогично пассивным способностям
- Прогресс за победу над врагами
- 100% = способность изучена
- Сохранение прогресса между сессиями

### **3. Визуальная система**
- Исчезновение спрайта ГГ при использовании способности врага
- Появление призрачной копии врага
- Анимация способности врага
- Возврат спрайта ГГ после анимации

---

## 🏗️ Архитектура системы

### **Новые файлы:**

```
Scripts/Systems/
├── ActiveAbilityLearningSystem.gd      # Система изучения активных способностей
├── PlayerActiveAbilityManager.gd       # Управление слотами и использованием
└── EnemyGhostVisual.gd                 # Призрачная копия врага

Scripts/UI/
├── ActiveAbilityLearningWindow.gd      # UI изучения активных способностей
└── ActiveAbilitySlotUI.gd              # UI слотов в бою

Scenes/UI/
├── ActiveAbilityLearningWindow.tscn    # Сцена окна изучения
└── ActiveAbilitySlotPanel.tscn         # Панель слотов в бою

Data/
└── PlayerActiveAbilities.tres          # Конфигурация способностей ГГ
```

---

## 📊 Структура данных

### **1. ActiveAbilityLearningSystem.gd**

```gdscript
class_name ActiveAbilityLearningSystem
extends Node

# Конфигурация изучения активных способностей
var learning_config = {
    # Крыса
    "rat_bite": {
        "ability_name": "Крысиный укус",
        "ability_id": "rat_bite",
        "rarity": "common",
        "required_progress": 100,
        "enemy_type": "rat",
        "sources": {
            "common": 10,    # Обычная крыса: +10%
            "uncommon": 15,  # Необычная: +15%
            "rare": 20,      # Редкая: +20%
            "epic": 25,      # Эпическая: +25%
            "legendary": 30  # Легендарная: +30%
        }
    },
    
    # Слизень
    "acid_blast": {
        "ability_name": "Кислотный взрыв",
        "ability_id": "acid_blast",
        "rarity": "rare",
        "required_progress": 100,
        "enemy_type": "slime",
        "sources": {
            "rare": 15,
            "epic": 20,
            "legendary": 25
        }
    },
    
    # И так далее для всех врагов...
}

# Прогресс изучения
var learning_progress = {}  # {ability_id: {current_progress, is_learned, learned_at}}

# Сигналы
signal ability_learned(ability_id: String, ability_name: String)
signal progress_updated(ability_id: String, current_progress: int, required_progress: int)

# Методы
func add_progress(enemy_type: String, enemy_rarity: String) -> void
func get_ability_progress(ability_id: String) -> Dictionary
func is_ability_learned(ability_id: String) -> bool
func get_learned_abilities() -> Array[String]
func learn_ability(ability_id: String) -> void
```

### **2. PlayerActiveAbilityManager.gd**

```gdscript
class_name PlayerActiveAbilityManager
extends Node

# Слоты способностей
var player_ability_slots = [
    {"type": "player", "ability_id": "spiritual_strike", "slot_index": 0},
    {"type": "player", "ability_id": "kinetic_strike", "slot_index": 1}
]

var enemy_ability_slots = [
    {"type": "enemy", "ability_id": "", "slot_index": 0}  # Пустой слот
]

# Максимум слотов (из мета-прогрессии)
var max_enemy_slots = 1

# Методы
func equip_enemy_ability(ability_id: String, slot_index: int) -> bool
func unequip_enemy_ability(slot_index: int) -> void
func get_equipped_abilities() -> Array[Dictionary]
func use_ability(slot_index: int, is_enemy_ability: bool) -> Dictionary
func unlock_enemy_slot() -> void  # Через мета-прогрессию
```

### **3. EnemyGhostVisual.gd**

```gdscript
class_name EnemyGhostVisual
extends Node2D

# Компоненты
var ghost_sprite: AnimatedSprite2D
var original_enemy_scene: String
var ability_animation: String

# Визуальные параметры
var ghost_opacity = 0.6
var ghost_tint = Color(0.5, 0.8, 1.0, ghost_opacity)  # Голубоватый призрачный оттенок

# Методы
func create_ghost(enemy_scene_path: String, ability_id: String) -> void
func play_ability_animation() -> void
func fade_in(duration: float = 0.3) -> void
func fade_out(duration: float = 0.3) -> void
```

---

## 🔄 Пошаговый план реализации

### **Этап 1: Базовая система изучения (2-3 часа)**

#### **Шаг 1.1: Создать ActiveAbilityLearningSystem**
- [ ] Создать файл `Scripts/Systems/ActiveAbilityLearningSystem.gd`
- [ ] Добавить конфигурацию для всех активных способностей врагов
- [ ] Реализовать методы `add_progress()`, `is_ability_learned()`, `get_learned_abilities()`
- [ ] Добавить систему сигналов
- [ ] Реализовать сохранение/загрузку прогресса (`user://active_ability_learning.save`)

**Тестирование:**
```gdscript
# Добавляем прогресс вручную
ability_system.add_progress("rat", "common")  # +10%
ability_system.add_progress("rat", "rare")    # +20%
print(ability_system.get_ability_progress("rat_bite"))  # {current: 30, required: 100}
```

#### **Шаг 1.2: Интеграция с боевой системой**
- [ ] В `battle_manager.gd` добавить вызов `ActiveAbilityLearningSystem.add_progress()`
- [ ] Определять `enemy_type` по имени врага (например, "Крыса" → "rat")
- [ ] Определять `enemy_rarity` из `enemy_node.rarity`
- [ ] Добавить уведомление о прогрессе в BattleLog

**Интеграция:**
```gdscript
# В battle_manager.gd::_on_enemy_death()
func _add_active_ability_learning_progress(enemy_node: Node2D):
    var active_learning = get_node_or_null("/root/ActiveAbilityLearningSystem")
    if active_learning:
        var enemy_type = _get_enemy_type_from_name(enemy_node.display_name)
        var enemy_rarity = enemy_node.rarity.to_lower()
        active_learning.add_progress(enemy_type, enemy_rarity)
```

---

### **Этап 2: UI системы изучения (3-4 часа)**

#### **Шаг 2.1: Создать окно изучения**
- [ ] Создать `ActiveAbilityLearningWindow.tscn`
- [ ] Добавить:
  - Список способностей с прогресс-барами
  - Кнопки "Изучить" (активны при 100%)
  - Кнопки "Подробнее"
  - Фильтрация по редкости
- [ ] Создать скрипт `ActiveAbilityLearningWindow.gd`
- [ ] Реализовать отображение прогресса
- [ ] Добавить обработку изучения способностей

#### **Шаг 2.2: Добавить доступ к окну**
- [ ] В `CharacterPreparation.gd` добавить кнопку "ИЗУЧЕНИЕ АКТИВНЫХ СПОСОБНОСТЕЙ"
- [ ] Подключить открытие окна `ActiveAbilityLearningWindow`
- [ ] Стилизовать кнопку (например, красный цвет для активных способностей)

---

### **Этап 3: Система слотов (4-5 часов)**

#### **Шаг 3.1: Создать PlayerActiveAbilityManager**
- [ ] Создать файл `Scripts/Systems/PlayerActiveAbilityManager.gd`
- [ ] Реализовать управление слотами
- [ ] Добавить методы экипировки/снятия способностей
- [ ] Реализовать сохранение/загрузку экипированных способностей
- [ ] Добавить autoload в `project.godot`

**Структура слотов:**
```gdscript
# Слоты игрока (фиксированные)
Slot 0: Духовный удар (spiritual_strike)
Slot 1: Кинетический удар (kinetic_strike)

# Слоты врагов (расширяемые)
Enemy Slot 0: [Пусто или Крысиный укус]
Enemy Slot 1: [Заблокирован]
Enemy Slot 2: [Заблокирован]
```

#### **Шаг 3.2: Создать UI слотов в бою**
- [ ] Создать `ActiveAbilitySlotPanel.tscn`
- [ ] Отобразить все доступные слоты
- [ ] Добавить иконки способностей
- [ ] Показать кулдауны
- [ ] Добавить хоткеи (1, 2, 3 для игрока, 4, 5, 6 для врагов)
- [ ] Интегрировать в `battle_manager.gd`

#### **Шаг 3.3: UI экипировки способностей**
- [ ] В `ActiveAbilityLearningWindow` добавить раздел "Экипированные способности"
- [ ] Показать все доступные слоты для способностей врагов
- [ ] Drag & Drop для экипировки (опционально)
- [ ] Кнопки "Экипировать"/"Снять"

---

### **Этап 4: Визуальная система призраков (5-6 часов)**

#### **Шаг 4.1: Создать EnemyGhostVisual**
- [ ] Создать файл `Scripts/Battle/EnemyGhostVisual.gd`
- [ ] Загружать сцену врага динамически
- [ ] Применять призрачный эффект (opacity + tint)
- [ ] Реализовать fade in/fade out анимации
- [ ] Позиционировать призрак на месте ГГ

**Визуальный эффект:**
```gdscript
func create_ghost(enemy_scene_path: String):
    var enemy_scene = load(enemy_scene_path)
    var enemy_instance = enemy_scene.instantiate()
    
    # Находим AnimatedSprite2D
    var sprite = enemy_instance.get_node("Visual")
    
    # Применяем призрачный эффект
    sprite.modulate = Color(0.5, 0.8, 1.0, 0.6)  # Голубой полупрозрачный
    sprite.scale *= 1.2  # Немного крупнее
    
    add_child(enemy_instance)
    ghost_sprite = sprite
```

#### **Шаг 4.2: Интеграция с боевой системой**
- [ ] В `battle_manager.gd` добавить метод `_use_enemy_ability()`
- [ ] При использовании способности врага:
  1. Скрыть спрайт ГГ (`player_visual.visible = false`)
  2. Создать призрак врага в позиции ГГ
  3. Проиграть анимацию способности на призраке
  4. Применить эффект способности
  5. Удалить призрак
  6. Показать спрайт ГГ (`player_visual.visible = true`)

**Последовательность анимации:**
```gdscript
func _use_enemy_ability(ability_id: String, target: Node2D):
    # 1. Скрываем ГГ
    player_visual.modulate.a = 0.0  # Fade out за 0.2 сек
    await get_tree().create_timer(0.2).timeout
    
    # 2. Создаем призрак
    var ghost = EnemyGhostVisual.new()
    ghost.position = player_node.position
    add_child(ghost)
    
    var enemy_scene = _get_enemy_scene_for_ability(ability_id)
    ghost.create_ghost(enemy_scene, ability_id)
    ghost.fade_in(0.3)
    await get_tree().create_timer(0.3).timeout
    
    # 3. Анимация способности
    await ghost.play_ability_animation()
    
    # 4. Применяем эффект
    var ability_result = _apply_ability_effect(ability_id, target)
    
    # 5. Убираем призрак
    ghost.fade_out(0.3)
    await get_tree().create_timer(0.3).timeout
    ghost.queue_free()
    
    # 6. Возвращаем ГГ
    player_visual.modulate.a = 1.0  # Fade in за 0.2 сек
```

---

### **Этап 5: Система способностей ГГ (3-4 часа)**

#### **Шаг 5.1: Создать конфигурацию способностей ГГ**
- [ ] Создать `Data/PlayerActiveAbilities.tres`
- [ ] Определить базовые способности:
  - **Духовный удар** (spiritual_strike) - текущая способность
  - **Кинетический удар** (kinetic_strike) - новая способность
- [ ] Для каждой способности указать:
  - Название, описание, иконка
  - Базовый урон, стоимость маны
  - Кулдаун, анимация
  - Тип урона (физический/магический)

**Пример конфигурации:**
```gdscript
# PlayerActiveAbilities.gd
class_name PlayerActiveAbilities
extends Resource

var abilities = {
    "spiritual_strike": {
        "name": "Духовный удар",
        "description": "Мощная магическая атака",
        "base_damage": 50,
        "mana_cost": 10,
        "cooldown": 0,
        "damage_type": "magical",
        "animation": "spiritual_strike_anim"
    },
    
    "kinetic_strike": {
        "name": "Кинетический удар",
        "description": "Быстрая физическая атака",
        "base_damage": 40,
        "mana_cost": 15,
        "cooldown": 1,
        "damage_type": "physical",
        "animation": "kinetic_strike_anim"
    }
}
```

#### **Шаг 5.2: Интеграция в бой**
- [ ] Модифицировать `battle_manager.gd::use_player_ability()`
- [ ] Добавить выбор способности (1, 2, 3 клавиши)
- [ ] Проверка маны и кулдаунов
- [ ] Применение модификаторов от пассивных способностей

---

### **Этап 6: Модификаторы и баланс (2-3 часа)**

#### **Шаг 6.1: Интеграция с пассивными способностями**
Пассивные способности должны влиять на **ВСЕ** активные способности:

**Примеры модификаторов:**
- **Сила (Strength)**: +физический урон для способностей врагов
- **Интеллект (Intelligence)**: +магический урон для способностей врагов
- **Крепость (Fortress)**: -стоимость маны для всех способностей
- **Быстрота (Speed)**: -кулдаун для всех способностей
- **Двойной удар**: шанс сработать и для способностей врагов

**Реализация:**
```gdscript
func _calculate_ability_damage(base_damage: float, damage_type: String) -> float:
    var final_damage = base_damage
    
    # Пассивные модификаторы
    var passive_manager = get_node_or_null("/root/PassiveAbilityManager")
    if passive_manager:
        # Физический урон
        if damage_type == "physical":
            final_damage += passive_manager.get_bonus("physical_damage")
            final_damage *= (1.0 + passive_manager.get_bonus("strength") * 0.02)
        
        # Магический урон
        elif damage_type == "magical":
            final_damage += passive_manager.get_bonus("magic_damage")
            final_damage *= (1.0 + passive_manager.get_bonus("intelligence") * 0.02)
        
        # Критический удар
        if passive_manager.roll_critical():
            final_damage *= 2.0
    
    return final_damage
```

#### **Шаг 6.2: Баланс способностей**
- [ ] Способности врагов должны иметь урон ~70-80% от оригинала
- [ ] Добавить стоимость маны (10-30 маны)
- [ ] Добавить кулдауны (1-3 хода)
- [ ] Некоторые способности адаптировать под ГГ (например, "Кислотный взрыв" не снижает броню союзника)

---

### **Этап 7: Мета-прогрессия (1-2 часа)**

#### **Шаг 7.1: Разблокировка слотов**
- [ ] Добавить в систему развития души улучшения:
  - **"Дополнительный слот способностей I"**: +1 слот (50 осколков душ)
  - **"Дополнительный слот способностей II"**: +1 слот (100 осколков душ)
  - **"Дополнительный слот способностей III"**: +1 слот (200 осколков душ)
- [ ] При покупке вызывать `PlayerActiveAbilityManager.unlock_enemy_slot()`
- [ ] Обновлять UI слотов в бою

---

### **Этап 8: Тестирование и полировка (2-3 часа)**

#### **Шаг 8.1: Тестовые сценарии**
- [ ] **Тест 1**: Изучение способности "Крысиный укус"
  - Победить 10 обычных крыс
  - Проверить прогресс 100%
  - Изучить способность
  - Экипировать в слот
- [ ] **Тест 2**: Использование способности в бою
  - Использовать "Крысиный укус" по врагу
  - Проверить появление призрака крысы
  - Проверить применение урона
  - Проверить возврат спрайта ГГ
- [ ] **Тест 3**: Модификаторы
  - Активировать пассивную способность "Сила"
  - Проверить увеличение урона "Крысиного укуса"
  - Активировать "Двойной удар"
  - Проверить срабатывание для способности врага
- [ ] **Тест 4**: Мета-прогрессия
  - Разблокировать 2-й слот
  - Экипировать 2 способности врагов
  - Использовать обе в бою

#### **Шаг 8.2: Баланс**
- [ ] Проверить урон всех способностей врагов
- [ ] Настроить стоимость маны
- [ ] Настроить кулдауны
- [ ] Проверить визуальные эффекты

---

## 🎨 Визуальные особенности

### **Призрачный эффект:**
- **Цвет**: Голубоватый оттенок (Color(0.5, 0.8, 1.0))
- **Прозрачность**: 60% (alpha = 0.6)
- **Размер**: 120% от оригинала (scale = 1.2)
- **Анимации**: Плавное появление/исчезновение (0.3 сек)

### **UI слотов:**
- **Собственные способности**: Зеленая рамка
- **Способности врагов**: Красная рамка
- **Заблокированные слоты**: Серая рамка с замком

---

## 📝 Структура сохранения

### **active_ability_learning.save**
```json
{
  "learning_progress": {
    "rat_bite": {
      "current_progress": 100,
      "is_learned": true,
      "learned_at": "2024-01-20 15:30:00"
    },
    "acid_blast": {
      "current_progress": 45,
      "is_learned": false,
      "learned_at": null
    }
  },
  "equipped_abilities": {
    "enemy_slots": [
      {"slot_index": 0, "ability_id": "rat_bite"},
      {"slot_index": 1, "ability_id": ""},
      {"slot_index": 2, "ability_id": ""}
    ]
  },
  "unlocked_slots": 1,
  "last_updated": "2024-01-20 15:30:00"
}
```

---

## ⚠️ Важные моменты

### **1. Совместимость с пассивными способностями:**
- Все пассивные бонусы применяются к способностям врагов
- Критический урон, двойной удар, усиление урона - всё работает

### **2. Анимации:**
- Использовать существующие анимации врагов
- Для врагов без анимации использовать fallback (attack_alt)
- Призрак должен появляться в позиции ГГ, но использовать анимацию врага

### **3. Баланс:**
- Способности врагов слабее оригиналов (70-80%)
- Добавлена стоимость маны для баланса
- Кулдауны не дают спамить одну способность

### **4. Производительность:**
- Призрак создается/удаляется каждый раз (не кэшируется)
- Использовать `queue_free()` для очистки
- Fade анимации через Tween для плавности

---

## 🚀 Итоговый чеклист реализации

### **Неделя 1: Базовая функциональность**
- [x] Этап 1: Базовая система изучения
- [x] Этап 2: UI системы изучения
- [x] Этап 3: Система слотов

### **Неделя 2: Визуальная система**
- [x] Этап 4: Визуальная система призраков
- [x] Этап 5: Система способностей ГГ
- [x] Этап 6: Модификаторы и баланс

### **Неделя 3: Финализация**
- [x] Этап 7: Мета-прогрессия
- [x] Этап 8: Тестирование и полировка

---

## 📚 Документация для пользователя

После реализации создать:
- `ACTIVE_ABILITY_LEARNING_GUIDE.md` - руководство для игрока
- `ACTIVE_ABILITY_DEVELOPER_GUIDE.md` - техническая документация
- Обновить `ABILITY_LEARNING_SYSTEM.md` с информацией об активных способностях

---

**Система спроектирована с учетом:**
✅ Совместимости с пассивными способностями
✅ Расширяемости через мета-прогрессию
✅ Визуальной привлекательности (призраки врагов)
✅ Баланса геймплея
✅ Простоты использования для игрока

**Готово к реализации!** 🎮✨
