# Реализация квестовой системы

## ✅ Статус: Реализовано

Базовая квестовая система для мета-прогрессии с артефактами и разблокировкой способностей.

---

## 🎯 Обзор системы

Квестовая система управляет прогрессией игрока через поиск артефактов, которые разблокир овывают новые возможности прокачки персонажа.

### Основные компоненты:

1. **PlayerData** - хранение состояния квестов и артефактов
2. **SoulSculptor NPC** - выдача квестов и прием артефактов
3. **BattleManager** - выдача артефактов после победы над боссами
4. **SoulSculptorDialog** - UI для взаимодействия с квестами

---

## 📊 Переменные в PlayerData.gd

### Мета-прогрессия:

```gdscript
# Встречи и артефакты
var met_soul_sculptor: bool = false  # Встретил ли игрок Скульптора душ
var has_soul_urn: bool = false  # Есть ли Урна душ (получена с босса)
var soul_urn_delivered: bool = false  # Доставлена ли Урна душ Скульптору
var has_knowledge_crystal: bool = false  # Есть ли Кристалл познания
var has_eternity_sphere: bool = false  # Есть ли Сфера вечности

# Ограничения изучения способностей
var max_passive_level: int = 0  # 0-3 (0=ничего, 1=до Rare, 2=до Legendary, 3=все)
var max_soul_development_level: int = 0  # 0-6 (количество доступных уровней развития души)

# Квесты
var active_quests: Array[String] = []  # Список активных квестов
var completed_quests: Array[String] = []  # Список завершенных квестов
```

---

## 🔑 Функции квестовой системы

### Проверка доступности:

```gdscript
func can_learn_passive_abilities() -> bool:
    """Может ли игрок изучать пассивные способности врагов"""
    return has_soul_urn and soul_urn_delivered

func can_learn_soul_development() -> bool:
    """Может ли игрок изучать способности развития души"""
    return has_soul_urn and soul_urn_delivered

func can_learn_passive_rarity(rarity: String) -> bool:
    """Проверка доступности изучения способности по редкости"""
    # Common/Uncommon/Rare: max_passive_level >= 1
    # Epic/Legendary: max_passive_level >= 2
    # Mythic: max_passive_level >= 3
```

### Управление квестами:

```gdscript
func add_quest(quest_id: String)
func complete_quest(quest_id: String)
func is_quest_active(quest_id: String) -> bool
func is_quest_completed(quest_id: String) -> bool
```

### Артефакты:

```gdscript
func give_soul_urn()
    # Выдает артефакт игроку

func deliver_soul_urn() -> bool
    # Доставляет урну Скульптору
    # Разблокирует:
    # - max_passive_level = 1 (до Rare)
    # - max_soul_development_level = 2 (уровни 1-2)
    # - Завершает квест "find_soul_urn"

func give_knowledge_crystal()
    # max_passive_level = 2 (до Legendary)
    # max_soul_development_level = 4 (уровни 3-4)

func give_eternity_sphere()
    # max_passive_level = 3 (Mythic)
    # max_soul_development_level = 6 (уровни 5-6)
```

---

## 🗿 Интеграция с NPC Скульптор душ

### Первая встреча:

```gdscript
func _check_first_meeting():
    if not player_data.met_soul_sculptor:
        player_data.met_soul_sculptor = true
        player_data.add_quest("find_soul_urn")
        # Квест активируется автоматически при первом клике на NPC
```

### Диалоговое меню:

**Опция 1: "🌟 Развитие души"**
- Если урна не доставлена → текст квеста
- Если доставлена → меню развития души (TODO)

**Опция 2: "⚡ Активация способностей"**
- Всегда доступна
- Открывает окно управления пассивными способностями

**Опция 3: "📚 Изучение способностей"**
- Если урна не доставлена → сообщение о необходимости урны
- Если доставлена → экран изучения способностей

**Опция 4: "🏺 Доставить Урну душ" (условная)**
- Появляется только если `has_soul_urn == true` и `soul_urn_delivered == false`
- При выборе:
  - Вызывает `player_data.deliver_soul_urn()`
  - Показывает текст разблокировки
  - Разблокирует изучение способностей

---

## ⚔️ Выдача артефактов в BattleManager

### Функция проверки:

```gdscript
func _check_and_give_artifact():
    # Вызывается в _on_enemy_died() после начисления наград
    
    # Урна душ - Редкий Слизень (rare rarity)
    if (enemy_name == "Слизень" or enemy_name == "Slime") and enemy_rarity == "rare":
        if not player_data.has_soul_urn:
            player_data.give_soul_urn()
            _show_message("🏺 Вы нашли Урну душ среди останков слизня!")
    
    # Кристалл познания - Древний страж
    elif enemy_name == "Древний страж":
        if not player_data.has_knowledge_crystal and player_data.soul_urn_delivered:
            player_data.give_knowledge_crystal()
            _show_message("💎 Вы нашли Кристалл познания!")
    
    # Сфера вечности - Владыка проклятых
    elif enemy_name == "Владыка проклятых":
        if not player_data.has_eternity_sphere and player_data.has_knowledge_crystal:
            player_data.give_eternity_sphere()
            _show_message("🌟 Сфера вечности в ваших руках!")
```

### Условия выдачи:

| Артефакт | Босс | Предварительное условие |
|----------|------|-------------------------|
| Урна душ | Редкий Слизень (rare) | Нет |
| Кристалл познания | Древний страж | Урна доставлена |
| Сфера вечности | Владыка проклятых | Кристалл получен |

---

## 💾 Сохранение и загрузка

### Сохранение в PlayerManager.gd:

```gdscript
"player_data": {
    // ... существующие поля ...
    
    # Мета-прогрессия
    "met_soul_sculptor": player_data.met_soul_sculptor,
    "has_soul_urn": player_data.has_soul_urn,
    "soul_urn_delivered": player_data.soul_urn_delivered,
    "has_knowledge_crystal": player_data.has_knowledge_crystal,
    "has_eternity_sphere": player_data.has_eternity_sphere,
    "max_passive_level": player_data.max_passive_level,
    "max_soul_development_level": player_data.max_soul_development_level,
    "active_quests": player_data.active_quests,
    "completed_quests": player_data.completed_quests
}
```

### Загрузка:

```gdscript
player_data.met_soul_sculptor = player_save.get("met_soul_sculptor", false)
player_data.has_soul_urn = player_save.get("has_soul_urn", false)
player_data.soul_urn_delivered = player_save.get("soul_urn_delivered", false)
player_data.has_knowledge_crystal = player_save.get("has_knowledge_crystal", false)
player_data.has_eternity_sphere = player_save.get("has_eternity_sphere", false)
player_data.max_passive_level = player_save.get("max_passive_level", 0)
player_data.max_soul_development_level = player_save.get("max_soul_development_level", 0)

# Загрузка массивов квестов с проверкой типов
```

---

## 🎮 Поток квеста "Урна душ"

### 1. Первая встреча со Скульптором:

```
Игрок кликает на Скульптора
└─> _check_first_meeting()
    ├─> met_soul_sculptor = true
    └─> add_quest("find_soul_urn")
```

### 2. Получение задания:

```
Игрок выбирает "🌟 Развитие души" (или "📚 Изучение способностей")
└─> soul_urn_delivered == false
    └─> Показывается текст квеста:
        "Мне нужна Урна душ из подземелья под городом..."
```

### 3. Победа над боссом:

```
Побежден "Слизень" с редкостью "rare"
└─> _on_enemy_died()
    └─> _check_and_give_artifact()
        ├─> Проверка: enemy_name == "Слизень" && enemy_rarity == "rare"
        ├─> has_soul_urn = true
        └─> Сообщение: "🏺 Вы нашли Урну душ!"
```

### 4. Возвращение к Скульптору:

```
Игрок кликает на Скульптора
└─> show_menu()
    ├─> has_soul_urn == true
    ├─> soul_urn_delivered == false
    └─> Показывается опция "🏺 Доставить Урну душ"

Игрок выбирает "🏺 Доставить Урну душ"
└─> deliver_soul_urn()
    ├─> soul_urn_delivered = true
    ├─> max_passive_level = 1
    ├─> max_soul_development_level = 2
    ├─> complete_quest("find_soul_urn")
    └─> Сообщение: "Превосходно! Урна душ..."
```

### 5. После доставки:

```
Опция "🏺 Доставить Урну душ" исчезает из меню
Опция "📚 Изучение способностей" теперь открывает экран изучения
can_learn_passive_abilities() == true
```

---

## 📜 Список квестов

### Реализованные:

| ID квеста | Название | Артефакт | Босс |
|-----------|----------|----------|------|
| `find_soul_urn` | Урна душ | Урна душ | Редкий Слизень (rare) |

### Планируются:

| ID квеста | Название | Артефакт | Босс |
|-----------|----------|----------|------|
| `find_knowledge_crystal` | Кристалл познания | Кристалл познания | Древний страж |
| `find_eternity_sphere` | Сфера вечности | Сфера вечности | Владыка проклятых |

---

## 🎨 Тексты диалогов

### Квест "Урна душ" (до получения):

```
Приветствую тебя, путник.

Я чувствую в тебе силу душ... Я - Скульптор душ, мастер древнего искусства работы с духовной энергией.

Я могу помочь тебе научиться новым способностям, перенимая их у поверженных врагов. Но для этого мне нужен особый инструмент - Урна душ.

Много лет назад неудачливый вор украл её и сгинул в подземельях под городом. Я чувствую, что артефакт все еще там.

Верни его мне и я смогу помочь тебе.
```

### После доставки Урны душ:

```
Превосходно! Урна душ... Я так долго её искал.

Теперь я могу помочь тебе. Это древний артефакт, способный хранить знания о способностях врагов.

С этого момента, побеждая врагов, ты будешь получать частицы их душ, которые я преобразую в знания об их способностях.

Также я могу научить тебя базовым техникам развития души.

Приходи ко мне, когда накопишь достаточно осколков душ.
```

---

## 🐛 Отладка

### Логи квестовой системы:

**Первая встреча:**
```
🗿 Первая встреча со Скульптором душ! Квест 'find_soul_urn' активирован
📜 Квест добавлен: find_soul_urn
```

**Получение артефакта:**
```
🏺 Получена Урна душ!
🏺 Вы нашли Урну душ среди останков слизня!
Отнесите её Скульптору душ.
```

**Доставка артефакта:**
```
🏺 Нажато: Доставить Урну душ
🎉 Урна душ доставлена! Разблокировано изучение способностей!
✅ Квест завершен: find_soul_urn
```

### Проверка состояния:

```gdscript
# В консоли Godot:
var pm = get_node("/root/PlayerManager")
var pd = pm.get_player_data()

# Проверка состояния квестов
print("Встретил Скульптора: ", pd.met_soul_sculptor)
print("Есть Урна: ", pd.has_soul_urn)
print("Урна доставлена: ", pd.soul_urn_delivered)
print("Активные квесты: ", pd.active_quests)
print("Завершенные квесты: ", pd.completed_quests)

# Проверка разблокировки
print("Может изучать способности: ", pd.can_learn_passive_abilities())
print("Макс уровень пассивок: ", pd.max_passive_level)
```

---

## 🔜 Будущие улучшения

1. **UI журнала квестов:**
   - Отдельный экран со списком квестов
   - Отображение прогресса
   - Награды за выполнение

2. **Расширенные квесты:**
   - Квесты на Кристалл познания и Сферу вечности
   - Побочные квесты от Скульптора
   - Цепочки квестов

3. **Визуальные улучшения:**
   - Иконки артефактов
   - Анимации получения артефактов
   - Специальные эффекты при доставке

4. **Система уведомлений:**
   - Индикатор новых квестов
   - Напоминания о невыполненных квестах
   - Счетчик артефактов: 🏺 1/3

---

**Дата создания:** 20 ноября 2025  
**Последнее обновление:** 20 ноября 2025  
**Версия:** 1.1  
**Статус:** ✅ Базовая реализация готова  

**Важно:** Урна душ выдается при победе над **редким Слизнем** (enemy_name == "Слизень" && enemy_rarity == "rare"), а не над Гнилостным слизнем!

**Следующий шаг:** 
1. Создать комнату "Логово вора" с редким Слизнем в локации "Подземелье под городом"
2. Тестирование полного цикла квеста

