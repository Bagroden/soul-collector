# Интеграция звука молнии для пассивки "Шаман бурь"

## Дата
28 октября 2025

## Добавленный функционал

Добавлен звук удара молнии, который проигрывается при срабатывании пассивки "Шаман бурь".

## Используемый звуковой файл

**Путь:** `Audio/Sounds/Thunder_bolt.wav`

**Характеристики:**
- Формат: WAV
- Проигрывается через AudioStreamPlayer2D
- Поддерживает 2D пространственный звук (звук слышен в зависимости от позиции)

## Реализация

### 1. Добавлены экспортируемые параметры

**Файл:** `Scripts/Battle/LightningEffectAnimated.gd`

```gdscript
@export var fade_duration: float = 0.2  # Длительность исчезновения
@export var scale_start: float = 0.5    # Начальный размер
@export var scale_end: float = 1.0      # Конечный размер во время анимации
@export var sound_volume_db: float = 0.0  # Громкость звука молнии (-80 до 24)
@export var sound_pitch: float = 1.0     # Высота звука (0.5 - низкий, 2.0 - высокий)
```

**Параметры звука:**
- **sound_volume_db** - громкость звука в децибелах
  - `-80` до `24` dB
  - `0` dB - стандартная громкость
  - `-10` dB - тише на 25%
  - `+6` dB - громче в 2 раза
  
- **sound_pitch** - высота звука
  - `0.5` - на октаву ниже (низкий рокот)
  - `1.0` - стандартная высота
  - `1.5` - выше на полтона (резкий треск)
  - `2.0` - на октаву выше

### 2. Создание аудио-плеера

**Функция:** `_setup_lightning_sound()`

```gdscript
func _setup_lightning_sound():
    """Создает и настраивает AudioStreamPlayer2D для звука молнии"""
    var audio_player = AudioStreamPlayer2D.new()
    audio_player.name = "LightningSoundPlayer"
    
    # Загружаем звук молнии
    var thunder_sound = load("res://Audio/Sounds/Thunder_bolt.wav")
    if thunder_sound:
        audio_player.stream = thunder_sound
        audio_player.volume_db = sound_volume_db
        audio_player.pitch_scale = sound_pitch
        audio_player.bus = "Master"
        audio_player.max_distance = 2000.0  # Максимальное расстояние слышимости
        audio_player.attenuation = 1.0      # Затухание звука с расстоянием
        print("🔊 Звук молнии загружен (громкость: ", sound_volume_db, " dB, высота: ", sound_pitch, ")")
    else:
        print("⚠️ Не удалось загрузить звук молнии")
    
    add_child(audio_player)
```

**Особенности:**
- Создается программно (не в редакторе)
- Использует `AudioStreamPlayer2D` для пространственного звука
- Звук слышен на расстоянии до 2000 пикселей от источника
- Автоматически удаляется вместе с эффектом

### 3. Проигрывание звука

**Функция:** `_play_animation()`

```gdscript
func _play_animation():
    """Проигрывает анимацию молнии"""
    print("⚡ _play_animation() начинается")
    
    # Проигрываем звук молнии
    var audio_player = get_node_or_null("LightningSoundPlayer")
    if audio_player:
        audio_player.play()
        print("🔊 Звук молнии проигрывается")
    
    # ... остальная анимация ...
```

**Тайминг:**
- Звук начинается **сразу** в начале анимации
- Совпадает с началом появления эффекта
- Проигрывается параллельно с визуальной анимацией

### 4. Обновлена сцена

**Файл:** `Scenes/Effects/LightningEffect.tscn`

Добавлены параметры по умолчанию:
```
sound_volume_db = 0.0
sound_pitch = 1.0
```

Эти значения можно изменить в редакторе Godot.

## Настройка в редакторе Godot

### Изменение громкости:

1. Откройте сцену `Scenes/Effects/LightningEffect.tscn`
2. Выберите узел `LightningEffect`
3. В инспекторе найдите параметр **Sound Volume Db**
4. Измените значение:
   - `0.0` - стандартная громкость
   - `-5.0` - немного тише
   - `-10.0` - значительно тише
   - `5.0` - громче
   - `-80.0` - отключить звук

### Изменение высоты звука:

1. В инспекторе найдите параметр **Sound Pitch**
2. Измените значение:
   - `0.8` - более низкий, глубокий удар
   - `1.0` - стандартная высота
   - `1.2` - более высокий, резкий удар
   - `1.5` - очень высокий треск

### Создание вариаций эффекта:

**Мощная молния (для высоких уровней):**
```
sound_volume_db = 3.0     # Громче
sound_pitch = 0.8         # Ниже (более мощный звук)
scale_start = 0.8
scale_end = 1.5
```

**Слабая молния (для низких уровней):**
```
sound_volume_db = -5.0    # Тише
sound_pitch = 1.2         # Выше (более слабый треск)
scale_start = 0.3
scale_end = 0.7
```

**Быстрая молния:**
```
sound_volume_db = -3.0
sound_pitch = 1.5         # Быстрый резкий удар
fade_duration = 0.1       # Быстрое исчезновение
```

## Как работает AudioStreamPlayer2D

### Пространственный звук:

`AudioStreamPlayer2D` создает 2D пространственный звук:
- Звук громче, если источник ближе к камере
- Звук тише, если источник дальше от камеры
- Звук слышен только в пределах `max_distance` (2000 пикселей)

### Параметры затухания:

```gdscript
max_distance = 2000.0  # Максимальное расстояние слышимости
attenuation = 1.0      # Коэффициент затухания (1.0 = линейное)
```

**Варианты attenuation:**
- `1.0` - линейное затухание (по умолчанию)
- `2.0` - квадратичное затухание (быстрее становится тише)
- `0.5` - медленное затухание (звук слышен дальше)

## Логи отладки

При создании эффекта молнии в консоли будут сообщения:

```
⚡ LightningEffect _ready() вызван
🔊 Звук молнии загружен (громкость: 0.0 dB, высота: 1.0)
⚡ _play_animation() начинается
🔊 Звук молнии проигрывается
```

Если звук не загружается:
```
⚠️ Не удалось загрузить звук молнии
```

## Возможные проблемы и решения

### Проблема 1: Звук не проигрывается

**Причины:**
1. Файл `Thunder_bolt.wav` отсутствует или поврежден
2. Неправильный путь к файлу
3. Громкость установлена в -80 dB (выключена)

**Решение:**
```gdscript
# Проверьте путь к файлу
var thunder_sound = load("res://Audio/Sounds/Thunder_bolt.wav")
if not thunder_sound:
    print("ОШИБКА: Файл не найден!")

# Проверьте громкость
print("Громкость: ", sound_volume_db)  # Должно быть > -80
```

### Проблема 2: Звук слишком тихий

**Решение:**
1. Увеличьте `sound_volume_db` до 5-10 dB
2. Проверьте общую громкость в настройках Godot (Audio Bus)
3. Проверьте расстояние от камеры до эффекта

### Проблема 3: Звук запаздывает

**Причина:** Звук проигрывается после задержки

**Решение:**
```gdscript
# Проигрывайте звук ДО анимации появления
func _play_animation():
    # 1. Сначала звук
    var audio_player = get_node_or_null("LightningSoundPlayer")
    if audio_player:
        audio_player.play()
    
    # 2. Потом анимация
    var appear_tween = create_tween()
    # ...
```

### Проблема 4: Звук слишком громкий

**Решение:**
1. Уменьшите `sound_volume_db` до -5 или -10 dB
2. Измените в редакторе параметр Sound Volume Db
3. Проверьте настройки Audio Bus в Godot

## Настройка Audio Bus (опционально)

Для более тонкой настройки можно создать отдельный Audio Bus:

1. Откройте **Audio** панель в Godot
2. Создайте новый Bus: "SFX" или "Effects"
3. В скрипте измените:
   ```gdscript
   audio_player.bus = "SFX"  # Вместо "Master"
   ```
4. Теперь можно регулировать громкость всех эффектов отдельно

## Дополнительные возможности

### Случайная вариация высоты звука:

```gdscript
func _setup_lightning_sound():
    # ... создание audio_player ...
    
    # Добавляем случайную вариацию высоты (±10%)
    var pitch_variation = randf_range(0.9, 1.1)
    audio_player.pitch_scale = sound_pitch * pitch_variation
```

**Результат:** Каждая молния звучит немного по-разному

### Разная громкость для разных уровней:

```gdscript
# В StormShaman.gd при создании эффекта:
func _spawn_lightning_effect(target: Node):
    var lightning_scene = load("res://Scenes/Effects/LightningEffect.tscn")
    var lightning_effect = lightning_scene.instantiate()
    
    # Настраиваем громкость в зависимости от уровня способности
    var ability_level = _owner.ability_levels.get(id, 1)
    match ability_level:
        1: lightning_effect.sound_volume_db = -3.0  # Тише для уровня 1
        2: lightning_effect.sound_volume_db = 0.0   # Нормально для уровня 2
        3: lightning_effect.sound_volume_db = 3.0   # Громче для уровня 3
    
    # ... остальной код ...
```

### Эхо после удара:

Для создания эффекта эха можно добавить Audio Effect:

1. В Audio Bus добавьте эффект "Reverb"
2. Настройте параметры эха
3. Звук молнии будет звучать с реверберацией

## Изменённые файлы

1. **Scripts/Battle/LightningEffectAnimated.gd**
   - Добавлены экспортируемые параметры звука
   - Добавлена функция `_setup_lightning_sound()`
   - Добавлено проигрывание звука в `_play_animation()`

2. **Scenes/Effects/LightningEffect.tscn**
   - Добавлены параметры звука по умолчанию

## Контрольный список

- ✅ Звуковой файл `Thunder_bolt.wav` доступен
- ✅ AudioStreamPlayer2D создается программно
- ✅ Звук проигрывается при появлении эффекта
- ✅ Громкость настраивается через экспорт-параметр
- ✅ Высота звука настраивается через экспорт-параметр
- ✅ Звук удаляется вместе с эффектом
- ✅ Логи показывают успешную загрузку звука

## Тестирование

### Шаг 1: Запустите игру
1. Изучите пассивку "Шаман бурь"
2. Используйте магические атаки

### Шаг 2: Проверьте звук
1. При срабатывании молнии должен быть слышен звук удара
2. Проверьте логи: `🔊 Звук молнии загружен`
3. Проверьте логи: `🔊 Звук молнии проигрывается`

### Шаг 3: Настройте параметры
1. Откройте `LightningEffect.tscn` в редакторе
2. Измените `sound_volume_db` и `sound_pitch`
3. Протестируйте в игре

## Результат

✅ Добавлен звук удара молнии
✅ Звук синхронизирован с визуальным эффектом
✅ Громкость и высота настраиваются в редакторе
✅ Используется пространственный 2D звук
✅ Звук автоматически удаляется с эффектом

