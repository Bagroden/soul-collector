# Звуки и анимация для Гоблина Воина

## Описание изменений

Добавлена звуковая озвучка и специальная анимация для Гоблина Воина, включая его способность "Двойной удар" и базовую атаку.

## Добавленные звуки и анимация

### 1. Способность "Двойной удар"

**Описание:** Гоблин Воин наносит два последовательных удара мечом. Каждый удар озвучен одним и тем же звуком для единообразия.

**Анимация:** `Double_strike` (специальная анимация для способности)

**Звуки:**
- **Первый удар:** `sword_attack1.wav` (громкость -5 dB)
- **Второй удар:** `sword_attack1.wav` (громкость -5 dB)
- **Задержка между ударами:** 0.3 секунды

**Расположение в коде:**
```gdscript
# Запускаем специальную анимацию "Двойного удара"
var visual_node = enemy_node.get_node_or_null("Visual")
if visual_node:
    # Проверяем наличие анимации
    if visual_node.sprite_frames and visual_node.sprite_frames.has_animation("Double_strike"):
        if visual_node.has_method("play_animation"):
            visual_node.play_animation("Double_strike")
        else:
            visual_node.play("Double_strike")
            # Вернемся к idle после завершения
            await visual_node.animation_finished
            visual_node.play_idle()
        custom_animation_played = true  # Отмечаем, что специальная анимация проиграна
    else:
        # Fallback: используем стандартную атаку, если специальная анимация не найдена
        visual_node.play_attack()
        custom_animation_played = true

# Первый удар
if _calculate_hit_chance(enemy_node, player_node, "double_strike_1", "Двойной удар (1-й удар)"):
    # Звук первого удара
    if SoundManager:
        SoundManager.play_sound("sword_attack1", -5.0)
    
    player_node.take_damage(first_hit_damage, damage_type)
    # ...

await get_tree().create_timer(0.3).timeout  # Задержка между ударами

# Второй удар
if _calculate_hit_chance(enemy_node, player_node, "double_strike_2", "Двойной удар (2-й удар)"):
    # Звук второго удара
    if SoundManager:
        SoundManager.play_sound("sword_attack1", -5.0)
    
    player_node.take_damage(second_hit_damage, damage_type)
    # ...
```

### 2. Базовая атака

**Описание:** Обычная атака Гоблина Воина мечом (когда способность "Двойной удар" на кулдауне или недостаточно ресурсов).

**Звук:** `sword_attack1.wav` (громкость -5 dB)

**Расположение в коде:**
```gdscript
elif damage_type == "physical" and enemy_node.display_name == "Гоблин Воин":
    # Звук базовой атаки Гоблина Воина
    var audio_player = AudioStreamPlayer.new()
    audio_player.stream = load("res://Audio/Sounds/sword_attack1.wav")
    audio_player.volume_db = -5.0
    audio_player.bus = "Master"
    add_child(audio_player)
    audio_player.play()
    # Удаляем после проигрывания
    audio_player.finished.connect(func(): audio_player.queue_free())
```

## Технические детали

### Файлы изменены

- **Scripts/Battle/battle_manager.gd**
  - Строки ~671-675: Добавлен звук для первого удара "Двойного удара"
  - Строки ~693-697: Добавлен звук для второго удара "Двойного удара"
  - Строки ~1157-1166: Добавлен звук для базовой атаки Гоблина Воина

### Используемые звуковые файлы

Все звуки находятся в папке `Audio/Sounds/`:
- `sword_attack1.wav` - удар мечом (используется для базовой атаки и обоих ударов "Двойного удара")

### Анимация

**Специальная анимация для "Двойного удара":**
- Имя: `Double_strike`
- Должна быть добавлена в `SpriteFrames` Гоблина Воина в редакторе Godot
- Если анимация не найдена, используется стандартная `attack` (fallback)

### Параметры воспроизведения

| Параметр | Значение |
|----------|----------|
| Громкость | -5.0 dB |
| Шина | Master |
| Автоудаление | Да (после проигрывания) |

## Логика работы

### Двойной удар

1. **Проверка попадания первого удара:**
   - Если попал → Воспроизводится `sword_attack3.wav` + наносится урон
   - Если промах → Показывается "Промах", звук не воспроизводится

2. **Задержка 0.3 секунды**

3. **Проверка попадания второго удара:**
   - Если попал → Воспроизводится `sword_attack4.wav` + наносится урон
   - Если промах → Показывается "Промах", звук не воспроизводится

### Базовая атака

1. **Проверка условий:**
   - `damage_type` == `"physical"`
   - `enemy_node.display_name` == `"GoblinWarrior"`

2. **Воспроизведение:**
   - Создается временный `AudioStreamPlayer`
   - Загружается звук `sword_attack1.wav`
   - Звук воспроизводится
   - После завершения плеер удаляется

## Примеры использования

### Пример 1: Двойной удар с двумя попаданиями

```
Гоблин Воин использует "Двойной удар"
▶️ Анимация "Double_strike" начинается
🔊 sword_attack1.wav → Ты получил 25 урона
⏱️ 0.3 секунды
🔊 sword_attack1.wav → Ты получил 30 урона
Итого: 55 урона
```

### Пример 2: Двойной удар с одним попаданием

```
Гоблин Воин использует "Двойной удар"
▶️ Анимация "Double_strike" начинается
🔊 sword_attack1.wav → Ты получил 25 урона
⏱️ 0.3 секунды
❌ Промах → Второй удар не попал, звук не воспроизводится
Итого: 25 урона
```

### Пример 3: Базовая атака

```
Способность на перезарядке!
🔊 sword_attack1.wav → Ты получил 20 урона
```

## Сравнение с другими врагами

| Враг | Базовая атака | Способность | Звуки способности | Анимация способности |
|------|---------------|-------------|-------------------|----------------------|
| Гоблин Вор | `sword_attack1.wav` | Poison Strike | `poison_strike1.wav` | `attack` |
| Гоблин Воин | `sword_attack1.wav` | Double Strike | `sword_attack1.wav` × 2 | `Double_strike` |
| Демон Палач | `sword_attack3.wav` | Executioner Strike | `sword_attack3.wav` + `sword_attack4.wav` | `Executioner_strike` |
| Крыса | `rat_attack1.wav` | Rat Bite | `rat_attack1.wav` | `attack` |

## Баланс звуков

### Выбор звуков

- **`sword_attack1.wav`** для всех атак:
  - Легкий звук удара
  - Используется для базовой атаки и обоих ударов "Двойного удара"
  - Совпадает с Гоблином Вором для единообразия гоблинских атак
  - Единый звук для всех действий Гоблина Воина создает узнаваемый стиль врага

### Громкость

- `-5.0 dB` для всех ударов мечом
- Балансируется с другими звуками в игре
- Не перебивает музыку и другие эффекты

## Настройка анимации в Godot

### Добавление анимации "Double_strike"

1. Откройте сцену Гоблина Воина (`Scenes/Battle/Enemy_GoblinWarrior.tscn`)
2. Выберите узел `Visual` (тип `AnimatedSprite2D`)
3. В инспекторе найдите свойство `SpriteFrames`
4. Откройте редактор `SpriteFrames`
5. Добавьте новую анимацию с именем **"Double_strike"** (точно с таким названием!)
6. Добавьте кадры анимации для двух ударов
7. Настройте FPS (рекомендуется ~12-15 FPS)
8. Отключите зацикливание (loop)
9. Сохраните изменения

**Важно:** Имя анимации должно быть точно `"Double_strike"` (с заглавной D)!

## Тестирование

### Чек-лист для проверки:

- [ ] Базовая атака Гоблина Воина воспроизводит `sword_attack1.wav`
- [ ] Первый удар "Двойного удара" воспроизводит `sword_attack1.wav`
- [ ] Второй удар "Двойного удара" воспроизводит `sword_attack1.wav`
- [ ] При использовании "Двойного удара" проигрывается анимация `Double_strike` (если добавлена)
- [ ] Если анимация `Double_strike` не найдена, проигрывается `attack` (fallback)
- [ ] Задержка между ударами составляет 0.3 секунды
- [ ] Звуки не воспроизводятся при промахе
- [ ] Звуки не накладываются друг на друга
- [ ] Громкость звуков сбалансирована
- [ ] После завершения анимации `Double_strike` враг возвращается к `idle`

### Как протестировать:

1. Запустите игру
2. Выберите локацию "Тёмный лес" или "Тестовую арену"
3. Начните бой с Гоблином Воином
4. **Тест базовой атаки:**
   - Дождитесь, пока "Двойной удар" будет на кулдауне
   - Убедитесь, что базовая атака воспроизводит `sword_attack1.wav`
5. **Тест "Двойного удара":**
   - Дождитесь использования способности
   - Убедитесь, что оба удара озвучены
   - Проверьте задержку между ударами

## Известные особенности

### Звуки при промахе

Если враг промахивается, звук **не воспроизводится**. Это сделано для:
- Ясной обратной связи игроку (промах = тишина)
- Избежания путаницы (звук удара, но урона нет)
- Логичности (если меч не попал, звука удара не будет)

### Плееры автоудаляются

Все `AudioStreamPlayer` автоматически удаляются после проигрывания:
```gdscript
audio_player.finished.connect(func(): audio_player.queue_free())
```

Это предотвращает утечки памяти и накопление неиспользуемых узлов.

## Возможные улучшения

### Будущие доработки:

1. **Вариативность звуков:**
   - Несколько вариантов звука для базовой атаки
   - Случайный выбор для разнообразия

2. **Звук замаха:**
   - Добавить звук замаха перед ударом
   - Предупреждать игрока о приближающейся атаке

3. **Звуковые эффекты блока:**
   - Разные звуки при попадании по барьеру
   - Звук блокировки щитом/уворота

4. **Голосовые эффекты:**
   - Боевой клич Гоблина Воина перед "Двойным ударом"
   - Возгласы при атаке

---

## Итоговые изменения

### Код изменен в файле:
- `Scripts/Battle/battle_manager.gd`
  - Добавлена анимация `Double_strike` для способности (~670-686 строки)
  - Изменены звуки ударов с `sword_attack3/4.wav` на `sword_attack1.wav` (~692, 696)
  - Добавлен звук для базовой атаки Гоблина Воина (~1175-1184)
  - Установлен флаг `custom_animation_played` для предотвращения перекрытия анимации
  - **Важно:** Используется `display_name == "Гоблин Воин"` (с пробелом и кириллицей!)

### Ключевые особенности:
- **Единый звук** `sword_attack1.wav` для всех атак
- **Специальная анимация** `Double_strike` для способности
- **Fallback система**: если анимация не найдена → используется `attack`
- **Автоматический возврат** к `idle` после завершения анимации

---

**Дата добавления:** 31 октября 2025  
**Версия:** 2.1  
**Статус:** Реализовано ✅  
**Линтер:** Ошибок нет ✅

**Исправления:**
- v2.1: Исправлено `display_name` с "GoblinWarrior" на "Гоблин Воин" для базовой атаки

