# Система визуальных эффектов для Магических Стрел

## 🎯 Задача:
Добавить визуальную анимацию магических стрел, летящих от атакующего к цели при использовании способности "Магические стрелы".

## ✅ Реализованные компоненты:

### **1. MagicArrowProjectile.gd** 
Скрипт визуального снаряда магической стрелы

**Местоположение:** `Scripts/Battle/MagicArrowProjectile.gd`

**Функции:**
- `setup(from: Vector2, to: Vector2, arrow_index: int)` - настраивает параметры полёта стрелы
- `_process(delta: float)` - обновляет позицию стрелы каждый кадр
- `_create_impact_effect()` - создаёт визуальный эффект попадания

**Визуальные эффекты:**
- ✨ Плавный полёт от источника к цели
- 🏹 Параболическая траектория (как настоящие стрелы)
- 🎯 Поворот по направлению движения (касательная к дуге)
- 💫 Пульсация размера (эффект мерцания)
- 🌟 Голубоватое свечение (модуляция цвета)
- 💥 Эффект взрыва при попадании
- ⏱️ Задержка между стрелами для "веера"

**Параметры:**
- `speed: 600.0` - скорость полёта пикселей/сек
- `arc_height: 50.0` - высота параболы (пикселей)
- `scale_pulse: 0.1` - амплитуда пульсации

---

### **2. MagicArrowProjectile.tscn**
Сцена визуального снаряда

**Местоположение:** `Scenes/Battle/MagicArrowProjectile.tscn`

**Компоненты:**
- `Sprite2D` - спрайт стрелы
- Текстура: `Assets/Sprites/Goblins/StoneWizardGoblin/Magic_arrow1.png`
- Скрипт: `MagicArrowProjectile.gd`

---

### **3. Интеграция в battle_manager.gd**

**Функция `_spawn_magic_arrow_projectiles()`:**
```gdscript
func _spawn_magic_arrow_projectiles(from_character: Node2D, to_character: Node2D, arrows_count: int)
```

**Что делает:**
- Создаёт указанное количество визуальных снарядов
- Настраивает позиции старта и цели
- Добавляет снаряды в игровой мир
- Синхронизирует с задержками звуков

**Интеграция для врагов** (строки 688-723):
- Обрабатывает флаг `magic_arrows` в результате способности врага
- Создаёт визуальные снаряды перед нанесением урона
- Синхронизирует урон с задержками (0.60 сек между стрелами)
- Каждая стрела проверяется на уворот отдельно

**Интеграция для игрока** (строки 1317-1353):
- Обрабатывает флаг `magic_arrows` в результате способности игрока
- Создаёт визуальные снаряды от игрока к врагу
- Показывает сообщение о количестве стрел и уроне
- Синхронизирует анимацию со звуками

---

## 🎮 Как это работает:

### **Для способности врага (Гоблин Колдун):**

1. Враг использует способность "Магические стрелы"
2. `EnemyAbility.gd` вычисляет количество стрел (1 + интеллект/15)
3. `battle_manager.gd` обнаруживает флаг `magic_arrows`
4. Вызывается `_spawn_magic_arrow_projectiles()` для создания визуальных снарядов
5. Каждая стрела:
   - Летит от врага к игроку с анимацией
   - Воспроизводится звук через 0.60 сек после предыдущей
   - Проверяется уворот
   - При попадании наносит урон и показывает число урона
   - Создаёт эффект взрыва при достижении цели

### **Для способности игрока:**

1. Игрок использует способность "Магические стрелы"
2. `MagicArrows.gd` вычисляет количество стрел
3. `battle_manager.gd` обнаруживает флаг `magic_arrows`
4. Создаются визуальные снаряды от игрока к врагу
5. Каждая стрела летит к врагу с задержкой 0.60 сек

---

## 📋 Технические детали:

### **Синхронизация:**
- Визуальные снаряды создаются ВСЕ сразу, но с задержками в `setup()`
- Урон наносится с задержками `await get_tree().create_timer(0.60).timeout`
- Звуки воспроизводятся в `EnemyAbility.gd` с таймерами
- Всё синхронизировано с одинаковой задержкой 0.60 сек

### **Позиционирование:**
- Снаряды используют `global_position` персонажей
- Если есть `Visual` узел, используется его позиция (более точно)
- Добавляется случайное смещение для визуального разнообразия

### **Производительность:**
- Снаряды автоматически удаляются после достижения цели
- Используется `queue_free()` для корректной очистки памяти
- Лёгкие вычисления в `_process()` (только интерполяция)

---

## 🎨 Визуальные настройки:

### **Цвет и эффекты:**
- Базовый цвет: `Color(0.5, 0.8, 1.0)` - голубоватый
- Пульсация альфа-канала: 0.8 ± 0.2
- Эффект взрыва: увеличение до 2x с исчезновением

### **Движение:**
- Использует `ease(progress, -2.0)` для плавного замедления
- Параболическая траектория: `sin(progress * PI) * arc_height`
- Поворот по касательной к траектории (вычисляется производная параболы)
- Пульсация масштаба: ±10%

---

## 🔧 Как расширить:

### **Добавить новые типы снарядов:**

1. Создайте новый спрайт в `Assets/Sprites/`
2. Создайте новую сцену на базе `MagicArrowProjectile.tscn`
3. Измените текстуру в сцене
4. В `battle_manager.gd` добавьте условие для новой способности:
```gdscript
if result.get("fire_arrows", false):
    _spawn_fire_arrow_projectiles(attacker, target, arrows_count)
```

### **Изменить визуальные эффекты:**

Отредактируйте параметры в `MagicArrowProjectile.gd`:
- `speed` - скорость полёта
- `arc_height` - высота параболы (чем больше, тем выше дуга)
- `scale_pulse` - амплитуда пульсации
- `modulate` - цвет свечения

---

## 📝 Заметки:

- ✅ Система работает как для игрока, так и для врагов
- ✅ Поддерживает любое количество стрел
- ✅ Синхронизирована со звуками
- ✅ Проверка уворота для каждой стрелы отдельно
- ✅ Пассивные способности активируются после каждой стрелы
- ✅ Нет ошибок линтера
- ⚠️ Требуется наличие файла `Magic_arrow1.png` в указанной директории

---

## 🎯 Тестирование:

Для тестирования:
1. Запустите бой с **Гоблином Колдуном** (имеет способность "Магические стрелы")
2. Или используйте способность "Магические стрелы" игроком (если изучена)
3. Наблюдайте за визуальными эффектами:
   - Стрелы должны лететь с задержкой
   - Звук должен совпадать с полётом
   - Урон должен отображаться при попадании каждой стрелы
   - Эффект взрыва при достижении цели

---

## 🐛 Возможные проблемы:

### **Стрелы не отображаются:**
- Проверьте, что файл `Magic_arrow1.png` существует
- Проверьте, что сцена `MagicArrowProjectile.tscn` корректно загружена
- Убедитесь, что `GameWorld` узел существует в сцене боя

### **Стрелы летят не в ту сторону:**
- Проверьте `look_at()` в функции `setup()`
- Убедитесь, что позиции `from_pos` и `to_pos` корректны

### **Задержки не синхронизированы:**
- Проверьте значение задержки в `EnemyAbility.gd` (строка 154)
- Убедитесь, что задержка в `battle_manager.gd` совпадает (0.60 сек)

---

## ✨ Итого:

Реализована полноценная система визуальных эффектов для способности "Магические стрелы" с:
- Красивой анимацией полёта
- Синхронизацией со звуками
- Поддержкой множественных снарядов
- Проверкой уворота для каждой стрелы
- Эффектами свечения и взрыва
- Работой для игрока и врагов

Система готова к использованию! 🚀

