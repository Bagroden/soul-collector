# Исправление проверки предыдущего уровня в UI

## 🔴 Проблема

На экране изучения способностей отображалась кнопка "Купить ур. 2.0" для способностей, у которых не был изучен уровень 1!

### Пример:
```
Крысиная живучесть (ур. 2.0) (Common)
Восстанавливает 8 HP за раунд
Прогресс: 200 / 500 очков
[Купить ур. 2.0 (1000 осколков)] ← Кнопка активна! ❌
```

Хотя уровень 1 не был изучен, UI показывал кнопку покупки уровня 2!

---

## 🔍 Причина

**Файл:** `Scripts/UI/AbilityLearningScreen.gd` (строки 375-386)

### Проблемный код:

```gdscript
else:
    # Для обычных способностей
    if can_learn.can_learn:
        var cost = can_learn.required_soul_shards
        learn_button.text = "Купить ур. " + str(next_level) + " (" + str(cost) + " осколков)"
        learn_button.disabled = false
    else:
        # Вычисляем прогресс для следующего уровня
        var req_progress = can_learn.required_progress
        var curr_progress = can_learn.current_progress
        learn_button.text = "Недоступно (" + str(curr_progress) + "/" + str(req_progress) + ")"
        learn_button.disabled = true
```

**Проблема:** Код проверял только `can_learn.can_learn`, но **не проверял причину** (`can_learn.reason`).

Функция `can_learn_ability` в `AbilityLearningSystem.gd` правильно возвращала:
```gdscript
{
    "can_learn": false,
    "reason": "Требуется изучить предыдущий уровень",
    ...
}
```

Но UI игнорировал поле `reason` и показывал сообщение о недостаточном прогрессе, которое было неправильным!

---

## ✅ Исправление

**Файл:** `Scripts/UI/AbilityLearningScreen.gd`

```gdscript
else:
    # Для обычных способностей
    if can_learn.can_learn:
        var cost = can_learn.required_soul_shards
        learn_button.text = "Купить ур. " + str(next_level) + " (" + str(cost) + " осколков)"
        learn_button.disabled = false
    else:
        # Проверяем причину недоступности
        if can_learn.reason == "Требуется изучить предыдущий уровень":
            # Предыдущий уровень не изучен
            learn_button.text = "Требуется изучить ур. " + str(next_level - 1)
            learn_button.disabled = true
        else:
            # Недостаточно прогресса
            var req_progress = can_learn.required_progress
            var curr_progress = can_learn.current_progress
            learn_button.text = "Недоступно (" + str(curr_progress) + "/" + str(req_progress) + ")"
            learn_button.disabled = true
```

**Изменения:**

1. ✅ Добавлена проверка `can_learn.reason`
2. ✅ Если причина "Требуется изучить предыдущий уровень", показываем соответствующее сообщение
3. ✅ Кнопка всегда отключена (`disabled = true`), если способность недоступна

---

## 📊 Результат после исправления

### До исправления:

```
Крысиная живучесть (ур. 2.0) (Common)
[Купить ур. 2.0 (1000 осколков)] ← АКТИВНА ❌
```

### После исправления:

```
Крысиная живучесть (ур. 2.0) (Common)
[Требуется изучить ур. 1] ← НЕАКТИВНА ✅
```

### Когда уровень 1 изучен:

```
Крысиная живучесть (ур. 2.0) (Common)
Прогресс: 200 / 500 очков
[Недоступно (200/500)] ← Показываем прогресс ✅
```

### Когда прогресс достигнут:

```
Крысиная живучесть (ур. 2.0) (Common)
Прогресс: 500 / 500 очков
[Купить ур. 2.0 (1000 осколков)] ← АКТИВНА ✅
```

---

## 🎯 Логика проверки

### Последовательность проверок:

1. **Уже изучено?** → "Изучено" (неактивна)
2. **Предыдущий уровень не изучен?** → "Требуется изучить ур. X" (неактивна)
3. **Недостаточно прогресса?** → "Недоступно (X/Y)" (неактивна)
4. **Недостаточно осколков?** → Не показываем (это проверяется при клике)
5. **Все условия выполнены!** → "Купить ур. X (Y осколков)" (активна)

---

## 💡 Связь с другими исправлениями

Это исправление работает вместе с:

1. **ABILITY_PROGRESS_SYSTEM_FIX.md** - ограничение накопления прогресса
2. **AbilityLearningSystem.gd (строки 690-695)** - проверка предыдущего уровня в логике

Теперь вся система работает последовательно:
- ✅ Прогресс не копится выше лимита текущего уровня
- ✅ Нельзя изучить следующий уровень без предыдущего (логика)
- ✅ UI не показывает кнопку покупки для недоступных уровней (UI)

---

## 📁 Измененные файлы

**Только один файл:**

`Scripts/UI/AbilityLearningScreen.gd`
- Строки 375-386: Добавлена проверка причины недоступности
- Добавлено сообщение "Требуется изучить ур. X"

---

## 🧪 Тестирование

### Проверить:

1. ✅ Способность уровня 2 недоступна, пока не изучен уровень 1
2. ✅ Сообщение "Требуется изучить ур. 1" отображается правильно
3. ✅ После изучения уровня 1, показывается прогресс для уровня 2
4. ✅ Способность уровня 3 недоступна, пока не изучен уровень 2
5. ✅ Кнопка активна только когда все условия выполнены

### Ожидаемое поведение:

**Способность на уровне 0 (не изучена):**
```
Уровень 1:
- Прогресс: 0/100 → "Недоступно (0/100)"
- Прогресс: 100/100 → "Купить ур. 1 (100 осколков)"

Уровень 2: "Требуется изучить ур. 1"
Уровень 3: "Требуется изучить ур. 1"
```

**Способность на уровне 1 (изучена):**
```
Уровень 1: "Изучено"

Уровень 2:
- Прогресс: 0/500 → "Недоступно (0/500)"
- Прогресс: 500/500 → "Купить ур. 2 (1000 осколков)"

Уровень 3: "Требуется изучить ур. 2"
```

**Способность на уровне 2 (изучена):**
```
Уровень 1: "Изучено"
Уровень 2: "Изучено"

Уровень 3:
- Прогресс: 0/1000 → "Недоступно (0/1000)"
- Прогресс: 1000/1000 → "Купить ур. 3 (10000 осколков)"
```

---

## 💡 Итог

### Что исправлено:
✅ UI теперь проверяет причину недоступности способности

### Что изменилось:
✅ Добавлено сообщение "Требуется изучить ур. X" когда предыдущий уровень не изучен  
✅ Кнопка всегда неактивна для недоступных уровней  
✅ Игрок видит понятное объяснение, почему способность недоступна  

### Результат:
✅ Невозможно купить уровень 2 без изучения уровня 1  
✅ Последовательное изучение способностей работает правильно  
✅ UI соответствует логике системы  
✅ Игрок понимает, что нужно сделать для разблокировки  

🎯 **Система прогрессии способностей теперь работает правильно на всех уровнях!**

