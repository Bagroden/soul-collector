# 🔧 ОБНОВЛЕНИЕ СИСТЕМЫ ПРОГРЕССА ИЗУЧЕНИЯ СПОСОБНОСТЕЙ

## 📋 **КРАТКОЕ ОПИСАНИЕ**

Обновлена логика начисления очков прогресса за убийство врагов. Теперь количество очков зависит от редкости врага и его элитности, а не является фиксированным значением 100.

---

## ❌ **ЧТО БЫЛО (ДЛЯ ТЕСТИРОВАНИЯ)**

```gdscript
// БЫЛО: Фиксированное значение в конфигурации
"sources": {
    "common_rat": 100,      // 100 очков
    "uncommon_rat": 100,    // 100 очков
    "rare_rat": 100,        // 100 очков
    "epic_rat": 100,        // 100 очков
    "legendary_rat": 100   // 100 очков
}

// Все враги давали одинаково 100 очков
```

---

## ✅ **ЧТО СТАЛО**

```gdscript
// СТАЛО: Динамический расчет на основе редкости
func _get_progress_by_rarity(rarity: String) -> int:
    match base_rarity:
        "common":      return 10   // × 2 = 20 (элитные)
        "uncommon":    return 20   // × 2 = 40 (элитные)
        "rare":        return 30   // × 2 = 60 (элитные)
        "epic":        return 40   // × 2 = 80 (элитные)
        "legendary":   return 50   // × 2 = 100 (элитные)
        "mythic":      return 100  // × 2 = 200 (элитные)
```

---

## 📊 **НОВАЯ СИСТЕМА РАСЧЕТА ОЧКОВ**

### **Базовые очки по редкости:**

| Редкость | Базовые очки | Элитные очки (×2) |
|----------|-------------|-------------------|
| **Common** | 10 | 20 |
| **Uncommon** | 20 | 40 |
| **Rare** | 30 | 60 |
| **Epic** | 40 | 80 |
| **Legendary** | 50 | 100 |
| **Mythic** | 100 | 200 |

### **Требования для изучения уровней:**

| Уровень | Требуется очков |
|---------|----------------|
| **1** | 100 |
| **2** | 500 |
| **3** | 1000 |

---

## 🔧 **ТЕХНИЧЕСКИЕ ИЗМЕНЕНИЯ**

### **Файл:** `Scripts/Systems/AbilityLearningSystem.gd`

#### **1. Модифицирована функция `add_progress()`:**

```gdscript
// БЫЛО:
func add_progress(enemy_type: String, enemy_rarity: String) -> Array:
    var enemy_key = enemy_rarity + "_" + enemy_type
    var progress_gained = []
    
    for ability_id in ability_learning_config:
        var config = ability_learning_config[ability_id]
        if enemy_key in config.sources:
            var progress_gain = config.sources[enemy_key]  // ❌ Брали из конфига (100)
            _add_ability_progress(ability_id, progress_gain)

// СТАЛО:
func add_progress(enemy_type: String, enemy_rarity: String) -> Array:
    var enemy_key = enemy_rarity + "_" + enemy_type
    var progress_gained = []
    
    // ✅ Рассчитываем очки на основе редкости
    var base_progress = _get_progress_by_rarity(enemy_rarity)
    
    for ability_id in ability_learning_config:
        var config = ability_learning_config[ability_id]
        if enemy_key in config.sources:
            var progress_gain = base_progress  // ✅ Используем рассчитанный прогресс
            _add_ability_progress(ability_id, progress_gain)
```

#### **2. Добавлена новая функция `_get_progress_by_rarity()`:**

```gdscript
func _get_progress_by_rarity(rarity: String) -> int:
    """Возвращает количество очков прогресса на основе редкости врага"""
    var base_rarity = rarity.to_lower()
    var is_elite = false
    
    // Проверяем элитность
    if base_rarity.begins_with("elite_"):
        is_elite = true
        base_rarity = base_rarity.substr(6)  // Убираем "elite_"
    
    // Определяем базовые очки
    var base_points = 0
    match base_rarity:
        "common":    base_points = 10
        "uncommon":  base_points = 20
        "rare":      base_points = 30
        "epic":      base_points = 40
        "legendary": base_points = 50
        "mythic":    base_points = 100
        _:           base_points = 10  // Fallback
    
    // Элитные враги → ×2
    if is_elite:
        base_points *= 2
    
    return base_points
```

---

## 📈 **ПРИМЕРЫ РАСЧЕТОВ**

### **Пример 1: Обычная крыса (Common)**
```
Редкость: Common
Базовые очки: 10
Элитность: Нет
Итого: 10 очков
```

### **Пример 2: Элитный гоблин воин (Elite Rare)**
```
Редкость: Rare
Базовые очки: 30
Элитность: Да (×2)
Итого: 60 очков
```

### **Пример 3: Легендарная летучая мышь (Legendary)**
```
Редкость: Legendary
Базовые очки: 50
Элитность: Нет
Итого: 50 очков
```

### **Пример 4: Элитный мифический демон (Elite Mythic)**
```
Редкость: Mythic
Базовые очки: 100
Элитность: Да (×2)
Итого: 200 очков
```

---

## 🎯 **ВЛИЯНИЕ НА ГЕЙМПЛЕЙ**

### **До изменений (тестовая система):**
- Все враги давали **100 очков**
- Уровень 1 = 1 враг
- Уровень 2 = 5 врагов
- Уровень 3 = 10 врагов
- **Очень быстро**, нет смысла искать редких врагов

### **После изменений (балансная система):**
- Враги дают от **10 до 200 очков** (в зависимости от редкости)
- Уровень 1 = 10 Common (или 5 Uncommon, или 1 Mythic)
- Уровень 2 = 50 Common (или 10 Legendary, или 5 Mythic)
- Уровень 3 = 100 Common (или 20 Legendary, или 10 Mythic)
- **Сбалансировано**, есть мотивация искать редких/элитных врагов

---

## 💡 **СТРАТЕГИЧЕСКИЕ ВОЗМОЖНОСТИ**

### **Быстрое изучение (сложнее):**
- Фармить **элитных** и **мифических** врагов
- Пример: 5 × Элитный Mythic = 1000 очков = Уровень 3 ✅
- **Риск:** Сложные враги
- **Награда:** Быстрый прогресс

### **Медленное изучение (безопаснее):**
- Фармить **обычных** и **необычных** врагов
- Пример: 100 × Common = 1000 очков = Уровень 3 ✅
- **Риск:** Минимальный
- **Награда:** Медленный, но стабильный прогресс

### **Сбалансированный подход:**
- Комбинировать разных врагов
- Пример: 10 × Legendary + 25 × Uncommon = 1000 очков ✅
- **Риск:** Средний
- **Награда:** Умеренный прогресс

---

## 🔍 **ПРОВЕРКА ЛОГИКИ**

### **Тест 1: Common враг**
```gdscript
add_progress("Крыса", "common")
// Ожидаемый результат: +10 очков
```

### **Тест 2: Rare враг**
```gdscript
add_progress("Гоблин Воин", "rare")
// Ожидаемый результат: +30 очков
```

### **Тест 3: Elite Rare враг**
```gdscript
add_progress("Гоблин Воин", "elite_rare")
// Ожидаемый результат: +60 очков (30 × 2)
```

### **Тест 4: Mythic враг**
```gdscript
add_progress("Демон", "mythic")
// Ожидаемый результат: +100 очков
```

### **Тест 5: Elite Mythic враг**
```gdscript
add_progress("Демон", "elite_mythic")
// Ожидаемый результат: +200 очков (100 × 2)
```

---

## ⚙️ **СОВМЕСТИМОСТЬ**

### **Конфигурация `ability_learning_config`:**
- ✅ **Не требует изменений**
- Значения в `sources` теперь **игнорируются**
- Функция проверяет только **наличие** ключа в `sources`
- Фактические очки **рассчитываются динамически**

### **Старые сохранения:**
- ✅ **Полностью совместимы**
- Прогресс продолжает накапливаться
- Никаких проблем с конвертацией

---

## 📝 **ИТОГ**

### **Изменено:**
- ✅ Функция `add_progress()` теперь использует динамический расчет
- ✅ Добавлена функция `_get_progress_by_rarity()` для расчета очков
- ✅ Элитные враги корректно дают ×2 прогресса

### **Результат:**
- ✅ Сбалансированная прогрессия изучения способностей
- ✅ Мотивация искать редких и элитных врагов
- ✅ Соответствие логике из справки
- ✅ Никаких ошибок linter

---

## 📊 **ТАБЛИЦА СРАВНЕНИЯ: СКОЛЬКО ВРАГОВ ДЛЯ УРОВНЯ 3**

| Тип врага | Очков за врага | Врагов для 1000 очков |
|-----------|----------------|----------------------|
| Common | 10 | 100 врагов |
| Uncommon | 20 | 50 врагов |
| Rare | 30 | 34 врага |
| Epic | 40 | 25 врагов |
| Legendary | 50 | 20 врагов |
| Mythic | 100 | 10 врагов |
| **Elite Common** | 20 | 50 врагов |
| **Elite Uncommon** | 40 | 25 врагов |
| **Elite Rare** | 60 | 17 врагов |
| **Elite Epic** | 80 | 13 врагов |
| **Elite Legendary** | 100 | 10 врагов |
| **Elite Mythic** | 200 | **5 врагов** ⭐ |

---

**Автор:** Claude AI Assistant  
**Дата:** 2024  
**Версия:** beta 0.6.11

