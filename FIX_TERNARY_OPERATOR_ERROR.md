# Исправление ошибки тернарного оператора

## 🎯 Проблема:
```
W 0:00:07:622   GDScript::reload: Values of the ternary operator are not mutually compatible.
  <Ошибка GDScript>INCOMPATIBLE_TERNARY
  <Исходный код GDScript>character_visual.gd:61 @ GDScript::reload()
```

## 🔍 Анализ проблемы:

### **1. Проблемный код:**
```gdscript
print("Доступные анимации: ", sprite_frames.get_animation_names() if sprite_frames else "sprite_frames = null")
```

### **2. Проблема:**
- `sprite_frames.get_animation_names()` возвращает `Array[StringName]`
- `"sprite_frames = null"` - это `String`
- Тернарный оператор не может сравнивать `Array[StringName]` и `String`

## 🛠️ Исправление:

### **Было (ошибка):**
```gdscript
print("Доступные анимации: ", sprite_frames.get_animation_names() if sprite_frames else "sprite_frames = null")
```

### **Стало (исправлено):**
```gdscript
if sprite_frames:
    print("Доступные анимации: ", sprite_frames.get_animation_names())
else:
    print("sprite_frames = null")
```

## ✅ Готово!

Теперь код:
- ✅ **Нет ошибок компиляции** - исправлен тернарный оператор
- ✅ **Правильная типизация** - разделены условия для разных типов
- ✅ **Читаемость** - код стал более понятным
- ✅ **Функциональность сохранена** - логика работы не изменилась

**Ошибка исправлена!** 🎮✨
