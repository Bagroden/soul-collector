# Цветовая стилизация кнопок фильтров редкости

## Дата
28 октября 2025

## Проблема
Кнопки фильтров редкости (Common, Uncommon, Rare, Epic, Legendary, Mythic) выглядели одинаково и теряются на экране изучения пассивных способностей. Пользователь не мог быстро визуально отличить одну редкость от другой.

## Решение

### Добавлена цветовая индикация для каждой кнопки фильтра

Каждая кнопка фильтра теперь имеет уникальный цвет фона и рамки, соответствующий редкости способностей.

## Цветовая схема

### Кнопка "Все"
- **Фон:** Темно-серый `Color(0.3, 0.3, 0.4, 0.7)`
- **Рамка:** Светло-серый `Color(0.6, 0.6, 0.7, 1.0)`
- **Наведение:** Фон становится ярче, рамка толще (3px)

### Кнопка "Common"
- **Фон:** Серый `Color(0.5, 0.5, 0.5, 0.6)`
- **Рамка:** Светло-серый `Color(0.8, 0.8, 0.8, 1.0)`
- **Наведение:** Фон светлеет на 30%, рамка 3px

### Кнопка "Uncommon"
- **Фон:** Зеленый `Color(0.2, 0.6, 0.2, 0.6)`
- **Рамка:** Яркий зеленый `Color(0.3, 0.9, 0.3, 1.0)`
- **Наведение:** Фон светлеет на 30%, рамка 3px

### Кнопка "Rare"
- **Фон:** Синий `Color(0.2, 0.4, 0.8, 0.6)`
- **Рамка:** Яркий синий `Color(0.3, 0.6, 1.0, 1.0)`
- **Наведение:** Фон светлеет на 30%, рамка 3px

### Кнопка "Epic"
- **Фон:** Фиолетовый `Color(0.6, 0.2, 0.8, 0.6)`
- **Рамка:** Яркий фиолетовый `Color(0.8, 0.3, 1.0, 1.0)`
- **Наведение:** Фон светлеет на 30%, рамка 3px

### Кнопка "Legendary"
- **Фон:** Оранжевый `Color(0.8, 0.5, 0.1, 0.6)`
- **Рамка:** Яркий оранжевый `Color(1.0, 0.7, 0.2, 1.0)`
- **Наведение:** Фон светлеет на 30%, рамка 3px

### Кнопка "Mythic"
- **Фон:** Красный `Color(0.8, 0.1, 0.1, 0.6)`
- **Рамка:** Яркий красный `Color(1.0, 0.2, 0.2, 1.0)`
- **Наведение:** Фон светлеет на 30%, рамка 3px

## Технические детали

### Новая функция `_style_rarity_filter_buttons()`

```gdscript
func _style_rarity_filter_buttons():
    """Применяет цветовую стилизацию к кнопкам фильтров редкости"""
    # Словарь с цветами фона для каждой редкости
    var rarity_bg_colors = {
        "common": Color(0.5, 0.5, 0.5, 0.6),      # Серый
        "uncommon": Color(0.2, 0.6, 0.2, 0.6),    # Зеленый
        "rare": Color(0.2, 0.4, 0.8, 0.6),        # Синий
        "epic": Color(0.6, 0.2, 0.8, 0.6),        # Фиолетовый
        "legendary": Color(0.8, 0.5, 0.1, 0.6),   # Оранжевый
        "mythic": Color(0.8, 0.1, 0.1, 0.6)       # Красный
    }
    
    # Словарь с цветами рамки (более яркие версии)
    var rarity_border_colors = {
        "common": Color(0.8, 0.8, 0.8, 1.0),
        "uncommon": Color(0.3, 0.9, 0.3, 1.0),
        "rare": Color(0.3, 0.6, 1.0, 1.0),
        "epic": Color(0.8, 0.3, 1.0, 1.0),
        "legendary": Color(1.0, 0.7, 0.2, 1.0),
        "mythic": Color(1.0, 0.2, 0.2, 1.0)
    }
    
    # Применяем стили к каждой кнопке фильтра
    for rarity in filters.keys():
        var btn = filters[rarity]
        
        # Создаем стиль для нормального состояния
        var normal_style = StyleBoxFlat.new()
        normal_style.bg_color = rarity_bg_colors[rarity]
        normal_style.border_color = rarity_border_colors[rarity]
        normal_style.border_width_left = 2
        normal_style.border_width_right = 2
        normal_style.border_width_top = 2
        normal_style.border_width_bottom = 2
        normal_style.corner_radius_top_left = 6
        normal_style.corner_radius_top_right = 6
        normal_style.corner_radius_bottom_left = 6
        normal_style.corner_radius_bottom_right = 6
        
        # Создаем стиль для наведения (более яркий)
        var hover_style = StyleBoxFlat.new()
        hover_style.bg_color = Color(
            rarity_bg_colors[rarity].r * 1.3,
            rarity_bg_colors[rarity].g * 1.3,
            rarity_bg_colors[rarity].b * 1.3,
            0.8
        )
        hover_style.border_color = rarity_border_colors[rarity]
        hover_style.border_width_left = 3
        hover_style.border_width_right = 3
        hover_style.border_width_top = 3
        hover_style.border_width_bottom = 3
        
        # Применяем стили
        btn.add_theme_stylebox_override("normal", normal_style)
        btn.add_theme_stylebox_override("hover", hover_style)
        btn.add_theme_stylebox_override("pressed", hover_style)
        btn.add_theme_color_override("font_color", Color.WHITE)
        btn.add_theme_font_size_override("font_size", 14)
```

### Интеграция в инициализацию

Функция вызывается в `_ready()` после инициализации системы вкладок:

```gdscript
func _ready():
    # ...
    # Инициализируем систему вкладок
    _setup_tab_system()
    
    # Рассчитываем реальное количество способностей по редкостям
    _calculate_rarity_totals()
    
    # Стилизуем кнопки фильтров редкости
    _style_rarity_filter_buttons()
    
    # Инициализируем UI
    _update_filter_buttons_with_progress()
    update_abilities_list()
```

## Визуальные эффекты

### Состояния кнопок

#### Нормальное состояние:
- Полупрозрачный цветной фон (60% прозрачности)
- Яркая цветная рамка 2px
- Белый текст
- Скругленные углы 6px

#### Наведение курсора:
- Фон становится на 30% ярче
- Прозрачность увеличивается до 80%
- Рамка утолщается до 3px
- Визуальная обратная связь о возможности клика

#### Нажатие:
- Идентично состоянию наведения
- Показывает активное взаимодействие

### Адаптивность цветов

Цвета соответствуют общей цветовой схеме редкостей в игре:
- ✅ Консистентность с цветами названий способностей
- ✅ Консистентность с цветами в других частях UI
- ✅ Хорошая читаемость белого текста на всех фонах

## Преимущества

### Улучшенная навигация:
✅ Мгновенное распознавание типа редкости по цвету
✅ Быстрый поиск нужного фильтра
✅ Снижение когнитивной нагрузки

### Профессиональный внешний вид:
✅ Современный дизайн с цветовыми акцентами
✅ Плавные переходы при наведении
✅ Консистентность с остальным UI

### Лучший UX:
✅ Визуальная обратная связь при взаимодействии
✅ Четкое разделение между категориями
✅ Приятная визуальная иерархия

### Accessibility:
✅ Высокая контрастность цветов
✅ Белый текст на всех фонах для читаемости
✅ Увеличение рамки при наведении для лучшего фокуса

## Пример использования

### Визуальный результат:

```
🔍 Фильтр: [Обычные способности] [Развитие души] | 
           [Все] [Common 4/16] [Uncommon 0/14] [Rare 0/16] [Epic 0/17] [Legendary 0/17] [Mythic 0/3]
            ╱           ╱              ╱             ╱            ╱              ╱
        Темно-серый   Серый      Зеленый       Синий     Фиолетовый    Оранжевый    Красный
```

### Интерактивность:

1. **Наведение на "Rare 0/16":**
   - Фон становится ярче (синий → светло-синий)
   - Рамка утолщается (2px → 3px)
   - Курсор меняется на "pointer"

2. **Клик на "Epic 0/17":**
   - Список фильтруется, показывая только Epic способности
   - Кнопка остается подсвеченной (состояние "pressed")
   - Счётчик показывает "0 изучено из 17 доступных"

## Изменённые файлы

### `Scripts/UI/AbilityLearningScreen.gd`
- **Добавлено:** Функция `_style_rarity_filter_buttons()`
- **Изменено:** `_ready()` - добавлен вызов `_style_rarity_filter_buttons()`

### Никаких изменений в `.tscn` файлах
Стилизация применяется программно через GDScript, что даёт:
- ✅ Гибкость в настройке цветов
- ✅ Возможность динамических изменений
- ✅ Лёгкое обслуживание и модификация

## Совместимость

### С существующими системами:
✅ Не влияет на логику фильтрации
✅ Не изменяет функциональность кнопок
✅ Совместимо с системой подсчёта прогресса

### С темами Godot:
✅ Использует `add_theme_stylebox_override()` для применения стилей
✅ При необходимости можно легко сбросить стили
✅ Не конфликтует с глобальной темой проекта

## Результат
✅ Все кнопки фильтров редкости имеют уникальные цвета
✅ Кнопка "Все" также стилизована
✅ Визуальная обратная связь при наведении и нажатии
✅ Улучшенная навигация и UX
✅ Профессиональный и современный внешний вид
✅ Полная консистентность с цветовой схемой редкостей в игре

