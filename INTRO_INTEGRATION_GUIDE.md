# 🔌 Руководство по интеграции интро в игру

## Шаг 1: Добавление IntroManager в автозагрузку

### В Project Settings → Autoload:

Добавить новый автозагружаемый скрипт:
- **Path:** `res://Scripts/IntroManager.gd`
- **Node Name:** `IntroManager`
- **Enabled:** ✅

---

## Шаг 2: Интеграция с главным меню

### Вариант A: Показ интро перед главным меню (рекомендуется)

Создать стартовую сцену `Scenes/Startup.tscn`:

```gdscript
# res://Scripts/Startup.gd
extends Node

func _ready():
	# Небольшая задержка для инициализации систем
	await get_tree().create_timer(0.5).timeout
	
	# Проверяем, нужно ли показать интро
	if IntroManager.should_show_intro():
		IntroManager.show_intro()
	else:
		# Переходим сразу к главному меню
		SceneTransition.change_scene_to_file("res://Scenes/MainMenu.tscn")
```

Установить `Startup.tscn` как главную сцену в Project Settings → Application → Run → Main Scene

### Вариант B: Интеграция в MainMenu.gd

Добавить в начало `_ready()`:

```gdscript
func _ready():
	# Проверка первого запуска
	if IntroManager.should_show_intro():
		# Скрываем меню и показываем интро
		visible = false
		await get_tree().create_timer(0.1).timeout
		IntroManager.show_intro()
		return
	
	# Обычная инициализация меню
	# ... существующий код ...
```

---

## Шаг 3: Обновление IntroScene.gd

Добавить вызов `mark_intro_as_shown()` при завершении:

```gdscript
func _finish_intro():
	"""Завершает показ интро и переходит к главному меню"""
	# Отмечаем, что интро показано
	IntroManager.mark_intro_as_shown()
	
	# Fade to black
	var final_fade = create_tween()
	final_fade.tween_property(fade_overlay, "modulate:a", 1.0, 1.5)
	await final_fade.finished
	
	# Переход к главному меню
	SceneTransition.change_scene_to_file("res://Scenes/MainMenu.tscn")
```

То же самое для функции пропуска:

```gdscript
func _on_skip_pressed():
	"""Обработчик нажатия кнопки пропуска"""
	if not can_skip:
		return
	
	can_skip = false
	
	# Отмечаем интро как просмотренное даже при пропуске
	IntroManager.mark_intro_as_shown()
	
	# ... остальной код ...
```

---

## Шаг 4: Добавление опции "Посмотреть интро" в настройки (опционально)

В меню настроек или главном меню добавить кнопку:

```gdscript
func _on_replay_intro_pressed():
	"""Показывает интро заново"""
	# НЕ вызываем mark_intro_as_shown(), чтобы интро показалось снова при следующем запуске
	SceneTransition.change_scene_to_file("res://Scenes/UI/IntroScene.tscn")
```

Или с временным сбросом флага:

```gdscript
func _on_replay_intro_pressed():
	"""Показывает интро заново и сбрасывает флаг"""
	IntroManager.reset_intro_flag()
	SceneTransition.change_scene_to_file("res://Scenes/UI/IntroScene.tscn")
```

---

## Шаг 5: Создание placeholder изображений

Пока нет финальных изображений, создайте простые placeholder'ы:

```gdscript
# Скрипт для генерации placeholder изображений
extends Node

func generate_placeholder_images():
	for i in range(1, 11):
		var img = Image.create(1920, 1080, false, Image.FORMAT_RGBA8)
		# Заливаем градиентом
		for y in range(1080):
			for x in range(1920):
				var color = Color(
					float(i) / 10.0,
					float(y) / 1080.0,
					float(x) / 1920.0,
					1.0
				)
				img.set_pixel(x, y, color)
		
		# Сохраняем
		var texture = ImageTexture.create_from_image(img)
		var path = "res://Assets/Intro/slide_%02d_placeholder.png" % i
		img.save_png(path)
		print("✅ Создан placeholder: ", path)
```

---

## Шаг 6: Тестирование

### Тест 1: Первый запуск
1. Удалить файл `user://intro_shown.dat`
2. Запустить игру
3. Должно показаться интро

### Тест 2: Последующие запуски
1. Запустить игру еще раз
2. Интро НЕ должно показаться

### Тест 3: Пропуск
1. Удалить `user://intro_shown.dat`
2. Запустить игру
3. Нажать ESC
4. Должен быть переход к главному меню

### Тест 4: Повтор
1. Нажать кнопку "Посмотреть интро"
2. Интро должно показаться

---

## 🎮 Полный код интеграции

### Startup.gd (рекомендуемый подход)

```gdscript
# res://Scripts/Startup.gd
extends Control

func _ready():
	# Показываем логотип студии (опционально)
	# await _show_studio_logo()
	
	# Небольшая задержка для инициализации
	await get_tree().create_timer(0.3).timeout
	
	# Проверяем интро
	_check_and_show_intro()

func _check_and_show_intro():
	"""Проверяет и показывает интро при необходимости"""
	if IntroManager.should_show_intro():
		print("🎬 Первый запуск - показываем интро")
		IntroManager.show_intro()
	else:
		print("⏩ Интро уже показано - переход к меню")
		SceneTransition.change_scene_to_file("res://Scenes/MainMenu.tscn")

func _show_studio_logo():
	"""Показывает логотип студии перед интро"""
	# Fade in логотипа
	# ...
	await get_tree().create_timer(3.0).timeout
```

### Startup.tscn

```
Control (Startup.gd)
└── ColorRect (черный фон)
```

### Project Settings

**Application → Run → Main Scene:** `res://Scenes/Startup.tscn`

---

## 🔧 Debug команды

Для тестирования добавить в консоль разработчика:

```gdscript
# Сброс интро
func debug_reset_intro():
	IntroManager.reset_intro_flag()
	print("✅ Интро сброшено")

# Показать интро принудительно
func debug_show_intro():
	IntroManager.show_intro()

# Проверить статус
func debug_intro_status():
	print("Интро показано: ", IntroManager.is_intro_shown())
```

---

## ⚠️ Важные замечания

1. **Автозагрузка:** `IntroManager` должен быть в автозагрузке ПЕРЕД проверкой
2. **SceneTransition:** Убедитесь что `SceneTransition` работает корректно
3. **Пути к файлам:** Проверьте все пути к изображениям
4. **Сохранения:** Файл `intro_shown.dat` создается в `user://` директории
5. **Тестирование:** Всегда тестируйте с чистым профилем (удалите `user://`)

---

## 📁 Структура проекта после интеграции

```
Scenes/
├── Startup.tscn                    # Стартовая сцена (новая)
├── MainMenu.tscn                   # Главное меню
└── UI/
    └── IntroScene.tscn             # Сцена интро

Scripts/
├── Startup.gd                      # Скрипт стартовой сцены (новый)
├── IntroManager.gd                 # Менеджер интро (автозагрузка)
└── UI/
    └── IntroScene.gd               # Скрипт интро

Assets/
├── Intro/
│   ├── slide_01_war.png
│   ├── slide_02_meeting.png
│   └── ... (все 10 слайдов)
└── Audio/
    └── Intro/
        ├── narration_slide_01.ogg
        ├── narration_slide_02.ogg
        ├── ... (все 10 озвучек)
        └── intro_music.ogg (опционально)
```

---

## ✅ Чеклист интеграции

- [ ] Создан `IntroManager.gd`
- [ ] `IntroManager` добавлен в Autoload
- [ ] Создана сцена `IntroScene.tscn`
- [ ] Создан скрипт `IntroScene.gd`
- [ ] Создана стартовая сцена `Startup.tscn` и `Startup.gd`
- [ ] Установлена главная сцена в Project Settings
- [ ] Добавлен вызов `mark_intro_as_shown()` в IntroScene
- [ ] Созданы placeholder изображения (или финальные)
- [ ] Созданы аудиофайлы с озвучкой (опционально)
- [ ] Добавлена фоновая музыка (опционально)
- [ ] Протестирован первый запуск
- [ ] Протестирован последующий запуск
- [ ] Протестирована функция пропуска
- [ ] Протестирована синхронизация аудио и слайдов
- [ ] Добавлена кнопка "Посмотреть интро" в меню (опционально)

---

## 🎙️ Дополнение: Подготовка озвучки

### Шаг 1: Генерация озвучки

Используйте файл `INTRO_VOICEOVER_SCRIPT.md` для создания аудиофайлов с помощью:
- **ElevenLabs** (рекомендуется для качества)
- **Play.ht** (хорош для русского языка)
- **Azure TTS** (бесплатная альтернатива)
- Или запись живым голосом актера

### Шаг 2: Конвертация в OGG

После генерации MP3/WAV файлов, конвертируйте их в OGG:

```bash
# Используя FFmpeg
ffmpeg -i narration_slide_01.mp3 -c:a libvorbis -q:a 7 narration_slide_01.ogg
```

Или используйте онлайн-конвертер: https://cloudconvert.com/mp3-to-ogg

### Шаг 3: Импорт в Godot

1. Разместите файлы в `Assets/Audio/Intro/`
2. Godot автоматически импортирует их
3. Проверьте настройки импорта (Loop: Off, Compress: On)

### Шаг 4: Проверка тайминга

Убедитесь, что длительность каждого аудиофайла соответствует длительности слайда:

| Слайд | Длительность | Аудиофайл |
|-------|--------------|-----------|
| 1     | 4.0 сек      | narration_slide_01.ogg |
| 2     | 3.5 сек      | narration_slide_02.ogg |
| 3     | 4.0 сек      | narration_slide_03.ogg |
| 4     | 5.0 сек      | narration_slide_04.ogg |
| 5     | 3.5 сек      | narration_slide_05.ogg |
| 6     | 4.0 сек      | narration_slide_06.ogg |
| 7     | 4.5 сек      | narration_slide_07.ogg |
| 8     | 5.0 сек      | narration_slide_08.ogg |
| 9     | 4.5 сек      | narration_slide_09.ogg |
| 10    | 5.0 сек      | narration_slide_10.ogg |

### Шаг 5: Настройка громкости

В Godot настройте audio buses:
1. Откройте Audio Bus panel
2. Создайте bus "Voiceover" (если нужно)
3. Настройте Volume для баланса с музыкой

---

## 🎵 Дополнение: Фоновая музыка

### Включение фоновой музыки:

1. Откройте `Scripts/UI/IntroScene.gd`
2. Найдите строку 10-12 в `_ready()`:
```gdscript
# Запускаем фоновую музыку (опционально)
# Раскомментируйте следующую строку для добавления музыки:
# _play_background_music()
```
3. Раскомментируйте: `_play_background_music()`

### Подготовка музыки:

- **Длительность:** ~43 секунды (общая длина интро)
- **Стиль:** Epic dark fantasy, cinematic
- **Громкость:** -10 dB (тише голоса)
- **Формат:** OGG Vorbis
- **Путь:** `Assets/Audio/Intro/intro_music.ogg`

### Рекомендуемые источники музыки:

- **Royalty-free:** [Pixabay Music](https://pixabay.com/music/)
- **Epic music:** [Epidemic Sound](https://www.epidemicsound.com/)
- **AI generation:** [Suno AI](https://suno.ai/), [Udio](https://udio.com/)
- **Free libraries:** [Free Music Archive](https://freemusicarchive.org/)

