# Исправление отступов для способностей врагов

## 🔴 Проблема

**Крыса** и **Летучая мышь** использовали свои активные способности ("Крысиный укус" и "Пикирование"), но **урон игроку не наносился**!

### Логи боя:

**Крыса:**
```
DEBUG: Способность 'Крысиный укус' (rat_bite_attack) на кулдауне: false
DEBUG: Может использовать (ресурсы): true
Player - Уворот (не сработала)
DEBUG: Установлен кулдаун для 'Крысиный укус': 2 ходов
Сообщение: Враг использовал Крысиный укус! физический урон: 36
```

**Летучая мышь:**
```
DEBUG: Способность 'Пикирование' (bat_swoop) на кулдауне: false
DEBUG: Может использовать (ресурсы): true
Player - Уворот (не сработала)
DEBUG: Установлен кулдаун для 'Пикирование': 5 ходов
Сообщение: Враг использовал Пикирование! физический урон: 37
```

Для обоих случаев:
- ✅ Способность использовалась
- ✅ Кулдаун устанавливался
- ✅ Сообщение показывалось
- ❌ **Урон НЕ наносился** (нет записи "Player получил урон")

---

## 🔍 Причина

**Файл:** `Scripts/Battle/battle_manager.gd`

**Неправильное количество табов** в цепочке `if/elif`!

### Структура до исправления:

```gdscript
			if result.get("double_strike", false):       // 3 ТАБА ✅
			elif result.get("executioner_strike", false): // 3 ТАБА ✅
			elif result.get("poison_strike", false):      // 3 ТАБА ✅
			elif result.get("magic_arrows", false):       // 3 ТАБА ✅
		elif result.get("rat_bite", false):           // 2 ТАБА ❌
		elif result.get("bat_swoop", false):          // 2 ТАБА ❌
		elif result.get("rotten_blast", false):       // 2 ТАБА ❌
		elif result.get("acid_blast", false):         // 2 ТАБА ❌
		elif result.get("crushing_strike", false):    // 2 ТАБА ❌
		elif result.get("dark_blast", false):         // 2 ТАБА ❌
		elif result.get("curse_blast", false):        // 2 ТАБА ❌
		else:                                         // 2 ТАБА ❌
```

**Проблема:** Блоки с 2 табами находятся на **другом уровне вложенности** и **не участвуют в основной цепочке `if/elif`**!

Из-за этого для способностей `rat_bite`, `bat_swoop` и других код всегда попадал в блок `else:` (который тоже на неправильном уровне), что приводило к неправильной обработке урона.

---

## ✅ Исправление

**Файл:** `Scripts/Battle/battle_manager.gd`

Добавлен **1 таб** ко всем блокам, которые имели неправильные отступы:

### Структура после исправления:

```gdscript
			if result.get("double_strike", false):       // 3 ТАБА ✅
			elif result.get("executioner_strike", false): // 3 ТАБА ✅
			elif result.get("poison_strike", false):      // 3 ТАБА ✅
			elif result.get("magic_arrows", false):       // 3 ТАБА ✅
			elif result.get("rat_bite", false):           // 3 ТАБА ✅ ИСПРАВЛЕНО
			elif result.get("bat_swoop", false):          // 3 ТАБА ✅ ИСПРАВЛЕНО
			elif result.get("rotten_blast", false):       // 3 ТАБА ✅ ИСПРАВЛЕНО
			elif result.get("acid_blast", false):         // 3 ТАБА ✅ ИСПРАВЛЕНО
			elif result.get("crushing_strike", false):    // 3 ТАБА ✅ ИСПРАВЛЕНО
			elif result.get("dark_blast", false):         // 3 ТАБА ✅ ИСПРАВЛЕНО
			elif result.get("curse_blast", false):        // 3 ТАБА ✅ ИСПРАВЛЕНО
			else:                                         // 3 ТАБА ✅ ИСПРАВЛЕНО
```

Также добавлен **1 таб** ко всему содержимому этих блоков.

---

## 📊 Исправленные способности

### 1. **Крысиный укус** (`rat_bite`)
- Урон: сила + ловкость * 1.5
- 50% шанс кровотечения (1 стак, 3 хода, 5 урона/ход)
- ✅ Теперь наносит урон правильно

### 2. **Пикирование** (`bat_swoop`)
- Урон: сила + ловкость * 1.5
- 30% шанс оглушения (1 ход)
- ✅ Теперь наносит урон правильно

### 3. **Гнилостный взрыв** (`rotten_blast`)
- Урон + возможность активации гнилостной ауры
- ✅ Теперь работает правильно

### 4. **Кислотный взрыв** (`acid_blast`)
- Урон + снижение брони на 5
- ✅ Теперь работает правильно

### 5. **Сокрушающий удар** (`crushing_strike`)
- Два удара (обычный + усиленный)
- Возможность отнять 1 ОД
- ✅ Теперь работает правильно

### 6. **Темный взрыв** (`dark_blast`)
- Урон + вампиризм (50% урона)
- ✅ Теперь работает правильно

### 7. **Проклятый взрыв** (`curse_blast`)
- Урон + проклятие (-20% урона на 3 хода)
- ✅ Теперь работает правильно

### 8. **Обычные способности** (`else`)
- Все остальные способности врагов
- ✅ Теперь работают правильно

---

## 🎯 Связь с предыдущими исправлениями

Эта ошибка появилась после серии исправлений:

1. **RAT_BITE_DOUBLE_DAMAGE_FIX.md** - переместили код внутрь `rat_bite` для устранения двойного урона
2. **Исправления отступов (строки 1016, 1087)** - исправили отступы для некоторых блоков
3. **BAT_SWOOP_INDENTATION_FIX.md** - частично исправили отступы для `rat_bite` и `bat_swoop`
4. **ENEMY_ABILITIES_INDENTATION_FIX.md (текущий)** - полностью исправили все отступы

---

## 💡 Итог

### Что исправлено:
✅ Отступы для всех блоков приведены в соответствие (3 таба для всех `if/elif/else`)  
✅ Все содержимое блоков имеет правильные отступы (4 таба)  

### Что изменилось:
✅ "Крысиный укус" теперь работает правильно  
✅ "Пикирование" теперь работает правильно  
✅ "Гнилостный взрыв" теперь работает правильно  
✅ "Кислотный взрыв" теперь работает правильно  
✅ "Сокрушающий удар" теперь работает правильно  
✅ "Темный взрыв" теперь работает правильно  
✅ "Проклятый взрыв" теперь работает правильно  
✅ Все обычные способности теперь работают правильно  

### Результат:
✅ Все способности врагов работают правильно  
✅ Урон наносится только один раз  
✅ Специальные эффекты (кровотечение, оглушение, проклятие, вампиризм) применяются правильно  
✅ Цепочка `if/elif/else` работает корректно  
✅ Никаких ошибок линтера  

🎯 **Все способности врагов теперь работают правильно!**

