# 🔧 ИСПРАВЛЕНИЕ: TOOLTIP НЕ ОТОБРАЖАЕТСЯ

## ❌ **ПРОБЛЕМА**

После добавления системы подсказок для душ, tooltip не появлялись при наведении курсора на счетчики душ (Label узлы).

```
Ситуация:
1. Навожу курсор на "Колодец душ: 500000"
2. Жду несколько секунд
3. ❌ Ничего не происходит
4. ❌ Подсказка не появляется
```

---

## 🔍 **ПРИЧИНА**

По умолчанию в Godot все `Label` узлы имеют свойство:

```gdscript
mouse_filter = Control.MOUSE_FILTER_IGNORE
```

Это означает, что **Label игнорирует все события мыши**, включая:
- Наведение курсора
- Клики
- Перетаскивание

**Результат:** Tooltip **не срабатывает**, потому что Label не "видит" курсор мыши.

---

## ✅ **РЕШЕНИЕ**

Установить для каждого Label свойство:

```gdscript
mouse_filter = Control.MOUSE_FILTER_STOP
```

Это позволит Label'у **обрабатывать события мыши** и **отображать tooltip**.

---

## 🔧 **ЧТО БЫЛО ИЗМЕНЕНО**

### **Файл:** `Scripts/UI/CharacterPreparation.gd`

### **Функция:** `_setup_soul_tooltips()`

#### **БЫЛО (не работало):**
```gdscript
func _setup_soul_tooltips():
    if soul_shard_storage_label:
        soul_shard_storage_label.tooltip_text = "Осколки душ..."  // ❌ tooltip не работает
    
    if strong_souls_label:
        strong_souls_label.tooltip_text = "Сильные души..."  // ❌ tooltip не работает
    
    if great_souls_label:
        great_souls_label.tooltip_text = "Великие души..."  // ❌ tooltip не работает
    
    if divine_souls_label:
        divine_souls_label.tooltip_text = "Божественные души..."  // ❌ tooltip не работает
```

**Проблема:** Не установлен `mouse_filter`, Label игнорирует события мыши.

---

#### **СТАЛО (работает):**
```gdscript
func _setup_soul_tooltips():
    if soul_shard_storage_label:
        soul_shard_storage_label.mouse_filter = Control.MOUSE_FILTER_STOP  // ✅ ДОБАВЛЕНО
        soul_shard_storage_label.tooltip_text = "Осколки душ..."
    
    if strong_souls_label:
        strong_souls_label.mouse_filter = Control.MOUSE_FILTER_STOP  // ✅ ДОБАВЛЕНО
        strong_souls_label.tooltip_text = "Сильные души..."
    
    if great_souls_label:
        great_souls_label.mouse_filter = Control.MOUSE_FILTER_STOP  // ✅ ДОБАВЛЕНО
        great_souls_label.tooltip_text = "Великие души..."
    
    if divine_souls_label:
        divine_souls_label.mouse_filter = Control.MOUSE_FILTER_STOP  // ✅ ДОБАВЛЕНО
        divine_souls_label.tooltip_text = "Божественные души..."
```

**Исправлено:** Добавлено `mouse_filter = Control.MOUSE_FILTER_STOP` для каждого Label.

---

## 📊 **РЕЖИМЫ MOUSE_FILTER**

### **1. MOUSE_FILTER_IGNORE (по умолчанию для Label):**
```gdscript
mouse_filter = Control.MOUSE_FILTER_IGNORE
```

**Поведение:**
- ❌ Игнорирует все события мыши
- ❌ Tooltip не работает
- ✅ События проходят к элементам под Label
- 📝 Используется, когда Label - просто текст без взаимодействия

---

### **2. MOUSE_FILTER_STOP (используется для tooltip):**
```gdscript
mouse_filter = Control.MOUSE_FILTER_STOP
```

**Поведение:**
- ✅ Обрабатывает события мыши
- ✅ **Tooltip работает**
- ✅ Блокирует события для элементов под Label
- 📝 **Используется для интерактивных Label (tooltip, клики)**

---

### **3. MOUSE_FILTER_PASS:**
```gdscript
mouse_filter = Control.MOUSE_FILTER_PASS
```

**Поведение:**
- ✅ Обрабатывает события мыши
- ✅ Tooltip работает
- ⚠️ Пропускает события к элементам под Label
- 📝 Используется, когда нужно взаимодействие И с Label, И с элементами под ним

---

## 🎯 **КАКОЙ РЕЖИМ ВЫБРАТЬ?**

### **Для tooltip (наш случай):**
```gdscript
✅ MOUSE_FILTER_STOP
```
**Почему:** Tooltip должен работать, и нам не нужно, чтобы клики проходили через Label.

### **Для обычного текста без взаимодействия:**
```gdscript
✅ MOUSE_FILTER_IGNORE (по умолчанию)
```
**Почему:** Текст не интерактивен, события должны проходить к элементам под ним.

### **Для tooltip с прозрачными областями:**
```gdscript
✅ MOUSE_FILTER_PASS
```
**Почему:** Tooltip работает, но клики в пустых местах проходят дальше.

---

## ✅ **ПРОВЕРКА ИСПРАВЛЕНИЯ**

### **Тест 1: Колодец душ**
```
1. Запустить игру
2. Открыть меню подготовки к путешествию
3. Навести курсор на "Колодец душ: X"
4. Подождать 0.5 секунды

Ожидаемый результат:
✅ Появляется подсказка:
"Осколки душ - базовая валюта для изучения способностей.
Получаются за победу над любыми врагами."
```

---

### **Тест 2: Сильные души**
```
1. Запустить игру
2. Открыть меню подготовки к путешествию
3. Навести курсор на "Сильные души: X"
4. Подождать 0.5 секунды

Ожидаемый результат:
✅ Появляется подсказка:
"Сильные души получаются за победу над элитными врагами.
Элитные враги доступны со 2 уровня сложности локации."
```

---

### **Тест 3: Великие души**
```
1. Запустить игру
2. Открыть меню подготовки к путешествию
3. Навести курсор на "Великие души: X"
4. Подождать 0.5 секунды

Ожидаемый результат:
✅ Появляется подсказка:
"Великие души получаются за победу над финальным боссом локации.
Используются для изучения мощных способностей."
```

---

### **Тест 4: Божественные души**
```
1. Запустить игру
2. Открыть меню подготовки к путешествию
3. Навести курсор на "Божественные души: X"
4. Подождать 0.5 секунды

Ожидаемый результат:
✅ Появляется подсказка:
"Божественные души получаются за победу над мифическими врагами.
Будут введены в следующих обновлениях."
```

---

### **Тест 5: Скрытие подсказки**
```
1. Навести курсор на любой счетчик душ
2. Дождаться появления подсказки
3. Убрать курсор с элемента

Ожидаемый результат:
✅ Подсказка исчезает
```

---

## 🔍 **ДОПОЛНИТЕЛЬНАЯ ИНФОРМАЦИЯ**

### **Порядок обработки событий в Godot:**

```
1. Событие мыши (наведение, клик)
       ↓
2. Проверка mouse_filter верхнего Control
       ↓
   ┌─────────────────────────┐
   │ MOUSE_FILTER_IGNORE?    │ → Да → Событие проходит дальше
   └─────────────────────────┘    ↓
       ↓ Нет                     К элементам под Control
   ┌─────────────────────────┐
   │ MOUSE_FILTER_STOP?      │ → Да → Control обрабатывает + блокирует
   └─────────────────────────┘
       ↓ Нет
   ┌─────────────────────────┐
   │ MOUSE_FILTER_PASS?      │ → Да → Control обрабатывает + пропускает
   └─────────────────────────┘
```

---

### **Почему Label по умолчанию IGNORE?**

**Дизайн Godot:**
- Label обычно используется **только для отображения текста**
- В большинстве случаев **не требуется взаимодействие**
- По умолчанию **события должны проходить** к кнопкам/элементам под текстом

**Пример:**
```
┌──────────────────────────┐
│  [Кнопка]                │
│                          │
│  Label: "Нажми кнопку"   │ ← mouse_filter = IGNORE
└──────────────────────────┘

Если Label был бы STOP по умолчанию:
❌ Клик на текст "Нажми кнопку" НЕ активировал бы кнопку под ним
❌ Пользователь должен был бы кликать точно на кнопку

С IGNORE:
✅ Клик на текст проходит к кнопке под ним
✅ Удобнее для пользователя
```

---

### **Когда нужно менять mouse_filter для Label?**

#### **Нужно (STOP или PASS):**
- ✅ Tooltip (наш случай)
- ✅ Кликабельные Label
- ✅ Перетаскиваемые Label
- ✅ Label с подсветкой при наведении

#### **Не нужно (оставить IGNORE):**
- ✅ Обычный текст без взаимодействия
- ✅ Label на прозрачном фоне
- ✅ Label поверх кнопок (чтобы кнопки работали)

---

## 📝 **ИТОГ**

### **Проблема:**
- ❌ Tooltip не появлялся при наведении на Label

### **Причина:**
- ❌ `mouse_filter = MOUSE_FILTER_IGNORE` (по умолчанию)

### **Решение:**
- ✅ Добавлено `mouse_filter = Control.MOUSE_FILTER_STOP` для всех Label

### **Результат:**
- ✅ Tooltip работает корректно
- ✅ Подсказки появляются при наведении
- ✅ Информация доступна игроку
- ✅ Никаких ошибок linter

### **Измененные файлы:**
- ✅ `Scripts/UI/CharacterPreparation.gd` - добавлен `mouse_filter`
- ✅ `SOUL_TOOLTIPS_SYSTEM.md` - обновлена документация
- ✅ `SOUL_TOOLTIPS_FIX.md` - создан новый документ с объяснением

---

## 💡 **ПОЛЕЗНЫЙ СОВЕТ**

**Всегда устанавливайте `mouse_filter` при добавлении tooltip к Label:**

```gdscript
// ❌ НЕ РАБОТАЕТ
label.tooltip_text = "Подсказка"

// ✅ РАБОТАЕТ
label.mouse_filter = Control.MOUSE_FILTER_STOP
label.tooltip_text = "Подсказка"
```

**Запомните:** Label **игнорирует мышь** по умолчанию!

---

**Автор:** Claude AI Assistant  
**Дата:** 2024  
**Версия:** beta 0.6.11

