# Исправление ошибки множественных диалогов

## 🔧 Проблема:
- ❌ **`Attempting to make child window exclusive`** - попытка открыть несколько диалогов одновременно
- ❌ **Конфликт окон** - Godot не позволяет открывать несколько эксклюзивных диалогов
- ❌ **Ошибка при нажатии "Подробнее"** - когда основное окно уже открыто

## ✅ Решение:

### **1. Добавлен метод закрытия диалогов:**
```gdscript
func _close_existing_dialogs():
	"""Закрывает все существующие диалоговые окна"""
	for child in get_children():
		if child is AcceptDialog:
			child.queue_free()
```

### **2. Закрытие перед открытием нового:**
- ✅ **В основном методе** - `_show_simple_passives_dialog()` закрывает предыдущие диалоги
- ✅ **В детальном методе** - `_on_show_details()` закрывает предыдущие диалоги
- ✅ **Задержка перед открытием** - `await get_tree().process_frame` для полного закрытия

### **3. Последовательность действий:**
1. **Закрыть все существующие диалоги** - `_close_existing_dialogs()`
2. **Подождать один кадр** - `await get_tree().process_frame`
3. **Открыть новый диалог** - создание и показ нового окна

## 🎮 Результат:

### **Основное окно пассивных способностей:**
- ✅ **Открывается без конфликтов** - предыдущие диалоги закрываются
- ✅ **Кнопка "Подробнее" работает** - открывает детальное окно
- ✅ **Нет ошибок** - система работает стабильно

### **Детальное окно:**
- ✅ **Открывается корректно** - после закрытия основного окна
- ✅ **Показывает полную информацию** - все способности с описаниями
- ✅ **Закрывается без проблем** - не мешает дальнейшей работе

## 🚀 Готово к использованию!

Теперь система пассивных способностей:
- ✅ **Работает без ошибок** - нет конфликтов диалогов
- ✅ **Удобна в использовании** - можно открывать детальную информацию
- ✅ **Стабильна** - все окна открываются и закрываются корректно

Система пассивных способностей полностью исправлена и готова к использованию! 🎯✨
