# Анимация поглощения душ

## Описание системы

После победы над врагом из его тела вылетают светящиеся частицы душ, которые летят по хаотичным траекториям вверх, а затем притягиваются к главному герою и поглощаются им.

## Визуальные параметры

### Спрайты частиц по редкости

Каждая редкость врага имеет свой уникальный спрайт души:

| Редкость | Спрайт | Цвет |
|----------|--------|------|
| Common | `Soul_fr_common.png` | Белый/серый |
| Uncommon | `Soul_fr_uncommon.png` | Зеленый |
| Rare | `Soul_fr_rare.png` | Синий |
| Epic | `Soul_fr_epic.png` | Фиолетовый |
| Legendary | `Soul_fr_legendary.png` | Оранжевый |
| Mythic | `Soul_fr_mythic.png` | Красный |

Все спрайты находятся в: `Assets/Sprites/Soul/`

### Количество частиц

Количество вылетающих частиц зависит от редкости и уровня врага:

#### Базовое количество по редкости:

- **Common**: 3-5 частиц
- **Uncommon**: 5-8 частиц
- **Rare**: 8-12 частиц
- **Epic**: 12-15 частиц
- **Legendary**: 15-18 частиц
- **Mythic**: 18-25 частиц

#### Бонус за уровень:

+1 частица за каждые 3 уровня врага.

**Формула:** `particle_count = base_count + floor(enemy_level / 3)`

**Примеры:**
- Common враг 1 уровня: 3-5 частиц
- Rare враг 6 уровня: 8-12 + 2 = 10-14 частиц
- Legendary враг 15 уровня: 15-18 + 5 = 20-23 частицы

## Механика анимации

### Фазы движения частиц

Каждая частица проходит через 3 фазы:

#### 1. **Фаза полета вверх (Flying Up)**
- **Длительность**: 0.6 - 1.0 секунды (случайно)
- **Поведение**:
  - Вылетает из позиции врага с случайным смещением (-30 до +30 по X, -20 до +20 по Y)
  - Летит под углом примерно вверх (-108° до -72°)
  - Начальная скорость: 150-300 пикселей/сек
  - Добавляется случайное боковое смещение: -100 до +100 по X
  - Применяется небольшая гравитация (50 пикселей/сек²)
  - Хаотичное колебание (синусоида с амплитудой 30)
  - Сопротивление воздуха (velocity *= 0.98)
  - Случайное вращение: -3 до +3 радиан/сек

#### 2. **Фаза притяжения (Attracted) - Параболическая хаотичная траектория**
- **Длительность**: До достижения игрока (макс. 4 секунды)
- **Поведение**: Частицы летят к игроку по изогнутым параболическим траекториям с хаотичными колебаниями
  
  **Компоненты движения:**
  
  1. **Основная сила притяжения** (параболическая):
     - Базовая сила: 800 пикселей/сек²
     - Усиливается при приближении (множитель до 4x)
     - Всегда направлена к центру игрока
  
  2. **Перпендикулярная сила** (создает изгиб):
     - Сила изгиба: 100-300 (случайная для каждой частицы)
     - Направлена перпендикулярно к вектору притяжения
     - Уменьшается при приближении (fade по расстоянию)
     - Создает параболическую дугу траектории
  
  3. **Волнообразные колебания** (хаотичность):
     - Частота: 2-5 Гц (случайная для каждой частицы)
     - Амплитуда: 20-60 пикселей (случайная)
     - Синусоидальные колебания перпендикулярно направлению
     - Амплитуда уменьшается при приближении
  
  4. **Случайные "толчки"** (непредсказуемость):
     - Небольшие хаотичные отклонения
     - Создают уникальность каждой траектории
  
  - **Максимальная скорость**: 1000 пикселей/сек
  - **Скорость вращения**: Увеличивается при приближении
  
  - **Условия перехода к поглощению** (любое из):
    1. Расстояние < 80 пикселей
    2. Расстояние < 100 пикселей И частица движется от игрока (предотвращает "проскок")
    3. Таймаут 4 секунды (предотвращает бесконечное вращение)

#### 3. **Фаза поглощения (Absorbed)**
- **Длительность**: ~0.2 секунды
- **Поведение**:
  - **Всасывание в центр игрока** (скорость 400 пикселей/сек)
  - Продолжает двигаться к центру спрайта во время исчезновения
  - Плавное исчезновение (fade out, скорость 5x)
  - Ускоренное уменьшение размера (scale down, скорость 4x)
  - Ускоренное вращение (для эффекта спирали)
  - **Удаление при любом из условий**:
    - Полное исчезновение (alpha <= 0)
    - Размер < 10% (scale <= 0.1)
    - Достижение центра игрока (distance < 10 пикселей)

### Эффект появления

При создании каждая частица:
- Начинается с прозрачности 0 и масштаба 0.5
- Плавно появляется за 0.2 секунды до полной непрозрачности
- Плавно увеличивается до масштаба 1.0

### Интервал создания

Частицы создаются не все одновременно, а с небольшой задержкой:
- Задержка между частицами: 0.0 - 0.15 секунды (случайно)
- Создает эффект "взрыва" душ из врага

## Техническая реализация

### Файловая структура

```
Scripts/Battle/
├── SoulParticle.gd          # Скрипт одной частицы души
├── SoulParticleManager.gd   # Менеджер для управления частицами
└── battle_manager.gd        # Интеграция в боевую систему

Assets/Sprites/Soul/
├── Soul_fr_common.png
├── Soul_fr_uncommon.png
├── Soul_fr_rare.png
├── Soul_fr_epic.png
├── Soul_fr_legendary.png
└── Soul_fr_mythic.png
```

### Класс SoulParticle

**Путь:** `Scripts/Battle/SoulParticle.gd`

**Родительский класс:** `Sprite2D`

**Основные методы:**
- `setup(start_pos, player, rarity)` - Инициализация частицы
- `_process(delta)` - Обновление движения и фазы
- `_process_flying_up(delta)` - Обработка фазы полета
- `_process_attracted(delta)` - Обработка фазы притяжения
- `_process_absorbed(delta)` - Обработка фазы поглощения
- `_get_sprite_path(rarity)` - Получение пути к спрайту по редкости

**Ключевые переменные:**
```gdscript
# Базовые параметры движения
var velocity: Vector2              # Текущая скорость
var acceleration: Vector2          # Ускорение
var target_position: Vector2       # Позиция игрока
var current_phase: Phase           # Текущая фаза анимации
var fly_up_duration: float         # Длительность полета вверх
var attraction_strength: float     # Сила притяжения к игроку
var rotation_speed: float          # Скорость вращения

# Параметры параболической траектории (v1.3+)
var curve_direction: Vector2       # Направление изгиба траектории
var curve_strength: float          # Сила изгиба (100-300, случайная)
var wobble_frequency: float        # Частота колебаний (2-5 Гц)
var wobble_amplitude: float        # Амплитуда колебаний (20-60 пикселей)
```

### Класс SoulParticleManager

**Путь:** `Scripts/Battle/SoulParticleManager.gd`

**Родительский класс:** `Node`

**Важно! Структура файла (Godot 4.5):**
```gdscript
extends Node
class_name SoulParticleManager

## Менеджер для создания и управления частицами душ
# НЕ используем preload для избежания проблем парсинга!
```

**Основные методы:**
- `spawn_soul_particles(enemy_pos, player, rarity, level)` - Создает частицы для врага
- `_calculate_particle_count(rarity, level)` - Вычисляет количество частиц
- `_create_particle(position, player, rarity)` - Создает одну частицу (использует динамическую загрузку скрипта)
- `clear_all_particles()` - Удаляет все активные частицы
- `get_active_particle_count()` - Возвращает количество активных частиц

**Создание частицы (Godot 4.5):**
```gdscript
func _create_particle(position: Vector2, player: Node2D, rarity: String) -> void:
    # Создаем Sprite2D и динамически загружаем скрипт
    var particle = Sprite2D.new()
    var script = load("res://Scripts/Battle/SoulParticle.gd")
    particle.set_script(script)
    
    # Добавляем в сцену и настраиваем
    particle_container.add_child(particle)
    particle.setup(start_position, player, rarity)
```

**Константы:**
```gdscript
const PARTICLE_COUNTS = {
	"Common": {"min": 3, "max": 5},
	"Uncommon": {"min": 5, "max": 8},
	"Rare": {"min": 8, "max": 12},
	"Epic": {"min": 12, "max": 15},
	"Legendary": {"min": 15, "max": 18},
	"Mythic": {"min": 18, "max": 25}
}
```

### Интеграция в battle_manager.gd

**Место вызова:** Функция `_on_enemy_died()` (после анимации смерти врага)

**Код интеграции (Godot 4.5 - динамическая загрузка):**
```gdscript
# В начале файла - объявление переменной БЕЗ типизации
var soul_particle_manager  # SoulParticleManager (без типизации для Godot 4.5)

# В _ready() - инициализация через динамическую загрузку
var soul_particle_script = load("res://Scripts/Battle/SoulParticleManager.gd")
soul_particle_manager = Node.new()
soul_particle_manager.set_script(soul_particle_script)
soul_particle_manager.name = "SoulParticleManager"
add_child(soul_particle_manager)

# В _on_enemy_died() - запуск анимации
if soul_particle_manager and enemy_node and player_node:
	var enemy_position = enemy_node.global_position
	var enemy_rarity = enemy_node.rarity
	var enemy_level = enemy_node.level
	soul_particle_manager.spawn_soul_particles(enemy_position, player_node, enemy_rarity, enemy_level)
```

**⚠️ Важно:** Используем `Node.new()` + `set_script()` вместо `SoulParticleManager.new()` для избежания ошибок парсинга в Godot 4.5.

## Примеры использования

### Пример 1: Common враг (уровень 1)

```
Победа над Слизнем (Common, ур. 1)
↓
Создается 3-5 белых/серых частиц
↓
Частицы вылетают из Слизня по разным траекториям
↓
Через ~0.7 секунды начинают притягиваться к игроку
↓
Поглощаются игроком с эффектом исчезновения
```

### Пример 2: Legendary враг (уровень 12)

```
Победа над Драконом (Legendary, ур. 12)
↓
Создается 19-22 оранжевых частицы (15-18 + 4 за уровень)
↓
Частицы вылетают с задержкой 0-0.15 сек между ними
↓
Создается эффект "взрыва" душ
↓
Хаотичный полет вверх с вращением
↓
Массовое притяжение к игроку
↓
Волна поглощения (~3 секунды на всю анимацию)
```

### Пример 3: Epic враг (уровень 6)

```
Победа над Гоблином Воином (Epic, ур. 6)
↓
Создается 14-17 фиолетовых частиц (12-15 + 2 за уровень)
↓
Вылетают из центра врага с небольшим разбросом
↓
Летят вверх по дугам с колебаниями
↓
На середине траектории резко меняют направление к игроку
↓
Ускоряются при приближении
↓
Поглощаются с вращением и fade out
```

## Визуальные эффекты

### Анимационная последовательность

1. **T=0.0с**: Враг умирает, проигрывается анимация смерти
2. **T=0.0с**: Начинают создаваться частицы с интервалом
3. **T=0.0-0.8с**: Фаза полета вверх (для разных частиц)
4. **T=0.8-2.5с**: Фаза притяжения к игроку
5. **T=2.5-3.0с**: Фаза поглощения и исчезновения

### Характеристики движения

**Кривая скорости:**
```
Скорость
    ^
300 |     ____
    |    /    \___
200 |   /         \___
150 |  /              \___
100 | /                   \____
    |/                         \___
  0 +--------------------------------> Время
     0s   0.5s   1.0s   1.5s   2.0s
     [Полет вверх] [Притяжение]
```

**Траектория движения (параболическая с колебаниями):**
```
        ★ (игрок - центр всасывания)
       ╱~│~╲ ╱~│~╲
      ╱ ~│~ ╲ ~│~ ╲
     │  ~│~  X ~│~  │  ← Параболические дуги
     │ ~ │ ~╱ ╲~ │ ~ │  ← с волнообразными колебаниями
     │~  ╱~    ~╲  ~│
     │~ ╱  ════> ╲ ~│  ← Фаза всасывания (спираль к центру)
     │~╱           ╲~│
     💀 (враг)
     
Легенда:
╱╲ - хаотичный полет вверх (фаза 1)
~ - волнообразные колебания (хаотичность)
дуга - параболическая траектория (изгиб)
════> - направленное всасывание в центр (фаза 3)
★ - точка схождения (центр игрока)

Примеры траекторий разных частиц:
Частица A: Дуга влево + малые колебания
Частица B: Дуга вправо + сильные колебания  
Частица C: Слабая дуга + средние колебания
```

## Настройка параметров

### Скорость анимации

Для изменения общей скорости анимации можно настроить:

```gdscript
# В SoulParticle.gd
var fly_up_duration: float = 0.8      # Меньше = быстрее полет
var attraction_strength: float = 800.0  # Больше = быстрее притяжение

# В SoulParticleManager.gd
await get_tree().create_timer(randf_range(0.0, 0.15)).timeout  # Интервал создания
```

### Количество частиц

Для изменения количества частиц:

```gdscript
# В SoulParticleManager.gd - константа PARTICLE_COUNTS
const PARTICLE_COUNTS = {
	"Common": {"min": 3, "max": 5},  # Изменить min/max
	# ...
}

# Бонус за уровень
var level_bonus = int(level / 3)  # Изменить делитель
```

### Визуальные эффекты

```gdscript
# В SoulParticle.gd - _ready()
scale = Vector2(0.5, 0.5)  # Начальный размер
tween.tween_property(self, "scale", Vector2(1.0, 1.0), 0.2)  # Конечный размер

# Скорость вращения
rotation_speed = randf_range(-3.0, 3.0)  # Диапазон вращения
```

## Производительность

### Оптимизация

- Частицы автоматически удаляются после поглощения
- Используется `queue_free()` для корректной очистки памяти
- Контейнер частиц изолирован в отдельном узле
- Нет физических тел (только визуальные Sprite2D)

### Рекомендации

- **Нормальная нагрузка**: До 25 частиц одновременно (Mythic враг)
- **Максимальная нагрузка**: ~50 частиц (несколько врагов подряд)
- **FPS**: Не влияет на производительность при <100 частиц

### Очистка ресурсов

```gdscript
# Принудительная очистка всех частиц
soul_particle_manager.clear_all_particles()

# Проверка активных частиц
var active_count = soul_particle_manager.get_active_particle_count()
print("Активных частиц: ", active_count)
```

## Дебаг и тестирование

### Дебаг-вывод

Менеджер выводит информацию о создании частиц:
```
Создаем 12 частиц души (редкость: Rare, уровень: 5)
```

### Тестирование

**Для проверки анимации:**

1. Запустите игру в тестовом режиме
2. Выберите врага нужной редкости
3. Победите врага
4. Наблюдайте за анимацией частиц

**Чек-лист:**
- [ ] Частицы вылетают из врага
- [ ] Цвет частиц соответствует редкости
- [ ] Количество частиц корректно
- [ ] Хаотичное движение вверх работает
- [ ] Притяжение к игроку активируется
- [ ] Частицы ускоряются при приближении
- [ ] Поглощение с fade out работает
- [ ] Нет ошибок в консоли
- [ ] FPS не падает

### Известные проблемы

**Проблема:** Частицы не создаются
**Решение:** Проверьте наличие спрайтов в `Assets/Sprites/Soul/`

**Проблема:** Частицы застревают в воздухе
**Решение:** Убедитесь, что `player_node` передается корректно

**Проблема:** Неправильное количество частиц
**Решение:** Проверьте `enemy_rarity` и `enemy_level` у врага

**Проблема (ИСПРАВЛЕНО v1.1):** Часть частиц вращается вокруг игрока, не поглощаясь
**Причина:** Быстрые частицы "проскакивали" зону поглощения (30 пикселей)
**Решение:** Добавлены множественные условия поглощения:
- Увеличен радиус до 80 пикселей
- Детекция "проскока" через проверку направления движения
- Таймаут 3 секунды для предотвращения бесконечного вращения

**Проблема (ИСПРАВЛЕНО v1.2):** Частицы исчезают за пределами спрайта игрока
**Причина:** Частицы останавливались и исчезали на месте при входе в зону поглощения
**Решение:** Добавлен эффект "всасывания":
- Частицы продолжают двигаться к центру игрока во время исчезновения (400 пикс/сек)
- Ускоренное вращение создает эффект спирального всасывания
- Удаление происходит при достижении центра (< 10 пикселей) или полном исчезновении

**Улучшение (v1.3):** Прямолинейная траектория притяжения выглядит слишком простой
**Запрос:** Добавить параболические траектории с хаотичностью
**Решение:** Реализована сложная система движения с 4 компонентами:
- Основная сила притяжения (к игроку)
- Перпендикулярная сила изгиба (создает параболу)
- Волнообразные колебания (хаотичность)
- Случайные "толчки" (уникальность каждой траектории)
Каждая частица имеет уникальные параметры, создающие разнообразие траекторий

**Проблема (ИСПРАВЛЕНО v1.3.1):** Ошибка парсинга класса SoulParticleManager
**Ошибка:** `Could not parse global class "SoulParticleManager"` в строке 20 файла `battle_manager.gd`

**Причины:** 
1. В Godot 4 использование тройных кавычек `"""` для комментариев вызывает ошибки парсинга
2. В Godot 4.5 `preload` класса с `class_name` вызывает проблемы из-за порядка загрузки
3. **Главная причина:** Типизация переменной с использованием `class_name` до полной загрузки класса

**Решение для Godot 4.5:** 
1. Заменены все комментарии `"""` на `##` в обоих файлах
2. Используется **динамическая загрузка скрипта** вместо `preload`:
   ```gdscript
   var particle = Sprite2D.new()
   var script = load("res://Scripts/Battle/SoulParticle.gd")
   particle.set_script(script)
   ```
3. **Убрана типизация переменной** в `battle_manager.gd`:
   ```gdscript
   # ❌ Было (вызывает ошибку парсинга):
   var soul_particle_manager: SoulParticleManager
   
   # ✅ Стало (работает в Godot 4.5):
   var soul_particle_manager  # SoulParticleManager (без типизации)
   ```

4. **Создание экземпляра через динамическую загрузку**:
   ```gdscript
   # ❌ Было (ошибка в строке 56):
   soul_particle_manager = SoulParticleManager.new()
   
   # ✅ Стало (динамическая загрузка):
   var soul_particle_script = load("res://Scripts/Battle/SoulParticleManager.gd")
   soul_particle_manager = Node.new()
   soul_particle_manager.set_script(soul_particle_script)
   ```
   Полностью избегаем прямых обращений к `class_name` при парсинге

5. **Исправлен оператор деления (строка 49 в SoulParticleManager.gd)**:
   ```gdscript
   # ❌ Было (GDScript воспринимает // как комментарий):
   var level_bonus = level // 3
   
   # ✅ Стало (целочисленное деление без предупреждения):
   var level_bonus = floori(level / 3.0)
   ```
   В GDScript:
   - `//` = начало комментария (не оператор деления!)
   - `floori()` = округление вниз (floor integer), эквивалент Python `//`
   - Это полностью убирает предупреждение `INTEGER_DIVISION` от Godot 4.5

## Возможные улучшения

### Идеи для расширения

1. **Звуковые эффекты:**
   - Звук вылета души из врага
   - Звук поглощения души игроком
   - Разные звуки для разных редкостей

2. **Дополнительные эффекты:**
   - Trail (след) за частицей
   - Particles (частицы) вокруг души
   - Glow (свечение) у редких душ
   - Вспышка при поглощении

3. **Анимация игрока:**
   - Реакция игрока на поглощение
   - Свечение игрока при получении душ
   - Счетчик поглощенных душ

4. **Вариативность:**
   - Разные траектории для разных типов врагов
   - Особая анимация для боссов
   - Комбо-эффект при быстрой серии побед

5. **Интеграция с геймплеем:**
   - Временный бафф после поглощения душ
   - Визуальная связь с получением опыта
   - Эффект "переполнения" душой

---

**Дата создания:** 9 ноября 2025  
**Версия:** 1.3.1  
**Статус:** Реализовано ✅  
**Линтер:** Ошибок нет ✅  
**Движок:** Godot 4.5

**История версий:**
- v1.0 (09.11.2025): Первоначальная реализация
- v1.1 (09.11.2025): Исправлена проблема с вращающимися частицами
- v1.2 (09.11.2025): Добавлен эффект всасывания в центр спрайта игрока
- v1.3 (09.11.2025): Реализована параболическая хаотичная траектория притяжения
- v1.3.1 (09.11.2025): Исправлен парсинг класса для Godot 4.5 (динамическая загрузка, `//` → `/`)

**Авторы системы:**
- Механика движения частиц: 3-фазная система (полет-притяжение-поглощение)
- Визуальные эффекты: Sprite2D с динамическим tween
- Интеграция: battle_manager.gd
- Умная детекция поглощения: множественные условия для надежного срабатывания
- Эффект всасывания: спиральное движение к центру с исчезновением
- Параболическая траектория: 4-компонентная система сил для реалистичного движения
  - Основное притяжение + перпендикулярный изгиб + волновые колебания + случайные толчки

