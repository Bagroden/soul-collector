# ОКОНЧАТЕЛЬНОЕ ИСПРАВЛЕНИЕ: Автоматическое повышение уровня способностей

## 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА

**Способности АВТОМАТИЧЕСКИ получали уровни** на основе накопленного прогресса, **БЕЗ покупки**!

### Пример с скриншота:

```
Кровосос (ур. 2.0) (Rare) - Прогресс: 100 / 500  ← ❌ Уровень 2, хотя не куплен!
Кровоток (ур. 2.0) (Rare) - Прогресс: 300 / 500  ← ❌ Уровень 2, хотя не куплен!
Регенерация слизи (ур. 2.0) (Rare) - Прогресс: 300 / 500  ← ❌ Уровень 2, хотя не куплен!
```

**Проблема:** UI показывает "ур. 2.0", что означает `current_level = 1` (изучен), но игрок НЕ ПОКУПАЛ эти способности!

---

## 🔍 КОРЕНЬ ПРОБЛЕМЫ

**Файл:** `Scripts/Systems/AbilityLearningSystem.gd` (строки 949-954)

### Проблемный код в `_migrate_old_saves()`:

```gdscript
# Пересчитываем уровень на основе прогресса
if ability_id in ability_learning_config:
    var required_progress = ability_learning_config[ability_id].required_progress
    var calculated_level = _calculate_level(progress.current_progress, required_progress)
    if calculated_level > progress.current_level:
        progress.current_level = calculated_level  // ← АВТОМАТИЧЕСКИ ПОВЫШАЕТ УРОВЕНЬ!
```

### Что происходило:

1. Игрок накапливал прогресс (например, 100 очков)
2. Функция `_calculate_level(100, [100, 500, 1000])` возвращала `1`
3. Код **АВТОМАТИЧЕСКИ** устанавливал `current_level = 1`
4. UI показывал "ур. 2.0" (current_level + 1)
5. Способность считалась изученной, хотя игрок **НЕ ПОКУПАЛ** её!

### Почему это критично:

- ✅ `is_learned = false` (способность НЕ изучена)
- ❌ `current_level = 1` (автоматически установлен!)
- ❌ UI показывает "ур. 2.0"
- ❌ Прогресс накапливается для уровня 2 (до 500)
- ❌ Система думает, что уровень 1 уже изучен!

---

## ✅ ОКОНЧАТЕЛЬНОЕ ИСПРАВЛЕНИЕ

### 1. Удалена автоматическая установка уровня в `_migrate_old_saves()`:

**БЫЛО:**
```gdscript
# Пересчитываем уровень на основе прогресса
var calculated_level = _calculate_level(progress.current_progress, required_progress)
if calculated_level > progress.current_level:
    progress.current_level = calculated_level  // ← УДАЛЕНО!
```

**СТАЛО:**
```gdscript
# Уровень устанавливается ТОЛЬКО при покупке через learn_ability()!
# НЕ автоматически на основе прогресса!
```

### 2. Добавлена функция принудительного исправления `_force_fix_ability_levels()`:

```gdscript
func _force_fix_ability_levels():
    """ПРИНУДИТЕЛЬНО исправляет уровни способностей на основе is_learned флага"""
    for ability_id in learning_progress:
        var progress = learning_progress[ability_id]
        var is_learned = progress.get("is_learned", false)
        var current_level = progress.get("current_level", 0)
        
        # Если способность НЕ изучена, но current_level > 0 - ЭТО ОШИБКА!
        if not is_learned and current_level > 0:
            print("ИСПРАВЛЕНИЕ: '", ability_id, "' сброс current_level с ", current_level, " в 0")
            progress.current_level = 0
            fixed_count += 1
        
        # Ограничиваем прогресс в соответствии с реальным уровнем
        # ... код ограничения ...
    
    if fixed_count > 0:
        _save_progress()  # Сохраняем исправленные данные
```

### 3. Добавлен вызов при каждом запуске игры в `_ready()`:

```gdscript
func _ready():
    _initialize_learning_config()
    _load_progress()
    _force_fix_ability_levels()  # ← НОВОЕ: Принудительное исправление
```

---

## 🎯 Как это работает теперь

### При запуске игры:

1. **Загружаются сохраненные данные**
   ```json
   {
       "bloodsucker": {
           "current_progress": 100,
           "current_level": 1,     // ← ОШИБКА! Автоматически установлен
           "is_learned": false     // ← Способность НЕ изучена!
       }
   }
   ```

2. **Вызывается `_force_fix_ability_levels()`**
   - Проверяет: `is_learned = false`, но `current_level = 1`
   - Обнаруживает ошибку!
   - **ИСПРАВЛЯЕТ:** `current_level = 0`
   - Ограничивает прогресс до 100 (требование для уровня 1)
   - Выводит лог: `"ИСПРАВЛЕНИЕ: 'bloodsucker' сброс current_level с 1 в 0"`

3. **Сохраняет исправленные данные**
   ```json
   {
       "bloodsucker": {
           "current_progress": 100,  // ✅ Ограничен
           "current_level": 0,       // ✅ ИСПРАВЛЕНО!
           "is_learned": false       // ✅ Соответствует!
       }
   }
   ```

4. **UI показывает правильную информацию**
   ```
   Кровосос (ур. 1) (Rare)         // ✅ Уровень 1 (не 2!)
   Прогресс: 100 / 100 очков       // ✅ Правильно!
   [Купить ур. 1 (100 осколков)]  // ✅ Кнопка доступна!
   ```

---

## 📊 Логика уровней

### Правильная логика (ПОСЛЕ исправления):

| Состояние | `is_learned` | `current_level` | `current_progress` | UI показывает |
|-----------|--------------|-----------------|-------------------|---------------|
| Не куплена | `false` | `0` | `0-100` | "ур. 1" (0+1) |
| Куплен ур. 1 | `true` | `1` | `0-500` | "ур. 2" (1+1) |
| Куплен ур. 2 | `true` | `2` | `0-1000` | "ур. 3" (2+1) |
| Куплен ур. 3 | `true` | `3` | `1000` | "" (макс) |

### Неправильная логика (ДО исправления):

| Состояние | `is_learned` | `current_level` | Проблема |
|-----------|--------------|-----------------|----------|
| Не куплена | `false` | `1` ❌ | Автоматически установлен! |
| Не куплена | `false` | `2` ❌ | Автоматически установлен! |

---

## 💡 Ключевые правила

### 1. **Уровень устанавливается ТОЛЬКО при покупке!**

```gdscript
func learn_ability(ability_id: String, level: int) -> bool:
    # ...
    progress.current_level = level  // ← ЕДИНСТВЕННОЕ место!
    progress.is_learned = true
    # ...
```

### 2. **Прогресс НЕ влияет на уровень!**

Прогресс - это только накопление очков для ПОКУПКИ следующего уровня.

### 3. **Миграция НЕ меняет уровень автоматически!**

Миграция только:
- Добавляет `current_level = 0`, если его нет
- Устанавливает `current_level = 1`, если `is_learned = true` (старый формат)
- **НЕ** вычисляет уровень на основе прогресса

### 4. **Принудительное исправление при каждом запуске!**

```gdscript
func _ready():
    _load_progress()
    _force_fix_ability_levels()  // ← Проверяет и исправляет ошибки
```

---

## 🔧 Что изменилось

### Удалено:

```gdscript
// ❌ УДАЛЕНО из _migrate_old_saves():
var calculated_level = _calculate_level(progress.current_progress, required_progress)
if calculated_level > progress.current_level:
    progress.current_level = calculated_level
```

### Добавлено:

```gdscript
// ✅ ДОБАВЛЕНО в _ready():
_force_fix_ability_levels()

// ✅ НОВАЯ ФУНКЦИЯ:
func _force_fix_ability_levels():
    # Проверяет is_learned и current_level
    # Исправляет несоответствия
    # Ограничивает прогресс
    # Сохраняет изменения
```

---

## 🎯 РЕЗУЛЬТАТ

### ДО исправления:

```
Кровосос (ур. 2.0) - Прогресс: 100 / 500  ← ❌ НЕ КУПЛЕН!
[Недоступно (100.0/500)]                  ← ❌ Неправильно!
```

### ПОСЛЕ исправления:

```
Кровосос (ур. 1) - Прогресс: 100 / 100     ← ✅ Правильно!
[Купить ур. 1 (100 осколков)]             ← ✅ Можно купить!
```

### После покупки уровня 1:

```
Кровосос (ур. 2) - Прогресс: 0 / 500      ← ✅ Уровень 1 изучен!
[Недоступно (0/500)]                       ← ✅ Копим на уровень 2!
```

---

## 💡 ИТОГ

### Что исправлено:

✅ Удалена автоматическая установка уровня на основе прогресса  
✅ Добавлена принудительная проверка и исправление при каждом запуске  
✅ Уровень устанавливается ТОЛЬКО при покупке через `learn_ability()`  
✅ Все старые сохранения автоматически исправляются  

### Что изменилось:

✅ Способности НЕ повышают уровень автоматически  
✅ Прогресс НЕ влияет на уровень  
✅ UI показывает правильный уровень (следующий для покупки)  
✅ Исправление применяется при каждом запуске игры  

### Результат:

✅ Система изучения способностей работает правильно  
✅ Уровни соответствуют флагу `is_learned`  
✅ Невозможно получить уровень без покупки  
✅ Старые сохранения автоматически исправляются  

🎯 **ПРОБЛЕМА РЕШЕНА РАЗ И НАВСЕГДА!**

---

## 📁 Измененные файлы

**Только один файл:**

`Scripts/Systems/AbilityLearningSystem.gd`

**Изменения:**

1. Функция `_ready()`: добавлен вызов `_force_fix_ability_levels()`
2. Функция `_migrate_old_saves()`: удалена автоматическая установка уровня
3. Новая функция `_force_fix_ability_levels()`: принудительное исправление

