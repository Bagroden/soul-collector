# Руководство по добавлению анимации "Теневые шипы"

## ✅ Что уже сделано:

1. ✅ Активная способность "Теневые шипы" создана (`Scripts/Abilities/EnemyAbility.gd`)
2. ✅ Обработка способности добавлена (`Scripts/Abilities/EnemyAbilities.gd`)
3. ✅ Обработка анимации добавлена (`Scripts/Battle/battle_manager.gd`)
4. ✅ Логика автокрита из невидимости реализована

---

## 📁 Что нужно сделать:

### Шаг 1: Подготовка спрайтшита

1. **Подготовьте ваш спрайтшит** для анимации "Теневые шипы"
   - Рекомендуемый формат: PNG с прозрачностью
   - Размер кадра: желательно такой же, как у других эффектов (128x58 или подобный)
   - Количество кадров: 6-8 кадров для плавной анимации
   - Расположение: горизонтально в одну строку

2. **Сохраните файл** как:
   ```
   Assets/Sprites/Effects/shadow_spikes_spritesheet.png
   ```
   (Или любое другое имя, которое вам нравится)

---

### Шаг 2: Импорт в Godot

1. **Откройте Godot** и найдите ваш спрайтшит в `FileSystem`
2. **Кликните на файл** в FileSystem
3. В панели `Import` (справа) выберите:
   - **Preset:** `2D Pixel`
   - **Filter:** `Nearest` (для пиксельной графики)
   - **Mipmaps:** `Off`
4. Нажмите **Reimport**

---

### Шаг 3: Добавление анимации в PlayerBody

#### Вариант A: Через Godot Editor (Рекомендуется)

1. **Откройте сцену:**
   ```
   Scenes/Battle/PlayerBody.tscn
   ```

2. **Выберите узел `EffectVisual`** в Scene Tree (внутри PlayerBody)

3. **В Inspector найдите свойство `Sprite Frames`** → Кликните на него → Выберите **"Edit"**

4. **В панели SpriteFrames (снизу):**
   - Нажмите кнопку **"+ Add Animation"**
   - Назовите анимацию: `shadow_spikes_anim`
   - Убедитесь что **"Loop"** выключен (мы хотим одноразовое воспроизведение)

5. **Добавьте кадры:**
   - В левой панели выберите вашу только что созданную анимацию `shadow_spikes_anim`
   - Кликните на иконку **"Add frames from sprite sheet"** (квадратная сетка)
   - Выберите ваш файл `shadow_spikes_spritesheet.png`
   - В открывшемся окне:
     - Установите **Horizontal/Vertical** (количество кадров в спрайтшите)
     - Выделите **ВСЕ кадры** (Ctrl+A или выделите мышкой)
     - Нажмите **"Add X Frame(s)"**

6. **Настройте скорость:**
   - В правом верхнем углу SpriteFrames найдите **"Speed (FPS)"**
   - Установите значение: `10.0` (можно подстроить под вашу анимацию)
   - Для более быстрой анимации: `12-15`
   - Для более медленной: `6-8`

7. **Сохраните сцену:** `Ctrl+S`

#### Вариант B: Ручное редактирование .tscn файла (Для опытных)

Если у вас уже есть опыт с .tscn файлами, вы можете добавить анимацию вручную:

1. **Создайте `AtlasTexture` субресурсы** для каждого кадра вашего спрайтшита
2. **Добавьте новую анимацию** в `SpriteFrames_5r4x2`:

```gdscript
# Пример структуры (добавить в Scenes/Battle/PlayerBody.tscn)

# [sub_resource type="AtlasTexture" id="AtlasTexture_shadow1"]
# atlas = ExtResource("XX_shadow_spikes")  # Ваш спрайтшит
# region = Rect2(0, 0, 128, 58)  # Первый кадр

# [sub_resource type="AtlasTexture" id="AtlasTexture_shadow2"]
# atlas = ExtResource("XX_shadow_spikes")
# region = Rect2(128, 0, 128, 58)  # Второй кадр

# ... и так далее для всех кадров

# Затем добавить в SpriteFrames:
{
"frames": [{
"duration": 1.0,
"texture": SubResource("AtlasTexture_shadow1")
}, {
"duration": 1.0,
"texture": SubResource("AtlasTexture_shadow2")
}, 
# ... остальные кадры
],
"loop": false,
"name": &"shadow_spikes_anim",
"speed": 10.0
}
```

---

### Шаг 4: Проверка

1. **Запустите проект**
2. **Начните бой с Тёмным шатуном (Dark Stalker)** - босс в локации "Лес гоблинов"
3. **Дождитесь использования способности "Теневые шипы"**
4. **Проверьте:**
   - ✅ Анимация проигрывается на игроке
   - ✅ Анимация синхронизирована с уроном
   - ✅ После анимации узел EffectVisual скрывается
   - ✅ При автокрите из невидимости появляется сообщение "💀 АВТОКРИТ ИЗ ТЕНЕЙ!"

---

## 🎨 Рекомендации по визуалу

### Для спрайтшита:
- **Цвета:** Темные фиолетовые/синие тона, черные тени
- **Эффект:** Шипы, вылетающие из тени/темной ауры
- **Стиль:** Темная фантазия, острые формы
- **Референсы:**
  - Кислотный взрыв (acid_blast_anim) - зеленые брызги
  - Духовный удар (spiritual_strike_anim) - магический эффект

### Прогрессия анимации:
1. **Кадр 1-2:** Появление темной ауры/тени вокруг цели
2. **Кадр 3-4:** Формирование шипов из тени
3. **Кадр 5-6:** Шипы пронзают цель
4. **Кадр 7-8:** Рассеивание эффекта

---

## 🔧 Тонкая настройка

### Если анимация слишком быстрая:
В `battle_manager.gd` (строка 2171):
```gdscript
await get_tree().create_timer(0.35).timeout  # Увеличьте значение до 0.5 или 0.6
```

### Если нужен другой звук:
В `battle_manager.gd` (строка 2168):
```gdscript
SoundManager.play_sound("magic_arrow", -5.0)  # Замените на другой звук
```

Доступные звуки:
- `"sword_hit"` - удар мечом
- `"magic_arrow"` - магическая стрела
- `"acid_blast"` - кислота
- `"demon_hit"` - демонический удар

### Если анимация не центрирована:
В `Scenes/Battle/PlayerBody.tscn`, узел `EffectVisual`:
```gdscript
position = Vector2(X, Y)  # Измените позицию
scale = Vector2(2, 2)  # Измените масштаб если нужно
offset = Vector2(X, Y)  # Добавьте offset если нужно
```

---

## 📊 Техническая информация

### Системные требования для анимации:
- **Узел:** `EffectVisual` (AnimatedSprite2D) в `PlayerBody`
- **Имя анимации:** `shadow_spikes_anim` (ВАЖНО: должно совпадать!)
- **Свойства анимации:**
  - `loop = false` - анимация проигрывается один раз
  - `speed = 10.0` - 10 кадров в секунду
  - `z_index = 1` - отображается поверх игрока

### Логика работы в коде:
1. Враг использует способность "Теневые шипы"
2. `EnemyAbility.gd` возвращает результат с флагом `"shadow_spikes": true`
3. `battle_manager.gd` ловит этот флаг (строка 2151)
4. Проигрывается звук (0.35 сек задержка)
5. Запускается анимация `shadow_spikes_anim` на `EffectVisual`
6. Ожидается завершение анимации
7. Наносится урон, показывается damage number
8. Если был автокрит - показывается особое сообщение
9. `EffectVisual` скрывается

---

## ❓ Часто задаваемые вопросы

### Q: Анимация не проигрывается, только урон
**A:** Проверьте:
1. Имя анимации точно `shadow_spikes_anim`
2. Анимация добавлена в `SpriteFrames` узла `EffectVisual`
3. В консоли нет ошибки `"Анимация 'shadow_spikes_anim' не найдена"`

### Q: Анимация проигрывается, но в неправильном месте
**A:** Измените `position` или `offset` узла `EffectVisual` в инспекторе

### Q: Анимация слишком маленькая/большая
**A:** Измените `scale` узла `EffectVisual` (по умолчанию `Vector2(2, 2)`)

### Q: Хочу использовать анимацию и для других способностей
**A:** Просто добавьте проверку в `battle_manager.gd`:
```gdscript
if player_effect_visual.sprite_frames.has_animation("your_anim_name"):
    player_effect_visual.play("your_anim_name")
```

---

## 🎮 Тестовый чеклист

- [ ] Спрайтшит импортирован в проект
- [ ] Анимация `shadow_spikes_anim` создана в SpriteFrames
- [ ] Все кадры добавлены в анимацию
- [ ] Скорость анимации настроена
- [ ] Сцена `PlayerBody.tscn` сохранена
- [ ] Запущен бой с Dark Stalker
- [ ] Анимация проигрывается корректно
- [ ] Урон наносится синхронно с анимацией
- [ ] Сообщение "АВТОКРИТ ИЗ ТЕНЕЙ!" появляется при невидимости

---

**Автор:** Claude AI Assistant  
**Дата:** 2024  
**Версия:** 1.0

