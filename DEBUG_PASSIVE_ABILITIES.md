# Отладка пассивных способностей игрока

## 🔍 Добавлена отладочная информация:

### **1. В `battle_manager.gd` - метод `_sync_player_data()`:**
- ✅ **Показывает max_hp после синхронизации** - `print("Макс. HP после синхронизации: ", player_node.max_hp)`
- ✅ **Показывает max_hp после применения пассивок** - `print("Макс. HP после применения пассивных способностей: ", player_node.max_hp)`
- ✅ **Порядок операций:** синхронизация → пассивки → установка ресурсов

### **2. В `battle_manager.gd` - метод `_apply_player_passive_abilities()`:**
- ✅ **Показывает активные способности** - `print("Активные способности: ", active_passives)`
- ✅ **Показывает каждую способность** - `print("Применяем пассивную способность: ", ability.name, " (ID: ", ability_id, ")")`
- ✅ **Показывает значение способности** - `print("Значение способности: ", ability.value)`

### **3. В `body.gd` - метод `_apply_passive_effects()` для `player_vitality`:**
- ✅ **Детальная отладка** - показывает персонажа, значение способности
- ✅ **Показывает изменения HP** - до и после применения
- ✅ **Показывает изменения max_hp** - до и после применения

## 🎯 Как проверить:

### **1. Запустите игру и войдите в бой:**
- Смотрите в консоль Godot на сообщения отладки
- Должны появиться сообщения:
  ```
  === ПРИМЕНЕНИЕ АКТИВНЫХ ПАССИВНЫХ СПОСОБНОСТЕЙ ИГРОКА ===
  Активных способностей: 1
  Активные способности: ["player_vitality"]
  Применяем пассивную способность: Живучесть игрока (ID: player_vitality)
  Значение способности: 50.0
  === ПРИМЕНЕНИЕ PLAYER_VITALITY ===
  Персонаж: Player
  Значение способности: 50.0
  Player получает бонус живучести: +50 HP
  Макс. HP ДО: 100, ПОСЛЕ: 150
  Текущее HP ДО: 100, ПОСЛЕ: 150
  === КОНЕЦ PLAYER_VITALITY ===
  ```

### **2. Проверьте значения:**
- **Макс. HP после синхронизации** - должно быть базовое значение
- **Макс. HP после применения пассивок** - должно быть +50 от базового
- **Текущее HP** - должно быть равно новому max_hp

## 🔧 Возможные проблемы:

### **1. Если пассивка не применяется:**
- Проверьте, что `player_vitality` в списке активных способностей
- Проверьте, что `ability.value` равно 50.0
- Проверьте, что способность найдена в `player_data.get_passive_ability_info()`

### **2. Если HP не увеличивается:**
- Проверьте, что `max_hp` увеличивается в `_apply_passive_effects`
- Проверьте, что `max_hp` не перезаписывается после применения пассивок
- Проверьте, что `hp` устанавливается в `max_hp` в конце

### **3. Если способность не активна:**
- Проверьте, что `player_vitality` активирована в экране подготовки персонажа
- Проверьте, что способность сохранена в `player_data.active_passives`

## 🎮 Готово к тестированию!

Теперь можно:
- ✅ **Отследить весь процесс** применения пассивных способностей
- ✅ **Найти проблему** если что-то не работает
- ✅ **Проверить значения** на каждом этапе
- ✅ **Убедиться** что пассивки применяются правильно

Система отладки готова! 🔍✨
