# Анимация молнии для пассивки "Шаман бурь"

## Дата
28 октября 2025

## Описание

Добавлена визуальная анимация удара молнии для пассивной способности "Шаман бурь". При срабатывании способности над целью появляется анимированный эффект молнии.

## Пассивка "Шаман бурь"

### Описание способности:
- **Название:** Шаман бурь
- **Редкость:** Legendary
- **Эффект:** При нанесении любого магического урона есть шанс X% вызвать удар молнии
- **Урон молнии:** Интеллект + Мудрость
- **Шансы:** 5% / 10% / 15% (зависит от уровня способности)

## Реализация

### 1. Создан скрипт визуального эффекта

**Файл:** `Scripts/Battle/LightningEffect.gd`

#### Основные параметры:
```gdscript
@export var animation_duration: float = 0.5  # Длительность анимации
@export var fade_duration: float = 0.2      # Длительность исчезновения
@export var flash_count: int = 3            # Количество вспышек
@export var scale_start: float = 1.5        # Начальный размер
@export var scale_end: float = 1.0          # Конечный размер
```

#### Фазы анимации:

**Фаза 1: Появление (0.1 сек)**
- Быстрое появление с увеличением прозрачности от 0 до 1
- Уменьшение размера от 1.5x до 1.0x с эффектом "отскока"
- Светло-голубой оттенок (Color(1.5, 1.5, 1.8))

**Фаза 2: Мерцание (3 вспышки)**
- Каждая вспышка длится 0.05 сек
- Яркая вспышка: Color(2.0, 2.0, 2.5, 1.0)
- Затухание: Color(1.0, 1.0, 1.5, 0.8)

**Фаза 3: Исчезновение (0.2 сек)**
- Плавное исчезновение (прозрачность 1.0 → 0.0)
- Увеличение размера (1.0x → 1.8x)
- Удаление эффекта из сцены

#### Позиционирование:
```gdscript
func setup(target_position: Vector2):
    # Размещаем молнию на 80 пикселей выше цели
    global_position = target_position + Vector2(0, -80)
    
    # Добавляем случайное смещение ±10 пикселей
    var random_offset = Vector2(randf_range(-10, 10), randf_range(-10, 10))
    global_position += random_offset
```

### 2. Модифицирована пассивка "Шаман бурь"

**Файл:** `Scripts/PassiveAbilities/Legendary/StormShaman.gd`

#### Добавлена функция создания эффекта:
```gdscript
func _spawn_lightning_effect(target: Node):
    """Создает визуальный эффект молнии над целью"""
    # Создаем Sprite2D с прикрепленным скриптом LightningEffect
    var lightning_effect = Sprite2D.new()
    lightning_effect.set_script(load("res://Scripts/Battle/LightningEffect.gd"))
    
    # Загружаем текстуру молнии
    var lightning_texture = load("res://Assets/Sprites/DemonsEnemy/CurseDemon/VFX_CurseDemon_CurseLightning.png")
    lightning_effect.texture = lightning_texture
    
    # Добавляем в сцену боя
    var battle_manager = target.get_node_or_null("/root/BattleScene")
    battle_manager.add_child(lightning_effect)
    
    # Настраиваем позицию
    lightning_effect.setup(target.global_position)
```

#### Интеграция в execute_ability:
```gdscript
if randf() * 100.0 < lightning_chance:
    var lightning_damage = _owner.intelligence + _owner.wisdom
    
    # ✨ Создаем визуальный эффект молнии
    _spawn_lightning_effect(_target)
    
    # Наносим урон молнии
    _target.take_damage(lightning_damage, "magic")
    
    return {
        "success": true,
        "lightning_damage": lightning_damage,
        "message": _owner.display_name + " вызывает удар молнии! Урон: " + str(lightning_damage)
    }
```

## Используемые ресурсы

### Текстура молнии:
**Путь:** `Assets/Sprites/DemonsEnemy/CurseDemon/VFX_CurseDemon_CurseLightning.png`

**Характеристики:**
- Пиксельная графика (используется TEXTURE_FILTER_NEAREST)
- Эффект проклятой молнии из набора спрайтов демона проклятий
- Подходит для эффекта удара молнии

## Визуальные эффекты

### Цветовая схема:
- **Основной цвет:** Светло-голубой/белый (1.5, 1.5, 1.8)
- **Вспышки:** Яркий бело-голубой (2.0, 2.0, 2.5)
- **Затухание:** Голубоватый (1.0, 1.0, 1.5)

### Анимация:
```
Время:    0s      0.1s     0.25s    0.4s     0.55s    0.7s     0.9s
          |        |        |        |        |        |        |
Размер:   [1.5x] → [1.0x] → [1.0x] → [1.0x] → [1.0x] → [1.2x] → [1.8x]
          ↓        ↓        ↓        ↓        ↓        ↓        ↓
Альфа:    [0.0]  → [1.0]  → [1.0]  → [0.8]  → [1.0]  → [0.5]  → [0.0]
          ↓        ↓        ↓        ↓        ↓        ↓        ↓
Эффект:   Появл.   Готова   Вспышка  Затух.   Вспышка  Затух.   Исчезн.
```

### Мерцание:
```
Вспышка 1: 💥 Яркая (0.05s) → Затухание (0.05s)
Вспышка 2: 💥 Яркая (0.05s) → Затухание (0.05s)
Вспышка 3: 💥 Яркая (0.05s) → Затухание (0.05s)
```

## Технические детали

### Tweens (плавные переходы):
Используется система Tween Godot для всех анимаций:
- `Tween.TRANS_BACK` с `EASE_OUT` для появления с "отскоком"
- Parallel tweens для одновременной анимации размера и прозрачности
- Sequential tweens для мерцания

### Автоматическая очистка:
```gdscript
# После завершения всех фаз анимации
await fade_tween.finished
queue_free()  # Автоматическое удаление из сцены
```

### Случайное позиционирование:
Каждый эффект имеет небольшое случайное смещение (±10 пикселей) для визуального разнообразия.

## Альтернативная реализация

В файле `LightningEffect.gd` есть закомментированная версия для `AnimatedSprite2D`:

```gdscript
# Если создать SpriteFrames ресурс с несколькими кадрами анимации
extends AnimatedSprite2D
class_name LightningEffect

func _ready():
    if sprite_frames and sprite_frames.has_animation("lightning"):
        play("lightning")
    
    # Анимация появления/исчезновения
    var appear_tween = create_tween()
    appear_tween.tween_property(self, "modulate:a", 1.0, 0.1)
    
    await animation_finished
    
    var fade_tween = create_tween()
    fade_tween.tween_property(self, "modulate:a", 0.0, 0.2)
    await fade_tween.finished
    
    queue_free()
```

**Преимущества AnimatedSprite2D:**
- Покадровая анимация для более сложных эффектов
- Возможность использовать несколько текстур
- Контроль скорости воспроизведения через `speed_scale`

## Примеры использования

### Сценарий 1: Игрок с пассивкой "Шаман бурь"
```
1. Игрок использует магическую атаку (Магические стрелы)
2. Проверка шанса: randf() < 10% (для уровня 2)
3. ✅ Шанс сработал!
4. Создается эффект молнии над врагом
5. Молния появляется, мерцает 3 раза, исчезает
6. Враг получает урон (Интеллект + Мудрость)
```

### Сценарий 2: Враг (Гоблин-колдун) с пассивкой
```
1. Гоблин-колдун использует Dark Blast
2. Проверка шанса: randf() < 5% (для уровня 1)
3. ✅ Шанс сработал!
4. Молния ударяет в игрока
5. Игрок видит эффект молнии над собой
6. Игрок получает дополнительный урон
```

## Производительность

### Оптимизация:
- ✅ Эффект существует только ~0.9 секунды
- ✅ Автоматическое удаление через `queue_free()`
- ✅ Использование одной текстуры
- ✅ Минимальное количество узлов (один Sprite2D)
- ✅ Tweens оптимизированы движком Godot

### Память:
- Текстура загружается один раз и кэшируется
- Скрипт загружается один раз
- Каждый эффект создает только один узел

## Возможные улучшения

### 1. Звуковой эффект
Можно добавить звук удара молнии:
```gdscript
func _spawn_lightning_effect(target: Node):
    # ... создание визуального эффекта ...
    
    # Добавляем звук
    var audio = AudioStreamPlayer2D.new()
    audio.stream = load("res://Audio/SFX/lightning_strike.ogg")
    battle_manager.add_child(audio)
    audio.global_position = target.global_position
    audio.play()
    audio.finished.connect(audio.queue_free)
```

### 2. Экранный эффект (screen shake)
При ударе молнии можно добавить тряску камеры:
```gdscript
# В battle_manager.gd
func screen_shake(intensity: float, duration: float):
    # ... код тряски камеры ...
```

### 3. Частицы
Добавить частицы искр вокруг молнии:
```gdscript
var particles = GPUParticles2D.new()
# ... настройка частиц ...
battle_manager.add_child(particles)
```

### 4. Световой эффект
Добавить временную подсветку сцены:
```gdscript
var light = PointLight2D.new()
light.energy = 2.0
light.color = Color(0.8, 0.9, 1.0)
# ... анимация яркости ...
```

## Тестирование

### Как протестировать:
1. Изучить пассивку "Шаман бурь" (или добавить ее врагу)
2. Использовать магические атаки
3. При срабатывании (5-15% шанс) над целью появится молния
4. Проверить, что эффект корректно позиционируется
5. Проверить, что эффект исчезает автоматически

### Ожидаемое поведение:
✅ Молния появляется над целью
✅ Анимация плавная и красивая
✅ Мерцание привлекает внимание
✅ Эффект исчезает без следов
✅ Не вызывает утечек памяти

## Совместимость

### С существующими системами:
✅ Работает с пассивными способностями игрока
✅ Работает с пассивными способностями врагов
✅ Не конфликтует с другими визуальными эффектами
✅ Совместимо с системой боевого лога

### Поддержка редактора:
✅ @export параметры доступны в редакторе
✅ Можно настроить длительность и интенсивность
✅ Легко менять текстуру молнии

## Результат
✅ Добавлена визуальная анимация молнии для пассивки "Шаман бурь"
✅ Эффект красиво появляется, мерцает и исчезает
✅ Позиционируется над целью с случайным смещением
✅ Автоматически удаляется после завершения
✅ Оптимизирован для производительности
✅ Легко настраивается через @export параметры

