# Универсальная система эффектов способностей врагов

## 📋 Обзор

Универсальная система позволяет проигрывать анимационные эффекты способностей врагов на цели атаки (игроке) без необходимости добавлять эти анимации в SpriteFrames каждого персонажа. Все анимации эффектов хранятся в одном универсальном ресурсе `AbilityEffectAnimations.tres`, а система автоматически создает временные узлы для проигрывания эффектов.

## 🏗️ Архитектура

### Компоненты системы:

1. **AbilityEffectManager** (`Scripts/Battle/AbilityEffectManager.gd`)
   - Универсальный менеджер для проигрывания эффектов способностей
   - Автоматически создает временные узлы `AnimatedSprite2D` для эффектов
   - Автоматически удаляет узлы после завершения анимации
   - Получает ссылку на `AbilityAnimationManager` при инициализации

2. **AbilityEffectAnimations.tres** (`Data/AbilityEffectAnimations.tres`)
   - Универсальный `SpriteFrames` ресурс с анимациями эффектов способностей
   - Создается автоматически при первом запуске игры
   - Содержит все анимации эффектов в одном месте

3. **AbilityAnimations.tres** (`Data/AbilityAnimations.tres`)
   - Ресурс с маппингом ID способностей на имена анимаций
   - Связывает `ability_id` (например, `"rat_bite"`) с именем анимации (например, `"rat_bite_anim"`)

4. **AbilityAnimationManager** (`Scripts/Battle/AbilityAnimationManager.gd`)
   - Менеджер для получения имени анимации по ID способности
   - Загружает маппинг из `AbilityAnimations.tres`
   - Передается в `AbilityEffectManager` при инициализации

---

## 🚀 Как система работает

### Инициализация (в `battle_manager.gd`):

```gdscript
# Создаем менеджер анимаций
ability_animation_manager = AbilityAnimationManager.new()
add_child(ability_animation_manager)

# Создаем менеджер эффектов
ability_effect_manager = AbilityEffectManager.new()
add_child(ability_effect_manager)

# Передаем ссылку на менеджер анимаций
ability_effect_manager.set_animation_manager(ability_animation_manager)
```

### Проигрывание эффекта на цели:

```gdscript
# В battle_manager.gd при обработке способности врага
if ability_effect_manager:
    ability_effect_manager.play_ability_effect_on_target(
        player_node,        # Node2D - цель (игрок)
        "rat_bite",         # String - ID способности
        Vector2.ZERO,       # Vector2 - смещение позиции (опционально)
        Vector2(2, 2),      # Vector2 - масштаб эффекта (опционально)
        100                 # int - z-index эффекта (опционально)
    )
```

### Что происходит внутри:

1. `AbilityEffectManager` получает `ability_id` (например, `"rat_bite"`)
2. Через `AbilityAnimationManager` получает имя анимации (например, `"rat_bite_anim"`)
3. Проверяет наличие анимации в `AbilityEffectAnimations.tres`
4. Создает временный узел `AnimatedSprite2D` с эффектом
5. Позиционирует узел на цели (игроке)
6. Проигрывает анимацию
7. Автоматически удаляет узел после завершения анимации

---

## 📝 Добавление нового эффекта анимации

### Шаг 1: Добавление анимации в PlayerBody

1. Откройте `Scenes/Battle/PlayerBody.tscn` в Godot
2. Выберите узел `Visual` (AnimatedSprite2D)
3. В инспекторе откройте `Sprite Frames`
4. Добавьте новую анимацию с именем `{ability_id}_anim`
   - Например, для способности `rat_bite` → анимация `rat_bite_anim`
   - Для способности `acid_blast` → анимация `acid_blast_anim`
5. Назначьте спрайты эффекта (текстуры для анимации)
6. Настройте параметры анимации:
   - `Speed` - скорость проигрывания (обычно 8.0)
   - `Loop` - должен быть `false` (анимация не зациклена)

### Шаг 2: Экспорт анимации в универсальный ресурс

#### Способ 1: Автоматический экспорт (рекомендуется)

1. Добавьте имя анимации в список `ABILITY_ANIMATIONS` в `Scripts/Tools/ExportAbilityAnimations.gd`:

```gdscript
const ABILITY_ANIMATIONS = [
    "acid_blast_anim",
    "bat_swoop_anim",
    # ... другие анимации ...
    "rat_bite_anim",        # ← Добавьте вашу анимацию
    "your_new_anim"         # ← Новая анимация
]
```

2. Запустите скрипт экспорта через меню:
   - **Редактор** → **Выполнить скрипт** → выберите `Scripts/Tools/ExportAbilityAnimationsEditor.gd`
   - Или через консоль:
   ```gdscript
   var exporter = preload("res://Scripts/Tools/ExportAbilityAnimations.gd").new()
   exporter.export_ability_animations()
   ```

3. Проверьте, что файл `Data/AbilityEffectAnimations.tres` обновлен

#### Способ 2: Ручной экспорт (если автоматический не работает)

1. Откройте `Data/AbilityEffectAnimations.tres` в Godot
2. Добавьте анимацию вручную через интерфейс Godot
3. Или скопируйте анимацию из `PlayerBody.tscn` → `Visual` → `Sprite Frames`

### Шаг 3: Добавление маппинга способности

Откройте `Data/AbilityAnimations.tres` и добавьте запись:

```tres
ability_animations = {
    "rat_bite": "rat_bite_anim",
    "double_strike": "double_strike_anim",
    "your_ability_id": "your_animation_name",  # ← Добавьте вашу запись
    ...
}
```

**Важно:** 
- `ability_id` должен совпадать с ID способности в `EnemyAbilities.gd`
- `animation_name` должно совпадать с именем анимации в `AbilityEffectAnimations.tres`

### Шаг 4: Использование в коде

#### Для обычных способностей (одноударные):

В `battle_manager.gd`, в функции `_enemy_action()`, найдите блок обработки способности:

```gdscript
elif result.get("your_ability_id", false):
    # Задержка для синхронизации с анимацией атаки (момент удара)
    await get_tree().create_timer(0.35).timeout
    
    # Проигрываем эффект способности на цели используя универсальный менеджер
    if ability_effect_manager:
        ability_effect_manager.play_ability_effect_on_target(
            player_node, 
            "your_ability_id", 
            Vector2.ZERO, 
            Vector2(2, 2), 
            100
        )
    
    # Наносим урон
    var player_old_hp = player_node.hp
    player_node.take_damage(damage, damage_type)
    var actual_damage = player_old_hp - player_node.hp
    DamageNumberManager.show_damage_on_character(player_node, actual_damage, is_crit, false, false, damage_type)
```

#### Для способностей с проверкой попадания:

Для способностей, которые проходят через блок проверки попадания, эффект проигрывается автоматически после успешной проверки:

```gdscript
# В блоке проверки попадания (строка ~1184)
if not _calculate_hit_chance(...):
    # Промах - эффект не проигрывается
    return

# Попадание успешно - проигрываем эффект способности на цели
var ability_id_for_effect = enemy_ability.id if enemy_ability else ""
if ability_id_for_effect != "" and ability_effect_manager:
    await get_tree().create_timer(0.35).timeout
    ability_effect_manager.play_ability_effect_on_target(
        player_node, 
        ability_id_for_effect, 
        Vector2.ZERO, 
        Vector2(2, 2), 
        100
    )
    result["effect_played"] = true  # Флаг, чтобы не проигрывать дважды
```

#### Для многоударных способностей:

Для способностей с несколькими ударами (например, `double_strike`, `crushing_hammer`):

```gdscript
# Первый удар
if _calculate_hit_chance(enemy_node, player_node, "double_strike_1", "Двойной удар (1-й удар)"):
    await get_tree().create_timer(0.35).timeout
    
    # Звук первого удара
    if SoundManager:
        SoundManager.play_sound("sword_attack1", -5.0)
    
    # Проигрываем эффект способности на цели
    if ability_effect_manager:
        ability_effect_manager.play_ability_effect_on_target(
            player_node, 
            "double_strike", 
            Vector2.ZERO, 
            Vector2(2, 2), 
            100
        )
    
    # Наносим урон и показываем цифру
    var player_old_hp = player_node.hp
    player_node.take_damage(first_hit_damage, damage_type)
    var actual_first_damage = player_old_hp - player_node.hp
    DamageNumberManager.show_damage_on_character(player_node, actual_first_damage, first_hit_crit, false, false, damage_type)

await get_tree().create_timer(0.3).timeout  # Задержка между ударами

# Второй удар (аналогично)
if _calculate_hit_chance(enemy_node, player_node, "double_strike_2", "Двойной удар (2-й удар)"):
    await get_tree().create_timer(0.35).timeout
    # ... аналогично первому удару
```

---

## ✅ Преимущества новой системы

1. **Не нужно добавлять анимации в каждый SpriteFrames** - все анимации эффектов в одном месте (`AbilityEffectAnimations.tres`)
2. **Работает на любых персонажах** - эффекты создаются как временные узлы поверх цели
3. **Автоматическая очистка** - временные узлы эффектов удаляются автоматически после завершения анимации
4. **Гибкость** - можно настроить позицию, масштаб, z-index для каждого эффекта
5. **Масштабируемость** - легко добавлять новые анимации способностей
6. **Централизованное управление** - все эффекты управляются через один менеджер

---

## ⚠️ Важные замечания

1. **ID способности должен совпадать** - `ability_id` в `EnemyAbilities.gd` должен совпадать с ключом в `AbilityAnimations.tres`
2. **Имя анимации должно совпадать** - имя анимации в `AbilityEffectAnimations.tres` должно совпадать со значением в `AbilityAnimations.tres`
3. **Эффекты создаются как временные узлы** - они автоматически удаляются после завершения анимации
4. **Эффекты добавляются в GameWorld** - чтобы быть поверх всех элементов боя
5. **Задержка синхронизации** - обычно используется `0.35` секунды для синхронизации с моментом удара в анимации атаки

---

## 🐛 Отладка

### Эффект не проигрывается

1. **Проверьте логи в консоли:**
   ```
   DEBUG AbilityEffectManager: play_ability_effect_on_target вызван для ability_id='rat_bite'
   DEBUG AbilityEffectManager: ability_effect_spriteframes загружен, анимаций: 14
   DEBUG AbilityEffectManager: animation_name для 'rat_bite' = 'rat_bite_anim'
   ```

2. **Проверьте инициализацию:**
   - Убедитесь, что `ability_effect_manager` инициализирован в `battle_manager._ready()`
   - Убедитесь, что `set_animation_manager()` вызван после создания менеджера

3. **Проверьте маппинг:**
   - Откройте `Data/AbilityAnimations.tres`
   - Убедитесь, что `ability_id` есть в словаре `ability_animations`
   - Убедитесь, что значение совпадает с именем анимации в `AbilityEffectAnimations.tres`

4. **Проверьте наличие анимации:**
   - Откройте `Data/AbilityEffectAnimations.tres` в Godot
   - Убедитесь, что анимация существует в списке анимаций

### Эффект проигрывается не в том месте

- Используйте параметр `position_offset` для корректировки позиции:
  ```gdscript
  ability_effect_manager.play_ability_effect_on_target(
      player_node, 
      "rat_bite", 
      Vector2(0, -20),  # Смещение вверх на 20 пикселей
      Vector2(2, 2), 
      100
  )
  ```

- Используйте параметр `scale` для изменения размера эффекта:
  ```gdscript
  ability_effect_manager.play_ability_effect_on_target(
      player_node, 
      "tombstone", 
      Vector2.ZERO, 
      Vector2(3, 3),  # Увеличенный масштаб для больших эффектов
      100
  )
  ```

- Используйте параметр `z_index` для изменения слоя отрисовки:
  ```gdscript
  ability_effect_manager.play_ability_effect_on_target(
      player_node, 
      "rat_bite", 
      Vector2.ZERO, 
      Vector2(2, 2), 
      150  # Поверх других эффектов
  )
  ```

### Эффект проигрывается дважды

- Убедитесь, что установлен флаг `result["effect_played"] = true` после проигрывания эффекта
- Проверьте, что эффект не проигрывается в блоке `elif not special_ability_handled:` если уже проигран ранее

---

## 📋 Чеклист добавления нового эффекта

- [ ] Анимация эффекта добавлена в `PlayerBody.tscn` → `Visual` → `Sprite Frames`
- [ ] Имя анимации соответствует формату `{ability_id}_anim`
- [ ] Анимация не зациклена (`Loop = false`)
- [ ] Имя анимации добавлено в `ABILITY_ANIMATIONS` в `ExportAbilityAnimations.gd`
- [ ] Анимация экспортирована в `AbilityEffectAnimations.tres`
- [ ] Запись добавлена в `Data/AbilityAnimations.tres` (`"ability_id": "animation_name"`)
- [ ] ID способности в `EnemyAbilities.gd` совпадает с ключом в `AbilityAnimations.tres`
- [ ] Логика эффекта добавлена в `battle_manager.gd` в блок обработки способности
- [ ] Задержка синхронизации добавлена (`await get_tree().create_timer(0.35).timeout`)
- [ ] Протестировано в игре

---

## 📚 Примеры использования

### Пример 1: Крысиный укус (rat_bite)

```gdscript
# В EnemyAbilities.gd
rat_ability.id = "rat_bite"  # ID способности

# В AbilityAnimations.tres
ability_animations = {
    "rat_bite": "rat_bite_anim"  # Маппинг на имя анимации
}

# В battle_manager.gd
elif result.get("rat_bite", false):
    await get_tree().create_timer(0.35).timeout
    
    if ability_effect_manager:
        ability_effect_manager.play_ability_effect_on_target(
            player_node, 
            "rat_bite", 
            Vector2.ZERO, 
            Vector2(2, 2), 
            100
        )
    
    # Наносим урон...
```

### Пример 2: Двойной удар (double_strike)

```gdscript
# Первый удар
if _calculate_hit_chance(...):
    await get_tree().create_timer(0.35).timeout
    
    if SoundManager:
        SoundManager.play_sound("sword_attack1", -5.0)
    
    if ability_effect_manager:
        ability_effect_manager.play_ability_effect_on_target(
            player_node, 
            "double_strike", 
            Vector2.ZERO, 
            Vector2(2, 2), 
            100
        )
    
    # Наносим урон...

await get_tree().create_timer(0.3).timeout

# Второй удар (аналогично)
```

---

## 🔄 Миграция со старой системы

### Старый способ (требует EffectVisual на каждом персонаже):

```gdscript
var player_effect_visual = player_node.get_node_or_null("EffectVisual")
if player_effect_visual:
    player_effect_visual.sprite_frames = player_visual.sprite_frames
    player_effect_visual.visible = true
    player_effect_visual.play("double_strike_anim")
    await player_effect_visual.animation_finished
    player_effect_visual.visible = false
```

### Новый способ (универсальный):

```gdscript
if ability_effect_manager:
    ability_effect_manager.play_ability_effect_on_target(
        player_node, 
        "double_strike"
    )
```

---

## 💡 Советы

1. **Именование:** Используйте единообразные имена: `{ability_id}_anim`
2. **Синхронизация:** Задержка 0.35 секунды обычно работает хорошо, но может потребоваться корректировка в зависимости от длительности анимации атаки
3. **Многоударные способности:** Проигрывайте эффект для каждого удара отдельно
4. **Тестирование:** Всегда тестируйте эффект в игре после добавления
5. **Оптимизация:** Эффекты должны быть короткими (1-2 секунды), чтобы не замедлять бой
6. **Масштаб:** Используйте больший масштаб (например, `Vector2(3, 3)`) для эффектов, которые должны быть более заметными

---

**Последнее обновление:** 2024  
**Версия:** 2.0 (Универсальная система с AbilityEffectManager)
