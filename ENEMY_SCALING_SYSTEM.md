# Система масштабирования характеристик врагов

## 📋 Описание

Система случайного распределения характеристик для врагов при повышении уровня. Каждый уровень враг получает **3 случайных очка характеристик**, которые распределяются между всеми шестью характеристиками.

## 🎯 Формула масштабирования

### **Очки характеристик за уровень:**
```
Очки за уровень = Уровень × 3
```

### **Примеры:**
- **Уровень 1**: 3 очка
- **Уровень 5**: 15 очков  
- **Уровень 10**: 30 очков
- **Уровень 20**: 60 очков

## 🔧 Техническая реализация

### **Функция `_add_random_level_stats()` в `body.gd`:**
```gdscript
func _add_random_level_stats():
    """Добавляет случайные характеристики за каждый уровень врага"""
    # Каждый уровень дает 3 случайных очка характеристик
    var total_points = level * 3
    
    # Список характеристик для случайного распределения
    var stats = ["strength", "agility", "vitality", "endurance", "intelligence", "wisdom"]
    
    # Распределяем очки случайно
    for i in range(total_points):
        var random_stat = stats[randi() % stats.size()]
        # Увеличиваем выбранную характеристику на 1
```

### **Интеграция с `set_enemy_level()`:**
```gdscript
func set_enemy_level(new_level: int) -> void:
    level = new_level
    
    # Добавляем случайные характеристики за каждый уровень
    _add_random_level_stats()
    
    calculate_stat_bonuses()
    apply_stat_bonuses()
    # Восстанавливаем полные ресурсы
    hp = max_hp
    mp = max_mp
    stamina = max_stamina
```

## 📊 Влияние на боевые характеристики

### **HP (Здоровье):**
```
Итоговое HP = (Базовое HP + Живучесть × 10 + Уровень × 10) × Множитель редкости
```

### **MP (Мана):**
```
Итоговое MP = (Базовое MP + Мудрость × 10 + Уровень × 10) × Множитель редкости
```

### **Атака:**
```
Итоговая атака = Базовая атака + Уровень × 2 + Бонус от силы
```

### **Выносливость:**
```
Итоговая выносливость = Базовая выносливость + Выносливость × 5 + Уровень × 10
```

## 🎲 Примеры распределения

### **Крыса на 10 уровне:**
- **Исходные характеристики**: Сила=5, Ловкость=12, Живучесть=8, Выносливость=6, Интеллект=3, Мудрость=2
- **Случайные очки**: 30 очков (10 × 3)
- **Возможное распределение**: +5 Сила, +7 Ловкость, +6 Живучесть, +5 Выносливость, +4 Интеллект, +3 Мудрость
- **Итоговые характеристики**: Сила=10, Ловкость=19, Живучесть=14, Выносливость=11, Интеллект=7, Мудрость=5

### **Расчет итоговых боевых характеристик:**
- **HP**: (20 + 14×10 + 10×10) × 1.0 = 260 HP
- **MP**: (10 + 5×10 + 10×10) × 1.0 = 160 MP
- **Атака**: 8 + 10×2 + 13 = 41 атака

## 🔄 Множители редкости

Система случайных характеристик работает **до** применения множителей редкости:

- **Common**: ×1.0
- **Uncommon**: ×1.2  
- **Rare**: ×1.5
- **Epic**: ×2.0
- **Legendary**: ×3.0
- **Boss**: ×4.0
- **Mythic**: ×5.0

## 🧪 Тестирование

### **Функция `test_scaling_system()`:**
```gdscript
func test_scaling_system():
    """Тестирует систему масштабирования характеристик"""
    # Тестирует уровни: 1, 5, 10, 15, 20
    # Проверяет правильность распределения очков
    # Выводит итоговые боевые характеристики
```

### **Использование:**
```gdscript
# В консоли Godot или через код
enemy_node.test_scaling_system()
```

## 📈 Преимущества системы

1. **Разнообразие**: Каждый враг уникален благодаря случайному распределению
2. **Баланс**: Линейный рост характеристик предотвращает экспоненциальное усиление
3. **Предсказуемость**: Общее количество очков всегда равно `уровень × 5`
4. **Гибкость**: Легко настраивать количество очков за уровень
5. **Тестируемость**: Встроенная функция тестирования для проверки баланса

## ⚙️ Настройка

### **Изменение количества очков за уровень:**
```gdscript
# В функции _add_random_level_stats()
var total_points = level * 5  # Изменить множитель (например, на 3 или 7)
```

### **Добавление весов для характеристик:**
```gdscript
# Можно добавить веса для более сбалансированного распределения
var stat_weights = {
    "strength": 1.0,
    "agility": 1.0,
    "vitality": 1.2,  # Живучесть получает больше очков
    "endurance": 1.0,
    "intelligence": 1.0,
    "wisdom": 1.0
}
```

## 🎯 Рекомендации по балансу

1. **Мониторинг**: Регулярно проверяйте итоговые характеристики врагов
2. **Тестирование**: Используйте `test_scaling_system()` для проверки баланса
3. **Настройка**: При необходимости корректируйте количество очков за уровень
4. **Документирование**: Записывайте результаты тестов для анализа баланса

---

**Автор**: Claude AI Assistant  
**Дата**: 2024  
**Версия**: 1.0
