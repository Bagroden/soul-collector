# 🚀 Быстрый старт: Экспорт анимаций способностей

## Для Godot 4.5.1

---

## ⭐ Способ 1: Автоматический экспорт (САМЫЙ ПРОСТОЙ!)

**Просто запустите игру - система сама все сделает!**

1. **Нажмите F5** или кнопку **"Игра"** в правом верхнем углу Godot
2. Система **автоматически экспортирует** анимации при первом бое
3. Проверьте в FileSystem (левая панель), что файл `Data/AbilityEffectAnimations.tres` создан
4. Проверьте консоль (панель Output внизу) - должны быть сообщения об экспорте

**Это все!** Система сама все сделает при первом запуске.

---

## Способ 2: Через меню Редактор (если нужно вручную)

### Шаг 1: Откройте меню Редактор

В верхней панели Godot найдите меню **Редактор** (Editor).

### Шаг 2: Запустите скрипт экспорта

1. **Редактор** → **Выполнить скрипт** (или **Editor** → **Run Script**)
2. В открывшемся диалоге найдите и выберите файл:
   ```
   Scripts/Tools/ExportAbilityAnimationsEditor.gd
   ```
3. Нажмите кнопку **Выполнить** (или **Run**)

**Если меню "Редактор" → "Выполнить скрипт" недоступно:**
- Используйте **Способ 3: Автоматический экспорт** (см. ниже) - это самый простой способ!

### Шаг 3: Проверьте результат

1. **Откройте панель Output** (если не открыта):
   - Внизу экрана должна быть панель с вкладками
   - Найдите вкладку **Output** (или **Вывод**)
   - Если не видно: **View** → **Dock** → **Output**
   - Или нажмите `Ctrl+Shift+O`

2. **Проверьте сообщения:**
   - Должны быть сообщения вида: `✅ Экспортирована анимация: double_strike_anim`
   - В конце: `✅ Успешно экспортировано X анимаций`

3. **Проверьте файл:**
   - Откройте `Data/AbilityEffectAnimations.tres` в FileSystem
   - Файл должен существовать и содержать анимации

---

## 🔍 Способ 3: Через консоль (альтернативный)

### Через консоль в Output панели

1. Откройте панель **Output** (внизу экрана, вкладка Output)
2. В консоли введите:

```gdscript
var exporter = preload("res://Scripts/Tools/ExportAbilityAnimations.gd").new()
exporter.export_ability_animations()
```

3. Нажмите **Enter**

**Примечание:** Этот способ работает только если консоль поддерживает выполнение кода (в некоторых версиях Godot может не работать).

---

## ✅ Что должно произойти

После успешного экспорта:

1. ✅ Создан файл `Data/AbilityEffectAnimations.tres`
2. ✅ В файле содержатся все анимации способностей из `PlayerBody.tscn`
3. ✅ В консоли выведено сообщение об успешном экспорте
4. ✅ Система эффектов способностей готова к использованию

---

## ❌ Если что-то пошло не так

### Проблема: "Меню Tools не найдено"
**Решение:** 
- Попробуйте **Editor** → **Run Script**
- Или используйте альтернативный способ через консоль

### Проблема: "Файл не найден"
**Решение:**
- Убедитесь, что файл `Scripts/Tools/ExportAbilityAnimationsEditor.gd` существует
- Проверьте путь в FileSystem

### Проблема: "Анимации не найдены"
**Решение:**
- Убедитесь, что анимации действительно есть в `PlayerBody.tscn` → `Visual` → `SpriteFrames`
- Проверьте имена анимаций в списке `ABILITY_ANIMATIONS` в скрипте экспорта

### Проблема: "Ошибка при сохранении"
**Решение:**
- Убедитесь, что папка `Data` существует
- Проверьте права доступа к файловой системе
- Попробуйте создать папку `Data` вручную в FileSystem

---

## 📝 После экспорта

После успешного экспорта система автоматически загрузит анимации при следующем запуске игры. Проверьте:

1. Запустите игру
2. Войдите в бой
3. Используйте способность с эффектом (например, двойной удар)
4. Эффект должен проигрываться корректно

---

## 🎯 Следующие шаги

После экспорта анимаций можно:

1. Протестировать систему эффектов в игре
2. Начать миграцию остальных способностей на новую систему
3. Добавить новые анимации способностей (если нужно)

Подробнее см. `ABILITY_EFFECT_SYSTEM_SETUP.md`
