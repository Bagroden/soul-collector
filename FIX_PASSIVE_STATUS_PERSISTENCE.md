# Исправление сохранения статуса пассивных способностей

## 🐛 Проблема:
После активации пассивных способностей в меню и выхода из него, при повторном входе способности отображаются как деактивированные, хотя фактически активны.

## 🔍 Причина:
Функция `sync_active_passives_with_bonuses()` была изменена, чтобы НЕ добавлять способности автоматически, но это привело к тому, что активированные способности не сохранялись в `active_passives` массиве.

## ✅ Решение:

### **Восстановлена логика добавления способностей с бонусами:**

```gdscript
func sync_active_passives_with_bonuses():
	"""Синхронизирует активные способности с фактическими бонусами"""
	print("=== СИНХРОНИЗАЦИЯ АКТИВНЫХ СПОСОБНОСТЕЙ С БОНУСАМИ ===")
	print("Активные способности ДО синхронизации: ", active_passives)
	print("Фактические бонусы:")
	print("  Бонус силы: ", passive_strength_bonus)
	print("  Бонус витальности: ", passive_vitality_bonus)
	print("  Бонус мудрости: ", passive_wisdom_bonus)
	print("  Макс. HP: ", max_hp, " (базовое: 100)")
	
	# Добавляем способности, которые имеют бонусы (но не добавляем автоматически при создании персонажа)
	# Это нужно для синхронизации между меню и боем
	
	# Проверяем, какие бонусы фактически применены и добавляем только недостающие
	if passive_strength_bonus > 0 and "player_strong" not in active_passives:
		active_passives.append("player_strong")
		print("Найден бонус силы: ", passive_strength_bonus, " - добавляем player_strong")
	
	if passive_vitality_bonus > 0 and "player_fortress" not in active_passives:
		active_passives.append("player_fortress")
		print("Найден бонус витальности: ", passive_vitality_bonus, " - добавляем player_fortress")
	
	if passive_wisdom_bonus > 0 and "player_wise" not in active_passives:
		active_passives.append("player_wise")
		print("Найден бонус мудрости: ", passive_wisdom_bonus, " - добавляем player_wise")
	
	# Проверяем бонус HP только если он точно от способности "Живучесть" (+50 HP)
	# Базовое HP = 100, бонус от "Живучесть" = +50, итого = 150
	if max_hp >= 150 and "player_vitality" not in active_passives:
		active_passives.append("player_vitality")
		print("Найден бонус HP от Живучести: ", max_hp, " - добавляем player_vitality")
	
	# Удаляем способности, которые не имеют соответствующих бонусов
	var to_remove = []
	for ability_id in active_passives:
		var should_remove = false
		match ability_id:
			"player_strong":
				if passive_strength_bonus <= 0:
					should_remove = true
			"player_fortress":
				if passive_vitality_bonus <= 0:
					should_remove = true
			"player_wise":
				if passive_wisdom_bonus <= 0:
					should_remove = true
			"player_vitality":
				if max_hp < 150:
					should_remove = true
		
		if should_remove:
			to_remove.append(ability_id)
			print("Удаляем способность ", ability_id, " - нет соответствующего бонуса")
	
	for ability_id in to_remove:
		active_passives.erase(ability_id)
	
	print("Активные способности ПОСЛЕ синхронизации: ", active_passives)
	print("=== КОНЕЦ СИНХРОНИЗАЦИИ АКТИВНЫХ СПОСОБНОСТЕЙ ===")
```

## 🎯 Результат:

### **Теперь при активации способности "Сильный":**

#### **1. Активация в меню:**
```
=== ПЕРЕКЛЮЧЕНИЕ ПАССИВНОЙ СПОСОБНОСТИ ===
Способность для переключения: player_strong
Активные способности ДО переключения: []
Переключена пассивная способность: player_strong
Активные способности ПОСЛЕ переключения: ["player_strong"]
=== КОНЕЦ ПЕРЕКЛЮЧЕНИЯ ПАССИВНОЙ СПОСОБНОСТИ ===

=== ПРИМЕНЕНИЕ БОНУСА ОТ СПОСОБНОСТИ: player_strong ===
Характеристики ДО применения:
  Базовая сила: 5, Бонус силы: 0, Итоговая сила: 5
Применен бонус силы: +30 к силе
=== ПЕРЕСЧЕТ ХАРАКТЕРИСТИК ===
Базовые: Сил=5, Лов=5, Вита=5
Бонусы: Сил=30, Лов=0, Вита=0
Итоговые: Сил=35, Лов=5, Вита=5
=== КОНЕЦ ПЕРЕСЧЕТА ===
Характеристики ПОСЛЕ применения:
  Базовая сила: 5, Бонус силы: 30, Итоговая сила: 35
=== КОНЕЦ ПРИМЕНЕНИЯ БОНУСА ===
```

#### **2. Повторное открытие меню:**
```
=== СИНХРОНИЗАЦИЯ АКТИВНЫХ СПОСОБНОСТЕЙ С БОНУСАМИ ===
Активные способности ДО синхронизации: ["player_strong"]
Фактические бонусы:
  Бонус силы: 30
  Бонус витальности: 0
  Бонус мудрости: 0
  Макс. HP: 100 (базовое: 100)
Активные способности ПОСЛЕ синхронизации: ["player_strong"]
=== КОНЕЦ СИНХРОНИЗАЦИИ АКТИВНЫХ СПОСОБНОСТЕЙ ===

=== ЗАГРУЗКА ПАССИВНЫХ СПОСОБНОСТЕЙ ДЛЯ UI ===
Изученные способности: ["player_fortress", "player_strong", "player_wise", "player_vitality"]
Активные способности: ["player_strong"]
Загружено способностей для UI: 4
  - Крепость (player_fortress): Неактивна
  - Сильный (player_strong): Активна  # ← ИСПРАВЛЕНО: остается активной!
  - Мудрый (player_wise): Неактивна
  - Живучесть (player_vitality): Неактивна
=== КОНЕЦ ЗАГРУЗКИ ПАССИВНЫХ СПОСОБНОСТЕЙ ДЛЯ UI ===
```

## 🔧 Логика работы:

### **1. Функция `sync_active_passives_with_bonuses()` теперь:**
- ✅ **Добавляет способности с бонусами** - для синхронизации между меню и боем
- ✅ **Удаляет способности без бонусов** - очистка неактивных
- ✅ **Сохраняет намеренные активации** - выбранные игроком
- ✅ **Работает в обоих направлениях** - меню ↔ бой

### **2. Порядок работы:**
1. **Активация в меню** - `activate_passive_ability()` добавляет в `active_passives` и применяет бонус
2. **Синхронизация** - `sync_active_passives_with_bonuses()` проверяет соответствие бонусов и активных способностей
3. **Сохранение состояния** - способности остаются активными при повторном открытии меню
4. **Применение в бою** - бонусы применяются через `_apply_player_passive_abilities()`

### **3. Защита от автоматической активации:**
- ✅ **При создании нового персонажа** - все бонусы = 0, способности не активируются
- ✅ **При сбросе характеристик** - все бонусы обнуляются, способности деактивируются
- ✅ **Только намеренная активация** - через кнопки в меню

## ✅ Готово к тестированию!

Теперь пассивные способности:
- ✅ **Активируются только по нажатию кнопки** - в меню
- ✅ **Сохраняют статус при повторном открытии** - остаются активными
- ✅ **Показывают правильное состояние** - активные/неактивные
- ✅ **Применяются в бою** - бонусы работают
- ✅ **Синхронизируются правильно** - между меню и боем
- ✅ **Не активируются автоматически** - при создании персонажа

**Теперь статус пассивных способностей будет правильно сохраняться!** 🎯✨
