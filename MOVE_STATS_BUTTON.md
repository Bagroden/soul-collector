# Перенос кнопки распределения характеристик

## 🎯 Цель:
Перенести кнопку распределения характеристик с экрана выбора локации на экран подготовки к путешествию.

## ✅ Реализовано:

### **1. Добавлена кнопка распределения характеристик в `CharacterPreparation.gd`:**

#### **Создание кнопки:**
```gdscript
func _create_distribute_stats_button():
	"""Создает кнопку распределения характеристик"""
	# Создаем кнопку
	distribute_stats_button = Button.new()
	distribute_stats_button.text = "РАСПРЕДЕЛИТЬ ХАРАКТЕРИСТИКИ"
	distribute_stats_button.custom_minimum_size = Vector2(250, 40)
	distribute_stats_button.add_theme_color_override("font_color", Color("66ff66"))  # Зеленый цвет
	
	# Добавляем кнопку в контейнер действий
	var action_container = $MainContainer/ActionContainer
	action_container.add_child(distribute_stats_button)
	
	# Подключаем сигнал
	distribute_stats_button.pressed.connect(_on_distribute_stats_button_pressed)
	
	print("Кнопка распределения характеристик создана")
```

#### **Обработка нажатия:**
```gdscript
func _on_distribute_stats_button_pressed():
	"""Обрабатывает нажатие кнопки распределения характеристик"""
	print("Открываем экран характеристик...")
	get_tree().change_scene_to_file("res://Scenes/StatsScreen.tscn")
```

### **2. Удалена кнопка распределения характеристик из `RoomSelector.gd`:**

#### **Удалены переменные:**
```gdscript
# БЫЛО:
@onready var btn_back = $VBoxContainer/HBoxContainer/Btn_Back
@onready var btn_stats = $VBoxContainer/HBoxContainer/Btn_Stats

# СТАЛО:
@onready var btn_back = $VBoxContainer/HBoxContainer/Btn_Back
```

#### **Удалены подключения сигналов:**
```gdscript
# БЫЛО:
btn_back.connect("pressed", Callable(self, "_on_back_pressed"))
btn_stats.connect("pressed", Callable(self, "_on_stats_pressed"))

# СТАЛО:
btn_back.connect("pressed", Callable(self, "_on_back_pressed"))
```

#### **Удалена функция обработки:**
```gdscript
# УДАЛЕНО:
func _on_stats_pressed():
	print("Открываем экран характеристик...")
	get_tree().change_scene_to_file("res://Scenes/StatsScreen.tscn")
```

## 🎮 Результат:

### **На экране подготовки к путешествию теперь есть:**
- ✅ **Кнопка "РАСПРЕДЕЛИТЬ ХАРАКТЕРИСТИКИ"** - зеленого цвета
- ✅ **Кнопка "СБРОС ХАРАКТЕРИСТИК"** - красного цвета
- ✅ **Кнопка "УПРАВЛЕНИЕ ПАССИВНЫМИ"** - для пассивных способностей
- ✅ **Кнопка "ОТПРАВИТЬСЯ В ПУТЕШЕСТВИЕ"** - для начала игры

### **На экране выбора локации:**
- ✅ **Убрана кнопка распределения характеристик** - больше нет
- ✅ **Осталась только кнопка "НАЗАД"** - для возврата в главное меню

### **Логика работы:**
1. **Главное меню** → **Подготовка к путешествию** (с кнопками характеристик)
2. **Подготовка к путешествию** → **Распределение характеристик** (StatsScreen)
3. **Подготовка к путешествию** → **Выбор локации** (без кнопки характеристик)
4. **Выбор локации** → **Игра** (без кнопки характеристик)

## ✅ Готово к тестированию!

Теперь кнопка распределения характеристик:
- ✅ **Перенесена** - с экрана выбора локации на экран подготовки
- ✅ **Создается программно** - зеленого цвета, размер 250x40
- ✅ **Открывает StatsScreen** - экран распределения характеристик
- ✅ **Удалена из RoomSelector** - больше нет на экране выбора локации

**Теперь распределение характеристик доступно только на экране подготовки к путешествию!** 🎯✨
