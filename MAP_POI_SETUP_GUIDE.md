# Руководство по настройке точек интереса (POI) на карте

## 📋 Создание файлов SpriteFrames

### Создание SpriteFrames в Godot 4.5.1

**Важно:** Удалите старые файлы `.tres` в папке `Assets/Sprites/MapPOI`, если они есть, и создайте их заново.

#### Способ 1: Через создание сцены с AnimatedSprite2D (рекомендуется)

1. В Godot создайте новую сцену: **Scene → New Scene**
2. Добавьте узел **AnimatedSprite2D** (или **Node2D** → добавьте дочерний **AnimatedSprite2D**)
3. Выберите узел **AnimatedSprite2D**
4. В инспекторе найдите свойство **Sprite Frames**
5. Нажмите на **[empty]** справа от "Sprite Frames"
6. Выберите **New SpriteFrames**
7. **Сразу сохраните сцену** как `DungeonPOI.tscn` (временно, потом удалим)
8. Теперь в инспекторе нажмите на **Sprite Frames** (должен быть виден ресурс)
9. В нижней панели откроется редактор **SpriteFrames**
10. Нажмите **"+"** рядом с "Animations" и создайте анимацию `idle`
11. Установите **Loop: true** и **Speed: 8.0**
12. Теперь **сохраните SpriteFrames ресурс отдельно:**
    - В инспекторе нажмите на иконку **"⤴"** (стрелка вверх) рядом с Sprite Frames
    - Выберите **Save As...**
    - Сохраните как `Assets/Sprites/MapPOI/DungeonPOI.tres`
13. Теперь можно удалить временную сцену `DungeonPOI.tscn`
14. Повторите для остальных файлов

#### Способ 2: Прямое создание ресурса

1. В FileSystem выберите папку `Assets/Sprites/MapPOI`
2. Правой кнопкой → **New Resource**
3. Выберите **SpriteFrames** → **Create**
4. **Сразу сохраните** (Ctrl+S) как `DungeonPOI.tres`
5. **Закройте и перезапустите Godot** (или перезагрузите проект: **Project → Reload Current Project**)
6. Теперь двойной клик по `DungeonPOI.tres` должен открыть редактор SpriteFrames
7. Если не открывается, попробуйте:
   - Выберите файл в FileSystem
   - В инспекторе (справа) должен отображаться ресурс SpriteFrames
   - Нажмите на иконку **"⤴"** (стрелка вверх) в инспекторе, чтобы открыть редактор

### Вариант 2: Через скрипт (альтернативный способ)

Если меню "Tools" доступно:

1. В Godot в верхнем меню найдите **Tools** (или **Editor** → **Run Script**)
2. Выберите **Run Script** (или **Execute Script**)
3. Выберите файл `Scripts/Utils/CreateMapPOI.gd`
4. Нажмите **Run**
5. Файлы будут созданы автоматически

**Примечание:** Если меню "Tools" отсутствует, используйте Вариант 1 (вручную).

1. В Godot выберите папку `Assets/Sprites/MapPOI` (или создайте её)
2. Правой кнопкой мыши → **New Resource**
3. Выберите **SpriteFrames**
4. Сохраните как `DungeonPOI.tres`
5. Повторите для остальных:
   - `DarkForestPOI.tres`
   - `CemeteryPOI.tres`
   - `SoulWellPOI.tres`

## 📋 Что должно быть создано

4 файла ресурсов SpriteFrames:
- `Assets/Sprites/MapPOI/DungeonPOI.tres` - для подземелья
- `Assets/Sprites/MapPOI/DarkForestPOI.tres` - для темного леса
- `Assets/Sprites/MapPOI/CemeteryPOI.tres` - для заброшенного кладбища
- `Assets/Sprites/MapPOI/SoulWellPOI.tres` - для колодца душ

## ⚠️ Важно понимать

**`.tres` файлы SpriteFrames - это НЕ сцены!** Это ресурсы данных, которые содержат информацию об анимациях. В них нет узлов типа `AnimatedSprite2D` или `Visual`.

## 🔧 Как добавить спрайты в SpriteFrames

### Шаг 1: Откройте файл SpriteFrames в Godot

1. В Godot откройте файл `Assets/Sprites/MapPOI/DungeonPOI.tres` (или любой другой POI файл)
2. В нижней панели откроется редактор **SpriteFrames**
3. Вы увидите анимацию `idle` (она уже создана, но пустая)

### Шаг 2: Добавьте кадры анимации

1. **Выберите анимацию `idle`** в списке анимаций
2. В правой части панели нажмите кнопку **"Add frames from sprite sheet"** или **"Add frames"**
3. Выберите ваши спрайты из таблицы:
   - Можно выбрать несколько файлов сразу (удерживайте Ctrl)
   - Или выберите spritesheet и укажите размер кадров
4. Кадры автоматически добавятся в анимацию

### Шаг 3: Настройте параметры анимации

В панели SpriteFrames вы можете настроить:
- **Speed** - скорость проигрывания (уже установлено 8.0)
- **Loop** - зацикливание (уже установлено true)
- Порядок кадров (перетаскиванием)

### Шаг 4: Повторите для всех POI

Повторите шаги 1-3 для каждого файла:
- `DarkForestPOI.tres`
- `CemeteryPOI.tres`
- `SoulWellPOI.tres`

## 📍 Где находятся узлы AnimatedSprite2D?

**Узлы AnimatedSprite2D создаются автоматически в коде!**

Когда вы запускаете игру и открываете экран выбора локации (`LocationSelector`), код автоматически:
1. Загружает SpriteFrames ресурсы
2. Создает узлы `AnimatedSprite2D` для каждой POI
3. Размещает их на карте в нужных позициях
4. Запускает анимацию `idle`

**Вы не увидите узлы в файлах .tres**, потому что они создаются динамически в `Scripts/LocationSelector.gd` в функции `_create_poi_sprite()`.

## 🎮 Как проверить, что все работает

1. Добавьте спрайты во все 4 файла SpriteFrames
2. Запустите игру
3. Перейдите к экрану выбора локации
4. Вы должны увидеть анимированные точки интереса на карте:
   - Подземелье - слева
   - Темный лес - в центре слева
   - Заброшенное кладбище - в центре
   - Колодец душ - в верхней части экрана

## 🔍 Если спрайты не отображаются

1. **Проверьте пути к файлам** в `Scripts/LocationSelector.gd`:
   ```gdscript
   "sprite_frames_path": "res://Assets/Sprites/MapPOI/DungeonPOI.tres"
   ```
   Убедитесь, что пути правильные.

2. **Проверьте название анимации** - должно быть `"idle"` (как в коде)

3. **Проверьте консоль** - там будут сообщения:
   - `DEBUG: Создана POI для dungeon...` - если все хорошо
   - `WARNING: Не удалось загрузить SpriteFrames...` - если путь неправильный
   - `WARNING: Анимация 'idle' не найдена...` - если анимация не добавлена

## 📝 Структура файла SpriteFrames

Файл `.tres` выглядит так:
```tres
[gd_resource type="SpriteFrames" load_steps=1 format=3]

[resource]
animations = [{
"frames": [],  # <-- Сюда добавляются кадры в Godot
"loop": true,
"name": &"idle",
"speed": 8.0
}]
```

После добавления спрайтов в Godot, массив `frames` заполнится автоматически.

## ✅ Чеклист

- [ ] Открыл `DungeonPOI.tres` в Godot
- [ ] Добавил спрайты в анимацию `idle`
- [ ] Открыл `DarkForestPOI.tres` и добавил спрайты
- [ ] Открыл `CemeteryPOI.tres` и добавил спрайты
- [ ] Открыл `SoulWellPOI.tres` и добавил спрайты
- [ ] Запустил игру и проверил отображение POI на карте

---

**Важно:** Узлы `AnimatedSprite2D` создаются в коде автоматически при запуске игры. Вам нужно только добавить спрайты в файлы SpriteFrames!

