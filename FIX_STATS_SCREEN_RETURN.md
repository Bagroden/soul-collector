# Исправление возврата из экрана характеристик

## 🐛 Проблема:
При нажатии кнопки "Закрыть" в экране распределения характеристик, игрок возвращается на экран выбора комнаты, хотя должен вернуться на экран подготовки к путешествию.

## ✅ Решение:

### **1. Добавлена переменная для отслеживания предыдущей сцены в `StatsScreen.gd`:**
```gdscript
# Переменная для отслеживания предыдущей сцены
var previous_scene: String = "res://Scenes/RoomSelector.tscn"
```

### **2. Добавлена функция для установки предыдущей сцены в `StatsScreen.gd`:**
```gdscript
func set_previous_scene(scene_path: String):
	"""Устанавливает предыдущую сцену для возврата"""
	previous_scene = scene_path
	print("Установлена предыдущая сцена: ", previous_scene)
```

### **3. Обновлена функция возврата в `StatsScreen.gd`:**
```gdscript
func _on_close_pressed():
	# Возвращаемся к предыдущей сцене
	print("Возвращаемся к предыдущей сцене: ", previous_scene)
	get_tree().change_scene_to_file(previous_scene)
```

### **4. Обновлена логика открытия экрана характеристик в `CharacterPreparation.gd`:**
```gdscript
func _on_distribute_stats_button_pressed():
	"""Обрабатывает нажатие кнопки распределения характеристик"""
	print("Открываем экран характеристик...")
	
	# Загружаем сцену StatsScreen
	var stats_scene = preload("res://Scenes/StatsScreen.tscn")
	var stats_instance = stats_scene.instantiate()
	
	# Устанавливаем предыдущую сцену
	stats_instance.set_previous_scene("res://Scenes/UI/CharacterPreparation.tscn")
	
	# Добавляем к текущей сцене
	add_child(stats_instance)
	
	print("Экран характеристик открыт с возвратом на CharacterPreparation")
```

## 🎯 Результат:

### **Теперь при открытии экрана характеристик из разных мест:**

#### **Из экрана подготовки к путешествию:**
- ✅ **Открывается StatsScreen** - как дочерняя сцена
- ✅ **Устанавливается предыдущая сцена** - CharacterPreparation
- ✅ **При закрытии возврат** - на экран подготовки к путешествию

#### **Из экрана выбора комнаты (если используется):**
- ✅ **Открывается StatsScreen** - как дочерняя сцена
- ✅ **Устанавливается предыдущая сцена** - RoomSelector
- ✅ **При закрытии возврат** - на экран выбора комнаты

### **Отладочные сообщения покажут:**
```
Открываем экран характеристик...
Установлена предыдущая сцена: res://Scenes/UI/CharacterPreparation.tscn
Экран характеристик открыт с возвратом на CharacterPreparation
```

**При закрытии:**
```
Возвращаемся к предыдущей сцене: res://Scenes/UI/CharacterPreparation.tscn
```

## 🔧 Технические детали:

### **Способ открытия экрана:**
- ✅ **Не через change_scene_to_file()** - не меняет текущую сцену
- ✅ **Через preload() и instantiate()** - создает дочернюю сцену
- ✅ **Через add_child()** - добавляет к текущей сцене
- ✅ **С установкой предыдущей сцены** - для правильного возврата

### **Преимущества:**
- ✅ **Гибкость** - можно открывать из разных мест
- ✅ **Правильный возврат** - всегда возвращается туда, откуда открыли
- ✅ **Отладочные сообщения** - показывают, куда происходит возврат
- ✅ **Обратная совместимость** - работает с существующим кодом

## ✅ Готово к тестированию!

Теперь экран характеристик:
- ✅ **Правильно возвращается** - на экран подготовки к путешествию
- ✅ **Работает гибко** - можно открывать из разных мест
- ✅ **Показывает отладочную информацию** - куда происходит возврат
- ✅ **Не ломает существующий функционал** - работает с RoomSelector

**Теперь при закрытии экрана характеристик игрок вернется на экран подготовки к путешествию!** 🎯✨
