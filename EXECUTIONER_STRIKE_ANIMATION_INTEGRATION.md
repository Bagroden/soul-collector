# Интеграция анимации "Удар палача"

## Описание изменений

Добавлена поддержка специальной анимации для способности "Удар палача" (Executioner Strike) у Демона палача.

## Найденная проблема

**Проблема:** После добавления анимации "Executioner_strike" в редакторе, в игре всё равно проигрывалась стандартная анимация "attack".

**Причина:** В коде `battle_manager.gd` на строках ~1164-1168 был общий код, который **всегда** вызывал `play_attack()` для врага после использования любой способности. Этот вызов перекрывал специальную анимацию, которую мы проигрывали ранее.

**Решение:** Добавлен флаг `custom_animation_played`, который отслеживает, была ли уже проиграна специальная анимация. Если да, то общий код пропускает вызов `play_attack()` и не перекрывает специальную анимацию.

## Изменения в коде

### 1. Scripts/Battle/battle_manager.gd

**Что изменено:**
- Добавлен флаг `custom_animation_played` для отслеживания проигрывания специальных анимаций
- Добавлен код проигрывания анимации "Executioner_strike" перед выполнением способности
- Изменен общий код анимации врага, чтобы не перекрывать специальные анимации

**Основные изменения:**

1. **Добавлен флаг (строка ~606):**
```gdscript
var custom_animation_played: bool = false  # Флаг для отслеживания специальных анимаций
```

2. **Проигрывание специальной анимации (строка ~710-726):**
```gdscript
# Запускаем специальную анимацию "Удара палача"
var visual_node = enemy_node.get_node_or_null("Visual")
if visual_node:
    # Проверяем наличие анимации
    if visual_node.sprite_frames and visual_node.sprite_frames.has_animation("Executioner_strike"):
        if visual_node.has_method("play_animation"):
            visual_node.play_animation("Executioner_strike")
        else:
            visual_node.play("Executioner_strike")
            # Вернемся к idle после завершения
            await visual_node.animation_finished
            visual_node.play_idle()
        custom_animation_played = true  # Отмечаем, что специальная анимация проиграна
    else:
        # Fallback: используем стандартную атаку, если специальная анимация не найдена
        visual_node.play_attack()
        custom_animation_played = true  # Отмечаем, что анимация уже проиграна
```

3. **Защита от перекрытия анимации (строка ~1164-1168):**
```gdscript
# Проигрываем анимации параллельно (не ждем), только если специальная анимация не была проиграна
if not custom_animation_played:
    var enemy_visual = enemy_node.get_node_or_null("Visual")
    if enemy_visual and enemy_visual.has_method("play_attack"):
        enemy_visual.play_attack()
```

### 2. Scripts/Battle/character_visual.gd

**Что добавлено:**
- Универсальный метод `play_animation(anim_name: String)` для проигрывания любой анимации по имени

**Код:**
```gdscript
func play_animation(anim_name: String) -> void:
    """Универсальный метод для проигрывания любой анимации по имени"""
    if has_animation(anim_name):
        play(anim_name)
        # Автоматически возвращаемся к idle после анимации
        await animation_finished
        play_idle()
    else:
        # Если анимация не найдена, проигрываем обычную атаку
        print("Анимация '", anim_name, "' не найдена, проигрываем стандартную атаку")
        play_attack()
```

**Особенности:**
- Автоматическое возвращение к анимации `idle` после завершения
- Fallback на стандартную анимацию атаки, если указанная анимация не найдена
- Поддержка любых анимаций по имени (не только "Executioner_strike")

## Использование

### Добавление анимации в Godot Editor

1. Откройте сцену Демона палача (например, `Scenes/Enemies/ExecutionerDemon.tscn`)
2. Выберите узел `Visual` (тип `AnimatedSprite2D`)
3. В инспекторе найдите свойство `SpriteFrames`
4. Откройте редактор `SpriteFrames`
5. Добавьте новую анимацию с именем **"Executioner_strike"** (именно с таким названием!)
6. Добавьте кадры анимации из спрайтшита
7. Настройте FPS и зацикливание (обычно без зацикливания для атаки)
8. Сохраните изменения

### Поведение в игре

**До изменений:**
- При использовании способности "Удар палача" проигрывалась стандартная анимация `attack`

**После изменений:**
- При использовании способности "Удар палача" проигрывается специальная анимация `Executioner_strike`
- Анимация проигрывается в начале способности (до первого удара)
- После завершения анимации автоматически возвращается `idle`
- Если анимация `Executioner_strike` не найдена, используется стандартная `attack` (fallback)

## Последовательность выполнения способности

```
t=0.0 сек:  ▶ Анимация "Executioner_strike" начинается
            ├─► Первый удар (рубящий)
            ├─► Проверка попадания
            ├─► Урон × 1.2
            └─► 🔊 Запуск таймера звука sword_attack3.wav (0.5 сек)

t=0.5 сек:  🔊 Звук sword_attack3.wav проигрывается
            ├─► Задержка между ударами (0.5 сек)
            └─► Второй удар (колющий)

t=1.0 сек:  ├─► Проверка попадания
            ├─► Урон × 1.5
            └─► 🔊 Запуск таймера звука sword_attack4.wav (1.0 сек)

t=2.0 сек:  🔊 Звук sword_attack4.wav проигрывается

t=???:      ✅ Анимация "Executioner_strike" завершается
            └─► Автоматически возвращается к "idle"
```

## Расширение системы

Теперь можно легко добавлять специальные анимации для других способностей:

```gdscript
# Пример для другой способности
if result.get("some_special_ability", false):
    var visual_node = enemy_node.get_node_or_null("Visual")
    if visual_node and visual_node.has_method("play_animation"):
        visual_node.play_animation("Special_Ability_Animation")
```

## Примечания

- Имя анимации регистрозависимое: `"Executioner_strike"` (не `"executioner_strike"`)
- Fallback система гарантирует, что способность будет работать даже если анимация не настроена
- Метод `play_animation()` универсальный и может использоваться для любых кастомных анимаций
- Анимация проигрывается асинхронно, не блокируя выполнение урона

## Тестирование

Для проверки работы:
1. Запустите бой с Демоном палачом
2. Дождитесь использования способности "Удар палача"
3. Убедитесь, что проигрывается анимация "Executioner_strike" (если добавлена)
4. Если анимация не добавлена, должна проигрываться стандартная "attack"
5. Проверьте, что после анимации персонаж возвращается к "idle"

---

**Дата изменений:** 28 октября 2025
**Файлы изменены:**
- `Scripts/Battle/battle_manager.gd`
- `Scripts/Battle/character_visual.gd`

