# Исправление автоматического изучения способностей

## 🐛 Проблема:
Способности автоматически изучались при достижении 100% прогресса изучения, хотя должны были только становиться доступными для покупки за осколки душ.

## 🔧 Исправление:

### **Было (неправильно):**
```gdscript
# Проверяем, изучена ли способность
var required = ability_learning_config[ability_id].required_progress
if progress.current_progress >= required and not progress.is_learned:
    progress.is_learned = true
    progress.learned_at = Time.get_datetime_string_from_system()
    ability_learned.emit(ability_id, progress.current_progress)
    print("🎉 СПОСОБНОСТЬ ИЗУЧЕНА: ", ability_learning_config[ability_id].name)
```

### **Стало (правильно):**
```gdscript
# Ограничиваем прогресс максимумом
var required = ability_learning_config[ability_id].required_progress
if progress.current_progress > required:
    progress.current_progress = required

# Отправляем сигнал обновления прогресса
progress_updated.emit(ability_id, progress.current_progress)
_save_progress()

# Проверяем, достигнут ли 100% прогресс для уведомления
if progress.current_progress >= required and not progress.is_learned:
    print("📚 СПОСОБНОСТЬ ГОТОВА К ИЗУЧЕНИЮ: ", ability_learning_config[ability_id].name, " (100% прогресс)")
```

## 🎯 Результат:

### **Теперь система работает правильно:**
1. **Побеждаешь врагов** → прогресс изучения увеличивается
2. **Достигаешь 100% прогресса** → способность становится доступной для покупки
3. **Покупаешь за осколки душ** → способность изучается и становится доступной для активации

### **Изменения в поведении:**
- **Убрана автоматическая активация** `is_learned = true` при 100% прогрессе
- **Добавлено ограничение прогресса** максимумом (не может превышать 100%)
- **Изменено сообщение** с "ИЗУЧЕНА" на "ГОТОВА К ИЗУЧЕНИЮ"
- **Сохранен сигнал прогресса** для обновления UI

## 📋 Workflow игрока:

1. **Побеждаешь врагов** → прогресс изучения растет (0% → 100%)
2. **При 100% прогрессе** → кнопка меняется на "Купить (X осколков)"
3. **Покупаешь способность** → она изучается и становится доступной
4. **Активируешь способность** → используешь в бою

## ✅ Статус: Исправлено

Теперь способности не изучаются автоматически, а становятся доступными для покупки при достижении 100% прогресса!
