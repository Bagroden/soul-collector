# 📊 Статус миграции активных способностей на универсальную систему

## ✅ Мигрировано (используют новую систему)

### 1. `double_strike` - Двойной удар
- **Статус:** ✅ Полностью мигрировано
- **Местоположение:** `battle_manager.gd` строки 1230, 1267
- **Использование:** `ability_effect_manager.play_ability_effect_on_target(player_node, "double_strike")`

---

## ⏳ Требуют миграции (используют старую систему EffectVisual)

### 2. `poison_strike` - Ядовитый удар
- **Статус:** ⏳ Использует старую систему
- **Местоположение:** `battle_manager.gd` строки 1363-1508
- **Текущий код:** Использует `player_effect_visual` с `poison_strike_anim`
- **Нужно заменить на:** `ability_effect_manager.play_ability_effect_on_target(player_node, "poison_strike")`

### 3. `rat_bite` - Крысиный укус
- **Статус:** ⏳ Использует старую систему
- **Местоположение:** `battle_manager.gd` строки 1556-1590
- **Текущий код:** Использует `player_effect_visual` с `rat_bite_anim`
- **Нужно заменить на:** `ability_effect_manager.play_ability_effect_on_target(player_node, "rat_bite")`

### 4. `crossbow_shot` - Выстрел из арбалета
- **Статус:** ⏳ Использует старую систему
- **Местоположение:** `battle_manager.gd` строки 1605-1682
- **Текущий код:** Использует `player_effect_visual` с `crossbow_shot_anim`
- **Нужно заменить на:** `ability_effect_manager.play_ability_effect_on_target(player_node, "crossbow_shot")`

### 5. `slashing_strike` - Рубящий удар
- **Статус:** ⏳ Использует старую систему
- **Местоположение:** `battle_manager.gd` строки 1693-1724
- **Текущий код:** Использует `player_effect_visual` с `slashing_strike_anim`
- **Нужно заменить на:** `ability_effect_manager.play_ability_effect_on_target(player_node, "slashing_strike")`

### 6. `tombstone` - Надгробие
- **Статус:** ⏳ Использует старую систему
- **Местоположение:** `battle_manager.gd` строки 1693-1855
- **Текущий код:** Использует `player_effect_visual` с `tombstone_anim`
- **Нужно заменить на:** `ability_effect_manager.play_ability_effect_on_target(player_node, "tombstone")`

### 7. `rending_claws` - Разрывающие когти
- **Статус:** ⏳ Использует старую систему
- **Местоположение:** `battle_manager.gd` строки 1882-1910
- **Текущий код:** Использует `player_effect_visual` с `rending_claws_anim`
- **Нужно заменить на:** `ability_effect_manager.play_ability_effect_on_target(player_node, "rending_claws")`

### 8. `bat_swoop` - Пикирование летучей мыши
- **Статус:** ⏳ Использует старую систему
- **Местоположение:** `battle_manager.gd` (нужно найти точное местоположение)
- **Текущий код:** Использует `player_effect_visual` с `bat_swoop_anim`
- **Нужно заменить на:** `ability_effect_manager.play_ability_effect_on_target(player_node, "bat_swoop")`

### 9. `acid_blast` - Кислотный взрыв
- **Статус:** ⏳ Использует старую систему
- **Местоположение:** `battle_manager.gd` (нужно найти точное местоположение)
- **Текущий код:** Использует `player_effect_visual` с `acid_blast_anim`
- **Нужно заменить на:** `ability_effect_manager.play_ability_effect_on_target(player_node, "acid_blast")`

### 10. `crushing_hammer` - Сокрушающий молот
- **Статус:** ⏳ Использует старую систему
- **Местоположение:** `battle_manager.gd` (нужно найти точное местоположение)
- **Текущий код:** Использует `player_effect_visual` с `crushing_hammer_anim`
- **Нужно заменить на:** `ability_effect_manager.play_ability_effect_on_target(player_node, "crushing_hammer")`

---

## 🔍 Способности с особыми случаями

### 11. `spiritual_strike` - Спиритический удар (способность игрока)
- **Статус:** ⚠️ Использует снаряды и временные узлы
- **Местоположение:** `battle_manager.gd` строки 3332-3396
- **Особенность:** Использует снаряд (`_spawn_spiritual_strike_projectile`) и временные узлы эффектов
- **Рекомендация:** Можно мигрировать, но требует проверки работы снарядов

### 12. `kinetic_strike` - Кинетический удар (способность игрока)
- **Статус:** ⚠️ Использует временные узлы
- **Местоположение:** `battle_manager.gd` строки 3649-3681
- **Особенность:** Уже использует временные узлы с `SpriteFrames` игрока
- **Рекомендация:** Можно мигрировать на новую систему

### 13. `magic_arrows` - Магические стрелы
- **Статус:** ⚠️ Использует снаряды
- **Местоположение:** `battle_manager.gd` строки 1509-1555
- **Особенность:** Использует визуальные снаряды (`_spawn_magic_arrow_projectiles`)
- **Рекомендация:** Не требует миграции (использует снаряды, не эффекты на цели)

---

## 📈 Статистика

- **Мигрировано:** 1 из 13 способностей (7.7%)
- **Требуют миграции:** 10 способностей (76.9%)
- **Особые случаи:** 3 способности (23.1%)

---

## 🎯 Приоритет миграции

### Высокий приоритет (часто используемые):
1. `rat_bite` - Крысиный укус (базовая способность крысы)
2. `poison_strike` - Ядовитый удар (способность Гоблина Вора)
3. `crossbow_shot` - Выстрел из арбалета (способность Скелета Арбалетчика)

### Средний приоритет:
4. `slashing_strike` - Рубящий удар
5. `rending_claws` - Разрывающие когти
6. `bat_swoop` - Пикирование летучей мыши

### Низкий приоритет (редко используемые или особые):
7. `tombstone` - Надгробие (массовая атака)
8. `acid_blast` - Кислотный взрыв
9. `crushing_hammer` - Сокрушающий молот
10. `spiritual_strike` - Спиритический удар (способность игрока)
11. `kinetic_strike` - Кинетический удар (способность игрока)

---

## 🔧 Как мигрировать способность

### Шаг 1: Найдите обработку способности
В `battle_manager.gd` найдите блок с `result.get("ability_id", false)`

### Шаг 2: Найдите код с EffectVisual
Найдите блок вида:
```gdscript
var player_effect_visual = player_node.get_node_or_null("EffectVisual")
if player_effect_visual:
    # ... код проигрывания эффекта ...
```

### Шаг 3: Замените на новую систему
Замените весь блок с `EffectVisual` на:
```gdscript
# Проигрываем эффект способности на цели используя универсальный менеджер
if ability_effect_manager:
    ability_effect_manager.play_ability_effect_on_target(player_node, "ability_id")
```

### Шаг 4: Проверьте синхронизацию урона
Убедитесь, что урон наносится после задержки (если нужна синхронизация):
```gdscript
await get_tree().create_timer(0.35).timeout  # Задержка перед эффектом
if ability_effect_manager:
    ability_effect_manager.play_ability_effect_on_target(player_node, "ability_id")
# Наносим урон после эффекта
player_node.take_damage(damage, damage_type)
```

---

## ✅ Итог

**Текущее состояние:** Только `double_strike` мигрирован на новую систему.

**Рекомендация:** 
- Для начала мигрируйте способности высокого приоритета (`rat_bite`, `poison_strike`, `crossbow_shot`)
- Остальные можно мигрировать постепенно по мере необходимости
- Старая система все еще работает, так что миграция не критична
