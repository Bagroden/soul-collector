# Руководство по добавлению анимации "Кинетический удар"

## ✅ Что уже сделано:

1. ✅ Способность "Кинетический удар" существует (`Scripts/Abilities/KineticStrike.gd`)
2. ✅ Обработка анимации добавлена в `battle_manager.gd` (строка ~3571)
3. ✅ Логика воспроизведения эффекта реализована

---

## 📁 Что нужно сделать:

### Шаг 1: Подготовка спрайтшита

1. **Подготовьте ваш спрайтшит** для анимации "Кинетический удар"
   - Рекомендуемый формат: PNG с прозрачностью
   - Размер кадра: 128x64 или 80x64 пикселей (как у врагов)
   - Количество кадров: 6-8 кадров для плавной анимации
   - Расположение: горизонтально в одну строку

2. **Сохраните файл** как:
   ```
   Assets/Sprites/Effects/kinetic_strike_spritesheet.png
   ```

---

### Шаг 2: Импорт в Godot

1. **Откройте Godot** и найдите ваш спрайтшит в `FileSystem`
2. **Кликните на файл** в FileSystem
3. В панели `Import` (справа) выберите:
   - **Preset:** `2D Pixel`
   - **Filter:** `Nearest` (для пиксельной графики)
   - **Mipmaps:** `Off`
4. Нажмите **Reimport**

---

### Шаг 3: Добавление анимации ТОЛЬКО в PlayerBody.tscn

**ВАЖНО!** Анимация добавляется ТОЛЬКО в `PlayerBody.tscn`, а эффект будет автоматически отображаться на всех врагах!

#### Единственный файл для обновления:

```
Scenes/Battle/PlayerBody.tscn ← ТОЛЬКО ЭТОТ ФАЙЛ!
```

**✅ НЕ НУЖНО редактировать сцены врагов!** Система автоматически создает временный узел с эффектом на каждом враге, используя `SpriteFrames` от игрока!

---

### Шаг 4: Добавление анимации в PlayerBody.tscn

#### Через Godot Editor (Рекомендуется)

1. **Откройте сцену игрока:**
   ```
   Scenes/Battle/PlayerBody.tscn
   ```

2. **Выберите узел `Visual`** в Scene Tree (AnimatedSprite2D)

3. **В Inspector найдите свойство `Sprite Frames`**
   - Кликните на него → Выберите **"Edit"**

4. **В панели SpriteFrames (снизу):**
   - Нажмите кнопку **"+ Add Animation"**
   - Назовите анимацию: `kinetic_strike_anim` (ТОЧНО так!)
   - Убедитесь что **"Loop"** выключен

5. **Добавьте кадры:**
   - Кликните на иконку **"Add frames from sprite sheet"** (квадратная сетка)
   - Выберите ваш файл `kinetic_strike_spritesheet.png`
   - В открывшемся окне:
     - Установите **Horizontal/Vertical** (количество кадров в спрайтшите)
     - Выделите **ВСЕ кадры** (Ctrl+A или выделите мышкой)
     - Нажмите **"Add X Frame(s)"**

6. **Настройте скорость:**
   - В правом верхнем углу SpriteFrames найдите **"Speed (FPS)"**
   - Установите значение: `12.0` (можно подстроить под вашу анимацию)

7. **Сохраните сцену:** `Ctrl+S`

**ВСЁ! ГОТОВО!** 🎉 Анимация будет автоматически работать для всех врагов!

---

## 🎨 Рекомендации по визуалу

### Для спрайтшита:
- **Цвета:** Яркие синие/голубые/белые тона с энергетическими разрядами
- **Эффект:** Кинетический импульс, энергетические волны, силовой удар
- **Стиль:** Физический, мощный, динамичный
- **Референсы:**
  - Теневые шипы (shadow_spikes_anim) - темные шипы
  - Кислотный взрыв (acid_blast_anim) - зеленые брызги

### Прогрессия анимации:
1. **Кадр 1-2:** Появление кинетической энергии вокруг цели
2. **Кадр 3-4:** Концентрация энергии в точке удара
3. **Кадр 5-6:** Взрыв кинетической энергии (удар!)
4. **Кадр 7-8:** Рассеивание энергетических волн

### Примеры эффектов:
- Синие энергетические волны
- Белые вспышки
- Круговые ударные волны
- Искажение воздуха (heat wave effect)
- Голубое свечение

---

## 🔧 Тонкая настройка

### Если анимация слишком быстрая/медленная:
В `battle_manager.gd` (строка ~3595):
```gdscript
await get_tree().create_timer(0.35).timeout  # Увеличьте/уменьшите задержку
```

### Если нужен другой звук:
В `battle_manager.gd` (строка ~3592):
```gdscript
SoundManager.play_sound("sword_hit", -5.0)  # Замените на другой звук
```

Доступные звуки:
- `"sword_hit"` - удар мечом (текущий)
- `"magic_arrow"` - магическая стрела
- `"demon_hit"` - демонический удар
- `"thunder"` - гром (если есть)

### Если анимация не центрирована:
В каждой сцене врага, узел `EffectVisual`:
```gdscript
position = Vector2(X, Y)  # Измените позицию
scale = Vector2(2, 2)     # Измените масштаб
offset = Vector2(X, Y)    # Добавьте offset
```

---

## 📊 Техническая информация

### Системные требования для анимации:
- **Узел:** `Visual` (AnimatedSprite2D) в `PlayerBody.tscn` ← ТОЛЬКО ЗДЕСЬ!
- **Имя анимации:** `kinetic_strike_anim` (ВАЖНО: должно совпадать!)
- **Свойства анимации:**
  - `loop = false` - анимация проигрывается один раз
  - `speed = 12.0` - 12 кадров в секунду

### Логика работы в коде:
1. Игрок использует "Кинетический удар"
2. Проверяется попадание
3. Проигрывается анимация атаки игрока
4. Воспроизводится звук удара (0.35 сек задержка)
5. **Создается временный AnimatedSprite2D** поверх врага
6. Временный узел использует `sprite_frames` от игрока (PlayerBody)
7. Запускается анимация `kinetic_strike_anim`
8. В середине анимации наносится урон
9. Показывается damage number и тряска камеры
10. После завершения анимации временный узел удаляется

---

## ❓ Часто задаваемые вопросы

### Q: Анимация не проигрывается
**A:** Проверьте:
1. Имя анимации точно `kinetic_strike_anim`
2. Анимация добавлена в `SpriteFrames` узла `Visual` в `PlayerBody.tscn`
3. В консоли нет ошибки `"Анимация 'kinetic_strike_anim' не найдена"`

### Q: Анимация проигрывается, но слишком маленькая/большая
**A:** Эффект масштабируется в 3 раза автоматически. Чтобы изменить:
   - В `battle_manager.gd` найдите `temp_effect.scale = Vector2(3.0, 3.0)`
   - Измените значение (например, `2.0` для меньшего или `4.0` для большего)

### Q: Анимация в неправильном месте
**A:** Эффект автоматически размещается на позиции врага. Чтобы настроить смещение:
   - В `battle_manager.gd` после `effect_position = target_visual.global_position`
   - Добавьте: `effect_position += Vector2(0, -20)` (смещение вверх на 20 пикселей)

### Q: Хочу другой цвет/эффект для разных врагов
**A:** Сейчас эффект один для всех. Для разных эффектов потребуется более сложная логика с проверкой типа врага и загрузкой разных анимаций.

---

## 🎮 Тестовый чеклист

- [ ] Спрайтшит импортирован в проект
- [ ] Открыт `PlayerBody.tscn`
- [ ] Узел `Visual` выбран
- [ ] Анимация `kinetic_strike_anim` добавлена в SpriteFrames
- [ ] Все кадры добавлены в анимацию
- [ ] Скорость анимации настроена (12 FPS)
- [ ] Loop отключен
- [ ] Сцена `PlayerBody.tscn` сохранена
- [ ] Запущен бой с **любым** врагом
- [ ] Анимация проигрывается над врагом
- [ ] Урон наносится в середине анимации
- [ ] Сообщение "⚡ Кинетический удар!" появляется
- [ ] Тряска камеры работает при крите

---

## 🔄 Сравнение с другими анимациями

| Характеристика | Теневые шипы | Спиритический удар | Кинетический удар |
|---|---|---|---|
| **Владелец** | Враг (Dark Stalker) | **Игрок** | **Игрок** |
| **Применяется к** | Игроку | **Врагу** | **Врагу** |
| **Цвет** | Темно-фиолетовый | Фиолетово-синий | **Синий/Голубой** |
| **Эффект** | Шипы из тени | Магический взрыв | **Кинетическая энергия** |
| **Звук** | `magic_arrow` | Снаряд | **`sword_hit`** |
| **Где хранится** | `PlayerBody.tscn` | **`PlayerBody.tscn`** | **`PlayerBody.tscn`** |
| **Механизм** | EffectVisual на игроке | **Временный узел** | **Временный узел** |
| **Масштаб** | 2x | **3x** | **3x** |

---

**Автор:** Claude AI Assistant  
**Дата:** 2024  
**Версия:** 1.0

