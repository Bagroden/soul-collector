# 🔊 Система звуковых эффектов

## 📋 Обзор

Система звуковых эффектов в игре Soul Collector обеспечивает воспроизведение звуков для способностей, атак, интерфейса и других игровых событий. Система построена на основе синглтона `SoundManager` и интегрирована с настройками громкости.

## 🏗️ Архитектура системы

### Основные компоненты:

1. **SoundManager** - главный менеджер звуков (автозагрузка)
2. **Звуковые файлы** - WAV/OGG файлы в папке `Audio/Sounds/`
3. **Интеграция с настройками** - связь с `SettingsManager`
4. **Вызовы из способностей** - интеграция с боевой системой

## 📁 Структура файлов

```
Audio/
└── Sounds/                    # Звуковые эффекты
    ├── rat_bite.wav          # Крысиный укус
    ├── sword_hit.wav         # Удары мечом
    ├── magic_arrow.wav      # Магические атаки
    ├── critical_hit.wav     # Критические удары
    ├── damage_taken.wav     # Получение урона
    ├── heal.wav             # Лечение
    ├── button_click.wav     # Нажатие кнопки
    └── level_up.wav         # Повышение уровня
```

## 🔧 SoundManager - Основной класс

### Расположение: `Scripts/SoundManager.gd`

```gdscript
# Основные свойства
static var instance: SoundManager
var sound_effects: Dictionary = {}
var sound_volume: float = 0.0
```

### Ключевые методы:

#### `play_sound(sound_name: String, volume_db: float = 0.0)`
- **Назначение**: Воспроизводит звуковой эффект
- **Параметры**:
  - `sound_name` - ID звука из словаря
  - `volume_db` - дополнительная громкость в децибелах
- **Пример**: `SoundManager.play_sound("rat_bite", -10.0)`

#### `play_sound_at_position(sound_name: String, position: Vector2, volume_db: float = 0.0)`
- **Назначение**: Воспроизводит звук в определенной позиции (для 3D звука)
- **Параметры**:
  - `sound_name` - ID звука
  - `position` - позиция в мире
  - `volume_db` - дополнительная громкость

## 🎵 Поддерживаемые звуки

### Звуки способностей:

| ID | Файл | Описание | Использование |
|---|---|---|---|
| `rat_bite` | `rat_bite.wav` | Крысиный укус | Атака крысы |
| `sword_hit` | `sword_hit.wav` | Удар мечом | Физические атаки |
| `magic_arrow` | `magic_arrow.wav` | Магическая стрела | Магические атаки |
| `magic2` | `magic2.wav` | Спиритический удар | Специальная магическая атака |
| `bat_attack` | `bat_attack.wav` | Пикирование летучей мыши | Атака летучей мыши |
| `magic_arrow` | `magic_arrow.wav` | Магическая стрела | Каждая выпущенная стрела |
| `critical_hit` | `critical_hit.wav` | Критический удар | Критические попадания |

### Звуки интерфейса:

| ID | Файл | Описание | Использование |
|---|---|---|---|
| `button_click` | `button_click.wav` | Нажатие кнопки | UI элементы |
| `level_up` | `level_up.wav` | Повышение уровня | Прогресс игрока |
| `page` | `page.wav` | Перелистывание страницы | Главное меню, навигация |

### Звуки урона:

| ID | Файл | Описание | Использование |
|---|---|---|---|
| `damage_taken` | `damage_taken.wav` | Получение урона | Получение урона |
| `heal` | `heal.wav` | Лечение | Восстановление HP |

## 🎮 Интеграция с главным меню

### Звуки кнопок главного меню

Все кнопки главного меню воспроизводят звук `page.wav` при нажатии:

```gdscript
# В MainMenu.gd
func _on_new_game_pressed():
    if SoundManager:
        SoundManager.play_sound("page", -5.0)
    # Логика кнопки...
```

### Кнопки с звуками:

- **Новая игра** → `page.wav`
- **Продолжить** → `page.wav`
- **Загрузить** → `page.wav`
- **Настройки** → `page.wav`
- **Выход** → `page.wav`
- **Вернуться в бой** → `page.wav`
- **Подтверждение диалогов** → `page.wav`

## 🎮 Интеграция с экраном подготовки персонажа

### Звуки кнопок экрана подготовки

Все кнопки экрана подготовки персонажа воспроизводят звук `page.wav` при нажатии:

```gdscript
# В CharacterPreparation.gd
func _on_button_pressed():
    if SoundManager:
        SoundManager.play_sound("page", -5.0)
    # Логика кнопки...
```

### Кнопки экрана подготовки с звуками:

- **Пассивные способности** → `page.wav`
- **Активные способности** → `page.wav`
- **Назад** → `page.wav`
- **Отправиться в путешествие** → `page.wav`
- **Сброс характеристик** → `page.wav`
- **Распределить характеристики** → `page.wav`
- **Изучение способностей** → `page.wav`

## ⚔️ Интеграция с боевой системой

### Звуки способностей игрока

Способности игрока воспроизводят специальные звуки при использовании:

```gdscript
# В SpiritualStrike.gd
func use_ability(owner: Node, target: Node) -> Dictionary:
    if SoundManager:
        SoundManager.play_sound("magic2", -5.0)
    return super.use_ability(owner, target)

# В BatDive.gd
func use_ability(owner: Node, target: Node) -> Dictionary:
    if SoundManager:
        SoundManager.play_sound("bat_attack", -5.0)
    return super.use_ability(owner, target)
```

### Способности игрока с звуками:

- **Спиритический удар** → `magic2.wav`
- **Пикирование летучей мыши** → `bat_attack.wav`
- **Другие способности** → соответствующие звуки (будут добавлены)

## ⚔️ Интеграция с боевой системой

### Способности врагов

Звуки автоматически воспроизводятся при использовании способностей врагов:

```gdscript
# В EnemyAbility.gd
if SoundManager:
    match id:
        "rat_bite_attack":
            SoundManager.play_sound("rat_bite", -10.0)
        "sword_attacks":
            SoundManager.play_sound("sword_hit", -5.0)
        "magic_attacks":
            SoundManager.play_sound("magic_arrow", -5.0)
```

### Типы способностей и звуки:

#### 🐀 Физические атаки → `sword_hit.wav`
- Мышиная стая
- Кислотные атаки слизней
- Удары палача
- Атаки гоблинов
- Теневые удары

#### 🔮 Магические атаки → `magic_arrow.wav`
- Темный взрыв (Алкар демон)
- Проклятый взрыв (Демон проклятия)
- Удар щитом (Тарнок демон)
- Ультразвуковая атака (Летучая мышь)

#### 🎯 Специальные способности:
- **Двойной удар** → `sword_hit.wav`
- **Ядовитый удар** → `sword_hit.wav`
- **Магические стрелы** → `magic_arrow.wav` (специальная обработка)

#### 🏹 Магические стрелы - специальная обработка звуков:

Способность "Магические стрелы" имеет простую систему звуков:

- **Количество стрел**: `1 + (Интеллект / 15)`
- **Звук для каждой стрелы**: `magic_arrow.wav`
- **Правило**: Одна стрела = один звук
- **Задержка**: 0.2 сек между стрелами

```gdscript
# Простая обработка для магических стрел
for i in range(arrows_count):
    SoundManager.play_sound("magic_arrow", -5.0)
    
    if i < arrows_count - 1:
        await get_tree().create_timer(0.2).timeout
```

## 🔊 Управление громкостью

### Интеграция с настройками:

```gdscript
# В SoundManager._ready()
var settings_manager = get_node_or_null("/root/SettingsManager")
if settings_manager:
    sound_volume = settings_manager.get_sound_volume()
```

### Уровни громкости:

- **Базовая громкость**: Настраивается в игре
- **Дополнительная громкость**: Указывается при вызове
- **Итоговая громкость**: `sound_volume + volume_db`

### Примеры громкости:

```gdscript
# Тихие звуки (крысиный укус)
SoundManager.play_sound("rat_bite", -10.0)

# Обычные звуки (удары мечом)
SoundManager.play_sound("sword_hit", -5.0)

# Громкие звуки (магические заклинания)
SoundManager.play_sound("magic_arrow", 0.0)
```

## 🛠️ Как добавить новый звук

### 1. Создайте звуковой файл
- **Формат**: WAV или OGG
- **Длительность**: 0.5-2.0 секунды
- **Частота**: 44.1 kHz
- **Разместите**: в папке `Audio/Sounds/`

### 2. Добавьте в SoundManager
```gdscript
# В _load_sound_effects()
sound_effects["new_sound"] = preload("res://Audio/Sounds/new_sound.wav")
```

### 3. Используйте в коде
```gdscript
# В любом месте игры
if SoundManager:
    SoundManager.play_sound("new_sound", -5.0)
```

## 📝 Примеры использования

### В способностях врагов:

```gdscript
# В EnemyAbility.gd
if SoundManager:
    match ability_id:
        "fire_blast":
            SoundManager.play_sound("magic_arrow", -5.0)
        "ice_strike":
            SoundManager.play_sound("sword_hit", -5.0)
```

### В способностях игрока:

```gdscript
# В PlayerAbility.gd
func use_ability(owner: Node, target: Node) -> Dictionary:
    # Логика способности...
    
    # Воспроизведение звука
    if SoundManager:
        SoundManager.play_sound("player_ability", -5.0)
    
    return result
```

### В UI элементах:

```gdscript
# В UI скриптах
func _on_button_pressed():
    if SoundManager:
        SoundManager.play_sound("button_click", -5.0)
```

## 🔧 Технические детали

### Автозагрузка:
```gdscript
# В project.godot
[autoload]
SoundManager="*res://Scripts/SoundManager.gd"
```

### Управление AudioStreamPlayer:
- Создаются динамически при воспроизведении
- Автоматически удаляются после завершения
- Поддерживают множественное воспроизведение

### Обработка ошибок:
```gdscript
if not sound_effects.has(sound_name):
    print("Звук не найден: ", sound_name)
    return
```

## 🎯 Рекомендации

### Создание звуков:
- Используйте короткие звуки (0.5-2.0 сек)
- Избегайте слишком громких звуков
- Тестируйте в контексте игры
- Используйте WAV для коротких звуков, OGG для длинных

### Производительность:
- Звуки загружаются при старте игры
- Не создавайте слишком много одновременных звуков
- Используйте разумные уровни громкости

### Интеграция:
- Всегда проверяйте `if SoundManager:` перед вызовом
- Используйте подходящие звуки для типов атак
- Настраивайте громкость в зависимости от важности звука

## 🐛 Отладка

### Частые проблемы:

1. **Звук не воспроизводится**:
   - Проверьте, что файл существует
   - Убедитесь, что SoundManager в автозагрузках
   - Проверьте правильность ID звука

2. **Слишком громкий/тихий звук**:
   - Настройте параметр `volume_db`
   - Проверьте настройки громкости в игре

3. **Звук не останавливается**:
   - AudioStreamPlayer автоматически удаляется
   - Проверьте, что нет бесконечных циклов

### Логи для отладки:
```gdscript
# В SoundManager.play_sound()
print("Воспроизводим звук: ", sound_name, " громкость: ", volume_db)
```

---

## 📚 Связанные файлы

- `Scripts/SoundManager.gd` - Основной менеджер звуков
- `Scripts/Abilities/EnemyAbility.gd` - Интеграция с способностями врагов
- `Scripts/Abilities/RatBite.gd` - Пример использования в способности
- `project.godot` - Настройки автозагрузки
- `Audio/Sounds/` - Папка со звуковыми файлами

---

*Документация обновлена для версии системы звуков Soul Collector*
