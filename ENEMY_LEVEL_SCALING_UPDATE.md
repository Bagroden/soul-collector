# Обновление системы уровней врагов

## 📋 Изменения

Изменена система определения уровней врагов с фиксированных уровней локаций на динамическое масштабирование по уровню игрока.

---

## 🔄 Старая система

**Было:**
- Каждая локация имела фиксированный диапазон уровней (min_level - max_level)
- Уровень врага = уровень игрока + случайный бонус от -1 до +2
- Не было связи между уровнем врага и его редкостью

**Пример:**
```
Подземелье под городом
Уровни: 1 - 5
```

---

## ⚡ Новая система

**Стало:**
- Уровень врага = **уровень игрока + бонус от редкости**
- Бонус зависит от редкости врага
- Отображается динамический диапазон уровней

**Пример:**
```
Если игрок 10 уровня:
Уровень врагов: 10 - 16
(10 для обычных, 16 для мифических)
```

---

## 📊 Бонусы от редкости

| Редкость | Бонус к уровню | Пример (ГГ 10 ур.) |
|----------|----------------|-------------------|
| **Common** | +0 | Уровень 10 |
| **Uncommon** | +1 | Уровень 11 |
| **Rare** | +2 | Уровень 12 |
| **Epic** | +3 | Уровень 13 |
| **Elite** | +3 | Уровень 13 |
| **Legendary** | +4 | Уровень 14 |
| **Boss** | +5 | Уровень 15 |
| **Mythic** | +6 | Уровень 16 |

**Максимальный бонус:** +6 (Мифическая редкость)

---

## 🎮 Отображение в игре

### Выбор локации:

**Старое отображение:**
```
Подземелье под городом
Уровни: 1 - 5
```

**Новое отображение:**
```
Подземелье под городом
Уровень врагов: 10 - 16
(где 10 = уровень игрока, 16 = уровень игрока + 6)
```

---

## 🔧 Реализация

### 1. enemy_spawner.gd

**Функция:** `_set_enemy_rarity()`

**Было:**
```gdscript
# Уровень врага = уровень игрока + случайный бонус от -1 до +2
enemy_level = max(1, player_data.level + randi_range(-1, 2))
```

**Стало:**
```gdscript
# Уровень врага = уровень игрока + бонус от редкости
var rarity_bonus = _get_rarity_level_bonus(rarity)
enemy_level = max(1, player_data.level + rarity_bonus)
```

---

**Новая функция:** `_get_rarity_level_bonus()`

```gdscript
func _get_rarity_level_bonus(rarity: String) -> int:
    """Возвращает бонус к уровню врага в зависимости от редкости"""
    match rarity.to_lower():
        "common":
            return 0
        "uncommon":
            return 1
        "rare":
            return 2
        "epic":
            return 3
        "elite":
            return 3
        "legendary":
            return 4
        "boss":
            return 5
        "mythic":
            return 6
        _:
            return 0
```

---

### 2. LocationSelector.gd

**Функция:** `_create_location_card()`

**Было:**
```gdscript
# Информация о уровнях
var level_label = Label.new()
level_label.text = "Уровни: %d - %d" % [location.min_level, location.max_level]
```

**Стало:**
```gdscript
# Информация о уровнях врагов (зависит от уровня игрока)
var level_label = Label.new()
var player_level = _get_player_level()
var max_rarity_bonus = 6  # Максимальный бонус от мифической редкости
level_label.text = "Уровень врагов: %d - %d" % [player_level, player_level + max_rarity_bonus]
```

---

**Новая функция:** `_get_player_level()`

```gdscript
func _get_player_level() -> int:
    """Возвращает текущий уровень игрока"""
    var player_manager = get_node_or_null("/root/PlayerManager")
    if player_manager and player_manager.has_method("get_player_data"):
        var player_data = player_manager.get_player_data()
        if player_data:
            return player_data.level
    return 1
```

---

## 📈 Примеры масштабирования

### Игрок 1 уровня:
```
Уровень врагов: 1 - 7
├─ Common: 1
├─ Uncommon: 2
├─ Rare: 3
├─ Epic: 4
├─ Elite: 4
├─ Legendary: 5
├─ Boss: 6
└─ Mythic: 7
```

### Игрок 10 уровня:
```
Уровень врагов: 10 - 16
├─ Common: 10
├─ Uncommon: 11
├─ Rare: 12
├─ Epic: 13
├─ Elite: 13
├─ Legendary: 14
├─ Boss: 15
└─ Mythic: 16
```

### Игрок 50 уровня:
```
Уровень врагов: 50 - 56
├─ Common: 50
├─ Uncommon: 51
├─ Rare: 52
├─ Epic: 53
├─ Elite: 53
├─ Legendary: 54
├─ Boss: 55
└─ Mythic: 56
```

---

## ⚖️ Баланс

### Преимущества новой системы:

✅ **Постоянная релевантность:**
- Все локации остаются актуальными на любом уровне игрока
- Нет "устаревших" локаций со слабыми врагами

✅ **Прозрачность:**
- Игрок сразу видит, какого уровня будут враги
- Связь между редкостью и сложностью очевидна

✅ **Прогрессия:**
- Редкие враги всегда сложнее обычных
- Разница в 6 уровней (от common до mythic) создает ощутимый вызов

✅ **Масштабируемость:**
- Система работает на любом уровне игрока
- Не требует ручной настройки локаций

---

### Кривая сложности:

```
Разница между редкостями:
Common → Uncommon:   +1 уровень
Uncommon → Rare:     +1 уровень
Rare → Epic:         +1 уровень
Epic → Elite:        0 уровней (одинаковые)
Epic → Legendary:    +1 уровень
Legendary → Boss:    +1 уровень
Boss → Mythic:       +1 уровень

Итого: 6 уровней разницы от Common до Mythic
```

---

## 🎯 Влияние на геймплей

### Для игрока:

1. **Выбор локации:**
   - Все локации одинаково опасны (враги всегда уровня игрока)
   - Выбор зависит от предпочтений, а не от "фарма уровней"

2. **Прогрессия:**
   - Редкие враги всегда представляют вызов (+2-6 уровней)
   - Награды от редких врагов стоят риска

3. **Стратегия:**
   - Можно избегать высоких редкостей для безопасности
   - Или искать их для лучших наград

### Для разработчика:

1. **Баланс:**
   - Один набор множителей для всех уровней
   - Не нужно балансировать каждую локацию отдельно

2. **Контент:**
   - Новые локации автоматически масштабируются
   - Фокус на уникальности врагов, а не на их уровнях

---

## 🔮 Будущее развитие

### Возможные дополнения:

1. **Модификаторы локаций:**
   ```
   Проклятое подземелье: +2 ко всем уровням врагов
   ```

2. **Сложность:**
   ```
   Нормальная: уровень ГГ + бонус
   Сложная:    уровень ГГ + бонус + 3
   ```

3. **События:**
   ```
   Во время события "Нашествие": все враги +5 уровней
   ```

---

## 📝 Тестирование

### Сценарий проверки:

1. **Игрок 1 уровня:**
   - Зайти в выбор локаций
   - Проверить отображение "Уровень врагов: 1 - 7"
   - Войти в бой с обычным врагом → уровень 1
   - Войти в бой с редким врагом → уровень 3
   - Войти в бой с мифическим → уровень 7

2. **Игрок 10 уровня:**
   - Зайти в выбор локаций
   - Проверить отображение "Уровень врагов: 10 - 16"
   - Войти в бой с обычным врагом → уровень 10
   - Войти в бой с боссом → уровень 15

3. **Повышение уровня:**
   - Получить уровень
   - Вернуться к выбору локаций
   - Убедиться что диапазон обновился

---

## ✅ Чек-лист изменений

- [x] Добавлена функция `_get_rarity_level_bonus()` в `enemy_spawner.gd`
- [x] Изменена логика расчета уровня врага в `_set_enemy_rarity()`
- [x] Добавлена функция `_get_player_level()` в `LocationSelector.gd`
- [x] Обновлено отображение уровней в `_create_location_card()`
- [x] Протестирована система для разных уровней игрока
- [x] Создана документация

---

**Дата обновления:** 20 ноября 2025  
**Версия:** 2.0  
**Статус:** ✅ Реализовано  
**Файлы:**
- `Scripts/Battle/enemy_spawner.gd`
- `Scripts/LocationSelector.gd`

