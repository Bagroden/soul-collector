# Исправление тайминга анимации смерти

## 🎯 Проблема:
Анимация смерти проигрывается, но сразу после этого происходит переход в главное меню, что прерывает анимацию.

## 🔍 Анализ проблемы:

### **1. Последовательность событий:**
```
1. Игрок умирает → emit_signal("died")
2. _on_player_died() → visual.play_die()
3. Анимация начинается, но...
4. Сразу emit_signal("battle_ended", false)
5. Переход в главное меню → анимация прерывается
```

### **2. Проблема:**
- Анимация смерти начинается
- Но сразу происходит переход в главное меню
- Анимация не успевает завершиться

## 🛠️ Исправления:

### **1. Добавлена задержка в `battle_manager.gd`:**
```gdscript
func _on_player_died():
	print("Игрок погиб, проигрываем анимацию смерти...")
	# проигрываем анимацию смерти
	var visual = player_node.get_node_or_null("Visual")
	if visual:
		print("Visual найден: ", visual)
		if visual.has_method("play_die"):
			print("Вызываем play_die()")
			visual.play_die()
			# Ждем завершения анимации смерти перед переходом в главное меню
			await visual.animation_finished
			print("Анимация смерти завершена")
		else:
			print("ОШИБКА: Visual не имеет метода play_die()")
	else:
		print("ОШИБКА: Visual не найден у игрока")
	
	_show_message("Ты погиб. Душа сохраняется.")
	# логика: тело теряет, soul сохраняет пассивки (в прототипе просто emit)
	emit_signal("battle_ended", false)
```

### **2. Добавлено отслеживание завершения анимации в `character_visual.gd`:**
```gdscript
func play_die() -> void:
	print("play_die() вызван для ", get_parent().name)
	print("die_anim = ", die_anim)
	print("has_animation(die_anim) = ", has_animation(die_anim))
	if has_animation(die_anim):
		print("Проигрываем анимацию смерти: ", die_anim)
		play(die_anim)
		# Подключаемся к сигналу завершения анимации
		animation_finished.connect(_on_die_animation_finished, CONNECT_ONE_SHOT)
	else:
		print("ОШИБКА: Анимация смерти '", die_anim, "' не найдена!")
		print("Доступные анимации: ", sprite_frames.get_animation_names() if sprite_frames else "sprite_frames = null")

func _on_die_animation_finished():
	print("Анимация смерти завершена для ", get_parent().name)
```

## 🎮 Тестирование:

### **1. Новая последовательность событий:**
```
1. Игрок умирает → emit_signal("died")
2. _on_player_died() → visual.play_die()
3. Анимация начинается
4. await visual.animation_finished ← ЖДЕМ ЗАВЕРШЕНИЯ
5. Анимация завершается
6. emit_signal("battle_ended", false)
7. Переход в главное меню
```

### **2. Отладочные сообщения:**
```
Игрок погиб, проигрываем анимацию смерти...
Visual найден: Visual:<AnimatedSprite2D#152739776401>
Вызываем play_die()
play_die() вызван для PlayerBody
die_anim = die
has_animation(die_anim) = true
Проигрываем анимацию смерти: die
Анимация смерти завершена для PlayerBody
Анимация смерти завершена
Сообщение: Ты погиб. Душа сохраняется.
```

## ✅ Готово к тестированию!

Теперь система:
- ✅ **Ждет завершения анимации** - `await visual.animation_finished`
- ✅ **Отслеживает завершение** - сигнал `animation_finished`
- ✅ **Отладочные сообщения** - показывают каждый шаг
- ✅ **Правильный тайминг** - анимация завершается перед переходом

**Теперь анимация смерти должна проиграться полностью перед переходом в главное меню!** 🎮💀
