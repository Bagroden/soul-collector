# Обновление звуков для гоблина вора

## Дата: 28 октября 2025

## Описание
Добавлены звуки для способности "Ядовитый удар" и базовой атаки гоблина вора.

## Изменения

### 1. Регистрация звуков в SoundManager (`Scripts/SoundManager.gd`)

**Добавлено:**
```gdscript
sound_effects["sword_attack1"] = preload("res://Audio/Sounds/sword_attack1.wav")
sound_effects["poison_strike1"] = preload("res://Audio/Sounds/poison_strike1.wav")
```

Оба звука зарегистрированы в `_load_sound_effects()` для доступа через `SoundManager.play_sound()`.

### 2. Звук способности "Ядовитый удар" (`Scripts/Abilities/EnemyAbility.gd`)

**Было:**
```gdscript
SoundManager.play_sound("sword_hit", -5.0)
```

**Стало:**
```gdscript
SoundManager.play_sound("poison_strike1", -5.0)
```

**Изменение в строке ~384:** В специальной обработке `poison_strike`

### 3. Звук базовой атаки гоблина вора (`Scripts/Battle/battle_manager.gd`)

**Добавлено:**
```gdscript
elif damage_type == "physical" and enemy_node.display_name == "Гоблин Вор":
    # Звук базовой атаки гоблина вора
    var audio_player = AudioStreamPlayer.new()
    audio_player.stream = load("res://Audio/Sounds/sword_attack1.wav")
    audio_player.volume_db = -5.0
    audio_player.bus = "Master"
    add_child(audio_player)
    audio_player.play()
    # Удаляем после проигрывания
    audio_player.finished.connect(func(): audio_player.queue_free())
```

**Место добавления:** Функция `_enemy_action()`, блок обработки базовой атаки врага (`if not ability_used:`), после звука для крысы.

## Файлы звуков

### poison_strike1.wav
- **Путь**: `Audio/Sounds/poison_strike1.wav`
- **Громкость**: -5.0 dB
- **Использование**: Способность "Ядовитый удар"
- **Формат**: WAV

### sword_attack1.wav
- **Путь**: `Audio/Sounds/sword_attack1.wav`
- **Громкость**: -5.0 dB
- **Использование**: Базовая атака гоблина вора
- **Формат**: WAV

## Когда проигрывается

### Способность "Ядовитый удар":
- Гоблин Вор использует активную способность "Ядовитый удар" (`poison_strike`)
- Проигрывается во время использования способности
- Громкость: -5.0 dB
- Накладывает 2 стака яда на цель

### Базовая атака гоблина вора:
- Гоблин Вор делает обычную атаку (когда способность на перезарядке)
- `damage_type == "physical"`
- `enemy_node.display_name == "Гоблин Вор"`
- Громкость: -5.0 dB

## Технические детали

### Проверка имени врага
Для базовой атаки используется проверка `enemy_node.display_name == "Гоблин Вор"` чтобы звук проигрывался только для гоблина вора, а не для всех врагов с физической атакой.

### Громкость
Установлена громкость `-5.0 dB` для обоих звуков (стандартная громкость для врагов среднего размера).

### Способность "Ядовитый удар"
- **ID**: `poison_strike`
- **Урон**: Ловкость × 2.5
- **Эффект**: Накладывает 2 стака яда
- **Кулдаун**: 2 хода (пропускает 1 ход врага)
- **Тип урона**: `poison` (яд)
- **Максимум стаков яда**: 3
- **Урон от яда**: 10 урона за раунд за стак

### Система воспроизведения
Используется динамическое создание `AudioStreamPlayer`:
- Загрузка звука через `load()` или `SoundManager.play_sound()`
- Настройка громкости и шины
- Автоматическое удаление после проигрывания через `finished.connect()`

## Отличия от других врагов

### Гоблин Вор vs Крыса:
- **Гоблин Вор**: `sword_attack1.wav` (-5.0 dB) - более громкий, так как крупнее
- **Крыса**: `rat_attack1.wav` (-10.0 dB) - тише, так как мелкий враг

### Звук способности:
- **Ядовитый удар**: `poison_strike1.wav` - специфический звук для ядовитой атаки
- **Крысиный укус**: `rat_attack1.wav` - более быстрый и агрессивный звук

## Статус
🟢 **Завершено** - Все изменения внесены и проверены линтером

## Примечания
- Звук `poison_strike1.wav` используется только для способности "Ядовитый удар"
- Звук `sword_attack1.wav` используется только для базовой атаки гоблина вора
- Громкость -5.0 dB подобрана с учетом размера и агрессивности врага
- Оба звука создают характерный образ гоблина вора - ловкого и коварного противника

