# Покупка способностей за осколки душ

## 🎯 Описание:
Добавлена возможность покупать пассивные способности за осколки душ, но только после достижения 100% прогресса изучения. Это основной способ получения способностей - сначала изучаешь через бои, затем покупаешь за осколки душ.

## 💰 Экономика:

| Редкость | Стоимость (осколки душ) |
|----------|------------------------|
| **Common** (Обычная) | 100 |
| **Uncommon** (Необычная) | 300 |
| **Rare** (Редкая) | 500 |
| **Epic** (Эпическая) | 700 |
| **Legendary** (Легендарная) | 1000 |
| **Mythic** (Мифическая) | 2000 |

## 🔧 Реализованные изменения:

### **1. Обновлен UI в AbilityLearningScreen.gd:**

#### **Кнопка изучения:**
```gdscript
# Кнопка изучения
var learn_button = Button.new()
if is_learned:
    learn_button.text = "Изучена"
    learn_button.disabled = true
else:
    var cost = _get_ability_cost(ability.rarity)
    var required_progress = ability_learning_system.ability_learning_config.get(ability.id, {}).get("required_progress", 100)
    var current_progress = progress_data.get("current_progress", 0)
    
    if current_progress >= required_progress:
        learn_button.text = "Купить (" + str(cost) + " осколков)"
        learn_button.disabled = false
    else:
        learn_button.text = "Недоступно (" + str(current_progress) + "/" + str(required_progress) + ")"
        learn_button.disabled = true
learn_button.custom_minimum_size = Vector2(200, 35)
```

#### **Функция стоимости:**
```gdscript
func _get_ability_cost(rarity: String) -> int:
    """Возвращает стоимость изучения способности в зависимости от редкости"""
    match rarity:
        "common": return 100
        "uncommon": return 300
        "rare": return 500
        "epic": return 700
        "legendary": return 1000
        "mythic": return 2000
        _: return 100
```

### **2. Логика покупки:**

#### **Проверка прогресса:**
- Проверка достижения 100% прогресса изучения
- Показ сообщения о недостаточном прогрессе

#### **Проверка ресурсов:**
- Проверка наличия достаточного количества осколков душ
- Показ сообщения о недостатке ресурсов

#### **Диалог подтверждения:**
- Подтверждение покупки с указанием стоимости
- Возможность отменить покупку

#### **Процесс покупки:**
- Списание осколков душ из хранилища
- Мгновенное изучение способности
- Автоматическое сохранение игры

### **3. Добавлена функция в AbilityLearningSystem.gd:**

```gdscript
func learn_ability_instantly(ability_id: String) -> bool:
    """Мгновенно изучает способность (для покупки за осколки душ)"""
    if not ability_id in ability_learning_config:
        print("Ошибка: способность не найдена в конфигурации: ", ability_id)
        return false
    
    # Устанавливаем способность как изученную
    if not ability_id in learning_progress:
        learning_progress[ability_id] = {
            "current_progress": 0,
            "is_learned": false,
            "learned_at": null,
            "sources": {}
        }
    
    var progress = learning_progress[ability_id]
    progress.is_learned = true
    progress.learned_at = Time.get_datetime_string_from_system()
    progress.current_progress = ability_learning_config[ability_id].required_progress
    
    # Отправляем сигнал
    ability_learned.emit(ability_id, progress.current_progress)
    print("🎉 СПОСОБНОСТЬ ИЗУЧЕНА ЗА ОСКОЛКИ ДУШ: ", ability_learning_config[ability_id].name)
    
    # Сохраняем прогресс
    _save_progress()
    
    return true
```

## 🎮 Как использовать:

### **1. Открытие экрана изучения:**
- Перейти на экран подготовки к путешествию
- Нажать кнопку "ИЗУЧЕНИЕ СПОСОБНОСТЕЙ"

### **2. Покупка способности:**
- Найти способность с 100% прогрессом в списке
- Нажать кнопку "Купить (X осколков)"
- Подтвердить покупку в диалоге

### **3. Результат:**
- Способность мгновенно изучается
- Осколки душ списываются из хранилища
- Способность становится доступной для активации

## 💡 Преимущества:

### **Для игрока:**
- **Основной способ получения** способностей
- **Мотивация** побеждать врагов для изучения
- **Стратегический выбор** - какие способности покупать в первую очередь

### **Для геймплея:**
- **Дополнительная валюта** для осколков душ
- **Прогрессивная система** - сначала изучаешь, потом покупаешь
- **Мотивация** собирать осколки душ и побеждать врагов

## 🔄 Workflow:

1. **Побеждаешь врагов** для изучения их способностей
2. **Достигаешь 100% прогресса** изучения способности
3. **Собираешь осколки душ** в боях
4. **Покупаешь способность** за осколки душ
5. **Активируешь** в окне управления пассивными способностями

## ⚠️ Ограничения:

- **Только способности с 100% прогрессом** можно покупать
- **Осколки душ** списываются из хранилища (не из забега)
- **Одна покупка** на способность (нельзя купить уже изученную)
- **Обязательный прогресс** - нельзя купить способность без изучения

## ✅ Статус: Реализовано

Система покупки способностей за осколки душ полностью функциональна!
