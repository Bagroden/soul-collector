# Исправление расчета MP и регенерации для пассивной способности "Мудрый"

## 🐛 Проблема:
Пассивная способность "Мудрый" (+5 мудрости) корректно отображалась в менеджере характеристик, но:
- Объем маны не увеличивался на 50 (5 * 10 = 50 MP)
- Регенерация MP не увеличивалась

## 🔍 Причина:
Двойной учет бонуса от пассивной способности:

### **1. В функции `calculate_stat_bonuses()`:**
```gdscript
# Мудрость: 1 мудрость = 10 МП, каждые 5 = 1 реген МП
mp_regen = int(float(wisdom) / 5.0)  # ← wisdom уже включает бонус!
```

### **2. В функции `apply_stat_bonuses()`:**
```gdscript
max_mp = base_mp + (wisdom * 10) + (level * 10)  # ← wisdom уже включает бонус!
```

### **3. Проблема:**
- `wisdom = base_wisdom + passive_wisdom_bonus` (итоговая мудрость)
- Но в расчетах мы снова используем `wisdom`, что приводит к двойному учету бонуса

## ✅ Решение:

### **1. Исправлена функция `calculate_stat_bonuses()`:**
```gdscript
# Мудрость: 1 мудрость = 10 МП, каждые 5 = 1 реген МП
# Используем базовую мудрость + бонусы от пассивных способностей
var total_wisdom = base_wisdom + passive_wisdom_bonus
mp_regen = int(float(total_wisdom) / 5.0)
```

### **2. Исправлена функция `apply_stat_bonuses()`:**
```gdscript
# Для MP используем базовую мудрость + бонусы от пассивных способностей отдельно
var total_wisdom = base_wisdom + passive_wisdom_bonus
max_mp = base_mp + (total_wisdom * 10) + (level * 10)
max_stamina = base_stamina + (endurance * 5) + (level * 10)
print("Макс. MP: ", max_mp, " (", base_mp, " + ", total_wisdom * 10, " + ", level * 10, ")")
```

## 🎯 Результат:

### **Теперь пассивная способность "Мудрый" работает корректно:**

#### **1. При активации (+5 мудрости):**
- **MP увеличивается на 50** (5 * 10 = 50 MP)
- **Регенерация MP увеличивается на 1** (5 / 5 = 1 реген)
- **Характеристика отображается правильно** в менеджере

#### **2. При деактивации (-5 мудрости):**
- **MP уменьшается на 50** (5 * 10 = 50 MP)
- **Регенерация MP уменьшается на 1** (5 / 5 = 1 реген)
- **Характеристика отображается правильно** в менеджере

#### **3. Формулы расчета:**
```gdscript
# Максимальная мана
max_mp = base_mp + (total_wisdom * 10) + (level * 10)
# где total_wisdom = base_wisdom + passive_wisdom_bonus

# Регенерация маны
mp_regen = int(total_wisdom / 5.0)
# где total_wisdom = base_wisdom + passive_wisdom_bonus
```

## ✅ Готово к тестированию!

Теперь пассивная способность "Мудрый":
- ✅ **Увеличивает MP на 50** - при активации
- ✅ **Увеличивает регенерацию MP на 1** - при активации
- ✅ **Уменьшает MP на 50** - при деактивации
- ✅ **Уменьшает регенерацию MP на 1** - при деактивации
- ✅ **Работает на любом уровне** - не ломается при повышении
- ✅ **Синхронизируется правильно** - между меню и боем

**Теперь "Мудрый" работает полностью корректно!** 🎯✨
