# ИСПРАВЛЕНИЕ КНОПКИ ПОБЕДЫ В ТЕСТОВОМ РЕЖИМЕ

## 🎯 ПРОБЛЕМА РЕШЕНА!

### **Проблема:**
После победы в тестовом режиме появлялась кнопка "Продолжить", которая отправляла на выбор комнаты для прохождения, вместо возврата к выбору врага в тестовом режиме.

### **Решение:**
Добавлена проверка на тестовый режим в функции `_on_next_pressed()` и изменен текст кнопки.

## 🔧 РЕАЛИЗОВАННЫЕ ИСПРАВЛЕНИЯ

### **1. Изменена функция `_on_next_pressed()`:**
```gdscript
func _on_next_pressed():
    # Скрываем кнопку
    var btn = $UI.get_node_or_null("Btn_Next")
    if btn:
        btn.visible = false
    
    # Проверяем, находимся ли мы в тестовом режиме
    if test_mode:
        print("Тестовый режим: возвращаемся к выбору врага")
        # В тестовом режиме возвращаемся к выбору врага
        SceneTransition.change_scene("res://Scenes/TestArena.tscn")
        return
    
    # Осколки душ за забег переносятся в хранилище только при завершении всего уровня
    # (при возврате к экрану подготовки персонажа)
    
    # Сохраняем данные игрока
    _save_player_data()
    
    # Автоматически сохраняем игру
    var player_manager_node = get_node_or_null("/root/PlayerManager")
    if player_manager_node:
        player_manager_node.save_game_data()
    
    # Переходим к выбору комнат
    SceneTransition.change_scene("res://Scenes/RoomSelector.tscn")
```

### **2. Изменена функция `_show_next_button()`:**
```gdscript
func _show_next_button():
    # Создаем кнопку "Далее" если её нет
    if not ui.has_node("Btn_Next"):
        var next_btn = Button.new()
        next_btn.name = "Btn_Next"
        next_btn.size_flags_horizontal = Control.SIZE_SHRINK_CENTER
        next_btn.size_flags_vertical = Control.SIZE_SHRINK_CENTER
        # Позиционируем кнопку в центре экрана
        next_btn.anchor_left = 0.5
        next_btn.anchor_right = 0.5
        next_btn.anchor_top = 0.5
        next_btn.anchor_bottom = 0.5
        next_btn.offset_left = -100
        next_btn.offset_right = 100
        next_btn.offset_top = -25
        next_btn.offset_bottom = 25
        ui.add_child(next_btn)
        next_btn.connect("pressed", Callable(self, "_on_next_pressed"))
    
    # Показываем кнопку с правильным текстом
    var btn = ui.get_node("Btn_Next")
    if btn:
        # Устанавливаем текст в зависимости от режима
        if test_mode:
            btn.text = "Выбрать другого врага"
        else:
            btn.text = "Далее →"
        btn.visible = true
    else:
        print("ОШИБКА: Кнопка 'Далее' не найдена!")
```

## 📊 РЕЗУЛЬТАТ

### **Теперь в тестовом режиме:**
- ✅ **Кнопка показывает правильный текст** - "Выбрать другого врага"
- ✅ **При нажатии возвращает к TestArena** - `res://Scenes/TestArena.tscn`
- ✅ **Не сохраняет данные игрока** - в тестовом режиме сохранение не нужно
- ✅ **Не переходит к RoomSelector** - избегает неправильного перехода

### **В обычном режиме:**
- ✅ **Кнопка показывает "Далее →"** - как и раньше
- ✅ **При нажатии переходит к RoomSelector** - `res://Scenes/RoomSelector.tscn`
- ✅ **Сохраняет данные игрока** - как и должно быть
- ✅ **Работает как обычно** - никаких изменений в обычном режиме

## 🎯 ЛОГИКА РАБОТЫ

### **Проверка тестового режима:**
```gdscript
if test_mode:
    print("Тестовый режим: возвращаемся к выбору врага")
    SceneTransition.change_scene("res://Scenes/TestArena.tscn")
    return
```

### **Условный текст кнопки:**
```gdscript
if test_mode:
    btn.text = "Выбрать другого врага"
else:
    btn.text = "Далее →"
```

## 🚀 ПРЕИМУЩЕСТВА РЕШЕНИЯ

### **1. Интуитивность:**
- В тестовом режиме кнопка говорит "Выбрать другого врага"
- В обычном режиме кнопка говорит "Далее →"
- Пользователь понимает, что произойдет при нажатии

### **2. Правильная навигация:**
- Тестовый режим → TestArena (выбор врага)
- Обычный режим → RoomSelector (выбор комнаты)
- Каждый режим ведет туда, куда должен

### **3. Отсутствие побочных эффектов:**
- В тестовом режиме не сохраняются данные
- В тестовом режиме не тратятся осколки душ
- Тестовый режим остается изолированным

## 🔍 ОТЛАДОЧНАЯ ИНФОРМАЦИЯ

### **Добавлено отладочное сообщение:**
```gdscript
print("Тестовый режим: возвращаемся к выбору врага")
```

### **Проверка режима:**
- `test_mode` - переменная, которая устанавливается при запуске тестового режима
- Проверяется в `_on_next_pressed()` перед выполнением обычной логики
- Если `test_mode = true`, выполняется ранний возврат

## 🎉 ИТОГ

### **Проблема полностью решена:**
- ✅ **Тестовый режим** - кнопка "Выбрать другого врага" → TestArena
- ✅ **Обычный режим** - кнопка "Далее →" → RoomSelector
- ✅ **Правильная навигация** в обоих режимах
- ✅ **Интуитивный интерфейс** с понятными кнопками

**Теперь после победы в тестовом режиме игрок автоматически возвращается к выбору врага для следующего теста!** 🚀

---

**Автор**: Claude AI Assistant  
**Дата**: 2024  
**Версия**: 1.0
