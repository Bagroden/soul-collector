# Инструкция по настройке универсальной системы эффектов способностей

> **📌 Быстрый старт:** Если вы впервые настраиваете систему, начните с `EXPORT_ANIMATIONS_QUICK_START.md` - там пошаговая инструкция с визуальными подсказками для Godot 4.5.1.

## Шаг 1: Экспорт анимаций из PlayerBody

### Способ 1: Через меню Редактор (для Godot 4.5.1)

1. Откройте Godot редактор
2. В верхнем меню выберите **Редактор** → **Выполнить скрипт** (или **Editor** → **Run Script**)
3. В открывшемся диалоге выберите файл `Scripts/Tools/ExportAbilityAnimationsEditor.gd`
4. Нажмите **Выполнить** (или **Run**)
5. Проверьте вывод в нижней панели (вкладка **Output**)
6. Убедитесь, что файл `Data/AbilityEffectAnimations.tres` создан

**Где найти Output панель:**
- Внизу экрана должна быть панель с вкладками (Output, Debugger, и т.д.)
- Если панель не видна: **Вид** → **Dock** → **Output** (или нажмите `Ctrl+Shift+O`)

**Если меню "Редактор" → "Выполнить скрипт" недоступно:**
- Убедитесь, что вы находитесь в редакторе (не в запущенной игре)
- Попробуйте перезапустить Godot
- Или используйте Способ 3 (автоматический экспорт)

### Способ 2: Через консоль (альтернативный)

Если меню Tools недоступно:

1. В нижней панели откройте вкладку **Output** (или нажмите `Ctrl+Shift+O`)
2. В консоли введите:

```gdscript
var exporter = preload("res://Scripts/Tools/ExportAbilityAnimations.gd").new()
exporter.export_ability_animations()
```

3. Нажмите Enter
4. Проверьте результат в консоли

### Способ 3: Автоматический экспорт при первом запуске (САМЫЙ ПРОСТОЙ!)

Система автоматически экспортирует анимации при первом запуске игры, если файл `Data/AbilityEffectAnimations.tres` не существует или пустой.

**Что нужно сделать:**
1. Просто запустите игру (F5 или кнопка "Игра")
2. Система автоматически экспортирует анимации при первом бое
3. Проверьте, что файл `Data/AbilityEffectAnimations.tres` создан в FileSystem

**Примечание:** Автоматический экспорт происходит только один раз при первом запуске. Если нужно обновить анимации, используйте Способ 1.

## Шаг 2: Проверка маппинга способностей

1. Откройте `Data/AbilityAnimations.tres`
2. Убедитесь, что все ID способностей правильно маппятся на имена анимаций:

```gdscript
ability_animations = {
    "rat_bite": "rat_bite_anim",
    "double_strike": "double_strike_anim",
    "poison_strike": "poison_strike_anim",
    # ... и т.д.
}
```

## Шаг 3: Тестирование

1. Запустите игру
2. Войдите в бой с врагом, который использует способность с эффектом
3. Проверьте, что эффект проигрывается корректно
4. Проверьте консоль на наличие ошибок или предупреждений

## Шаг 4: Миграция существующего кода (опционально)

Если вы хотите постепенно мигрировать со старой системы на новую:

1. Найдите все места, где используется `EffectVisual`
2. Замените их на вызовы `ability_effect_manager.play_ability_effect_on_target()`
3. Удалите узел `EffectVisual` из сцен персонажей (если он больше не нужен)

## Текущий статус миграции

- ✅ `double_strike` - мигрировано
- ✅ `rat_bite` - мигрировано
- ✅ `poison_strike` - мигрировано
- ✅ `crossbow_shot` - мигрировано
- ✅ `slashing_strike` - мигрировано
- ✅ `tombstone` - мигрировано
- ✅ `rending_claws` - мигрировано
- ✅ `bat_swoop` - мигрировано
- ✅ `acid_blast` - мигрировано
- ✅ `crushing_hammer` - мигрировано
- ✅ `shadow_spikes` - мигрировано

**Все активные способности врагов используют новую универсальную систему!**

## Примечания

- Новая система полностью заменила старую систему с `EffectVisual`
- Все эффекты теперь проигрываются через `AbilityEffectManager`
- Система автоматически создает и удаляет временные узлы эффектов
- Для добавления новых эффектов следуйте инструкциям в `UNIVERSAL_ABILITY_EFFECT_SYSTEM.md`
