# Система звуков для способностей

## 🎵 Обзор системы звуков

Система звуков позволяет добавлять звуковые эффекты к способностям, атакам и другим действиям в игре.

## 📁 Структура файлов

```
Audio/
├── Music/           # Фоновая музыка
│   └── Background/
└── Sounds/          # Звуковые эффекты
    ├── rat_bite.wav         # Звук крысиного укуса
    ├── sword_hit.ogg        # Звук удара мечом
    ├── magic_cast.ogg       # Звук магического заклинания
    ├── critical_hit.ogg     # Звук критического удара
    ├── damage_taken.ogg     # Звук получения урона
    ├── heal.ogg             # Звук лечения
    ├── button_click.ogg     # Звук нажатия кнопки
    └── level_up.ogg         # Звук повышения уровня
```

## 🔧 Как добавить звук к способности

### 1. Создайте звуковой файл
- Формат: OGG Vorbis (.ogg) или WAV (.wav)
- Длительность: 0.5-2.0 секунды
- Частота: 44.1 kHz
- Битрейт: 128 kbps (для OGG)
- Разместите в папке `Audio/Sounds/`

### 2. Добавьте звук в SoundManager
Откройте `Scripts/SoundManager.gd` и добавьте в функцию `_load_sound_effects()`:

```gdscript
func _load_sound_effects():
    # ... существующие звуки ...
    sound_effects["your_sound_name"] = preload("res://Audio/Sounds/your_sound_file.ogg")
```

### 3. Воспроизведите звук в способности
В файле способности добавьте:

```gdscript
# Воспроизводим звук
var sound_manager = get_node_or_null("/root/SoundManager")
if sound_manager:
    sound_manager.play_sound("your_sound_name", -10.0)  # Громкость -10 dB
```

## 🎚️ Настройка громкости

- **0.0 dB** - максимальная громкость
- **-10.0 dB** - средняя громкость (рекомендуется)
- **-20.0 dB** - тихая громкость
- **-30.0 dB** - очень тихая громкость

## 📋 Примеры использования

### Звук атаки
```gdscript
# В способности атаки
sound_manager.play_sound("sword_hit", -10.0)
```

### Звук в позиции
```gdscript
# Для 3D звука (например, у врага)
sound_manager.play_sound_at_position("rat_bite", enemy_position, -15.0)
```

### Звук критического удара
```gdscript
# При критическом ударе
if is_critical:
    sound_manager.play_sound("critical_hit", -5.0)  # Громче обычного
```

## 🎯 Рекомендации по звукам

### Для способностей крысы:
- **rat_bite.ogg** - короткий звук укуса/царапания
- **rat_squeak.ogg** - писк крысы при атаке

### Для магических способностей:
- **magic_cast.ogg** - звук произнесения заклинания
- **magic_impact.ogg** - звук попадания магии

### Для физических атак:
- **sword_hit.ogg** - звук удара мечом
- **punch.ogg** - звук удара кулаком

## 🔄 Обновление существующих способностей

Чтобы добавить звук к существующей способности:

1. Найдите файл способности в `Scripts/Abilities/`
2. Добавьте воспроизведение звука перед нанесением урона
3. Выберите подходящий звук из доступных
4. Настройте громкость

## 🎵 Интеграция с настройками

SoundManager автоматически интегрируется с SettingsManager для управления громкостью звуков через настройки игры.

## 📝 Примечания

- Поддерживаются форматы: OGG Vorbis (.ogg) и WAV (.wav)
- Рекомендуемая длительность: 0.5-2.0 секунды
- Не используйте слишком громкие звуки (избегайте clipping)
- Тестируйте звуки в контексте игры
